# Portal Design System

> **Canonical shared copy** in `control-plane/docs/ai/design/`. Origin: student-portal (extracted 23 Sep 2026). Update here for shared rules, then mirror to `student-portal/docs/design-system.md` and eventually `design-system`.

## Direction

The current portal is a dark-only, desktop-first product. Semantic tokens and reusable components are the supported way to express visual decisions. Mobile remediation is deferred to a later phase.

Open `/dev/design-system` during local development to view the live token and component catalogue.

## Token rules

Online Forms follows Finance's nested detail rhythm, Portfolio's editable-field styling and Lecturer Review's confirmation popup. Its Available Forms/My Forms tabs use the existing `TabbedPagePanel` with plain labels; unread reviewer updates sit in a separate warning `IssueCard` above the panel instead of a tab counter. Shared `UnreadBadge` remains available for contextual navigation counts so form counts are not called messages. Available Forms uses the shared banner slot, full-width `SegmentedTabs` and `LinkedList`; My Forms retains the bordered `DataTable` pattern. Form details use a `Prefilled Details` `DetailSectionCard` and route-level actions use `PageActionBar`.

`FileUploadField` owns distinct empty, uploaded, multiple-file, and read-only states inside one dashed container. Uploaded metadata sits in a raised, centred file tile with its filename, file size and accessible remove control inside that boundary; replacement uses the shared underlined link treatment and no source or selection label is shown. Guidance and validation follow the shared 8px field rhythm. Its consumer owns file selection, validation and storage; Finance keeps its existing single-proof 10 MiB policy while Online Forms supplies field-specific limits and optional MIME restrictions. Submitted Online Forms and university-held evidence use the read-only state so metadata remains legible without edit or delivery controls. Hidden native file inputs are excluded from tab order; the visible picker receives focus and validation associations. Date/select triggers accept explicit IDs and error descriptions. When a route workflow requires a structured document submission, its progress step links to the existing Online Forms lifecycle and reflects the form status; it does not embed a second one-off uploader inside the progress hub.

`ChatComposer`, `ChatMessageRow` and attachment presentation live under `components/system`, with compatibility wrappers for Community. Composer outer spacing is opt-in: Community retains its footer treatment while Online Forms uses a full-width composer whose divider reaches the edges of its borderless `surface` case-message inner section; reviewer avatar fallbacks use `surface-subtle`. Draft disposal is shared, pending sends preserve preview ownership until settlement, and sent previews belong to the current portal session. Case messages sit outside form details under the same page-title hierarchy, with a closely grouped explanatory label and a 48px inter-panel gap; they contain no delete controls, and closed cases replace the composer with a readable explanation. The Design System Explorer includes form-field, case-message and badge examples.

- Use five neutral elevation tiers directly in components: `background` (tier 0), `surface-subtle` (tier 1), `surface` (tier 2), `surface-raised` (tier 3), and `surface-elevated` (tier 4). Use `surface-subtle` for quiet inset content such as charts; dashboard module cards use `surface`; Academic module cards, grade accordions, timetable periods, Calendar agenda/details rows, and standard cards use `surface-raised`; overlays use `surface-elevated`. Use `surface-hover` for neutral interactive hover and selected states rather than a component-specific alias.
- Use direct global surface, brand-palette, and status tokens in components. Retain semantic text and intent tokens only where they communicate meaning, such as `brand`, `success`, `destructive`, and `warning`. The fully opaque `brand-cyan`, `brand-sky`, `brand`, and `brand-indigo` palette is for branded hierarchy and non-status breakdowns; do not use it to communicate success, warning, or destructive meaning. Typography uses a separate hierarchy: `title` for highest-emphasis content, headings, and card labels; `body` for readable content; and Muted for metadata. The implementation name is `muted-foreground` to distinguish it from the separate muted surface token.
- `success`, `warning`, and `destructive` form one balanced status palette: teal-green for positive or confirmed state, amber for attention or due work, and coral-red for overdue or destructive state. Semantic surfaces use the faded status colour, while visible text and icons use the accessible text role. Filled brand, success, warning and destructive controls use the separate `brand-solid`, `success-solid`, `warning-solid` and `destructive-solid` surface roles with light foreground content. This split keeps small semantic text readable on dark surfaces without making filled controls too light for their white labels.
- `Badge` is the reusable compact 400-weight status pill. Its standard `sm` size is 20px high; its `lg` size is 32px high with 12px horizontal padding and 14px body copy for prominent status rows and summary pills. The shared student summary uses the large `secondary` Badge with the `surface-elevated` background override. Its default brand-blue, `success`, `warning`, and `destructive` variants use faded semantic backgrounds with matching solid label copy. The `success-solid`, `warning-solid`, and `destructive-solid` variants use their solid semantic surfaces with light copy for a Badge inside a same-colour faded summary card; they do not use feature-specific status styling.
- `AttentionIndicator` is the reusable non-interactive 20px status marker for a compact action-needed signal. It uses the solid `warning` circle, a white exclamation mark, and an accessible label so colour is never its only meaning. Module Materials places one beside a current or completed week Badge when derived assessment state requires attention.
- Issue cards use a 12px radius and solid severity surfaces: warning cards retain the amber `warning` surface with light content; destructive cards use light content on `destructive-solid`. Amber is for upcoming or due-today work; red is for overdue work. Their hover surface darkens immediately to the same 80%-intensity treatment as the destructive `Action required` button, and they use a pointer cursor as the current card affordance.
- Neutral buttons, cards, menus, sidebars, text fields, and selects use `surface-hover` for their gray hover background. Brand, destructive, timetable, and media-overlay states retain their own semantic hover treatments. Hover changes are immediate: do not add colour, brightness, or opacity transitions for hover feedback.
- Timetable periods use the shared neutral system directly: scheduled classes use `surface-elevated`, while open periods use `surface-raised` at reduced opacity. Non-current class hover uses `surface-hover`; the surface background shows through their shared spacing. Each visible hourly label begins with a compact vertical marker using `muted-foreground` at reduced opacity.
- `TimetableCard` uses minute-based columns under hourly labels. The default 07:00–19:00 extent expands for early/evening classes and scrolls within the card when needed, preserving the 16px inset and existing colours. Expanded ranges reserve 3.25rem per hour plus the day label; normal ranges retain their compact desktop width. Accessible class names include exact times, and the Design System Explorer includes a 09:15–09:45 session and a 20:00–21:00 session.
- Short timetable classes (under one hour) use 4px horizontal content insets so padding does not consume their label space; longer classes retain 12px insets. Truncated metadata remains available through the shared tooltip and accessible class name.
- Performance charts use global roles directly: success for rising values, destructive for falling values, muted foreground for unchanged values, and border for chart reference lines. Trend meaning is reinforced by visible values and labels rather than colour alone.
- Calendar event identity reuses direct global roles: brand for classes, warning for assignments, destructive for exams, success for confirmed meetups, title for holidays, and the purple `registration` role repeated across every date in the module-registration window. The centred legend and agenda kind labels repeat the meaning so the dots never communicate category alone.
- Service identity accents (`service-amber`, `service-pink`, `service-blue`, and `service-red`) are global tokens reserved for their named student-service contexts. They are not status colours.
- Scrollable regions use the global `scrollbar-track`, `scrollbar-thumb`, and `scrollbar-thumb-hover` roles. Firefox uses the standard scrollbar properties; Chrome, Edge, and Safari use the matching WebKit selectors.
- The main portal scroller reserves a stable scrollbar gutter so route content keeps the same width whether that page overflows or not. The document root is overflow-hidden: expanding a resizeable field keeps overflow in this one portal scroller rather than creating a competing document scrollbar. Main route content retains a 12px bottom inset (while preserving its 48px top inset), leaving 4px of visual breathing room above the sidebar's bottom controls.
- Every pathname, query-string, or hash change immediately resets the main portal scroller to the top. The same rule applies to browser Back and Forward navigation; local interactions that do not change the URL retain their scroll position.
- Use the shared Tailwind spacing scale. Add layout tokens only for stable application dimensions such as page gutters, content widths, and sidebar widths.
- Dashboard section-card content uses 16px (`p-4`) padding. This includes module cards, article text and metadata areas, and the timetable surface; compact timetable period content remains an intentional exception.
- Use typography utilities for all application text instead of ad-hoc font sizes. The supported utilities are `type-label` (spaced uppercase), `type-label-normal`, `type-title`, `type-section`, `type-section-lg`, `type-body`, `type-metric`, and `type-badge`. Both label utilities use the 12px regular (400) style and define only size and weight; each component applies its own colour token according to meaning. Editable form and detail-row labels use `type-body text-title` at 14px, followed by the standard 8px field gap (`mt-2`); reserve `type-label` for metadata or group headings, not interactive field labels.
- All actionable button and tab labels use regular (400) weight. Emphasis belongs to content hierarchy and semantic titles, not to an action's default, active, or selected state.
- Standard labelled `Button` actions use one 36px (`h-9`) height across default and large call sites; the standard square icon size is also 36px. Explicit compact icon-only controls such as dialog close, sidebar toggles, and timetable removal retain their named compact variants, while inline link-style actions may use content height. The Design System Explorer's Button examples consume this shared rule directly.
- `type-section-lg` is the 24px, medium-weight, title-colour heading for a nested content title such as a module name. It appears in the Typography explorer above the standard 18px section style.
- `type-title` is the 16px, medium-weight, high-emphasis title style used for contextual card headings such as the selected `Semester 4`. `type-body` uses the general body-text colour rather than a sidebar-specific alias. The smallest supported portal text is 12px; do not introduce text below 10px.
- Use shared radius, shadow, duration, and easing values.
- Raw white, black, hexadecimal, or arbitrary values are allowed only for genuine media overlays or externally defined brand colours; otherwise introduce or reuse a semantic token.
- Keep `.dark` on the root for component-library compatibility, while treating dark as the only supported theme in this phase.
- Tooltip uses the tier-3 `surface-elevated` with `border` and the shared `floating` shadow so it stays distinct above lower-elevation content. Use it for supporting information that is otherwise visually clipped, not as a replacement for visible labels or controls.
- The Design System Explorer separates global tokens (surface tiers, typography, and shared roles) from component-specific tokens such as the cross-browser scrollbar. Components use these direct tokens rather than colour aliases.
- `npm run check:tokens` prevents component code from reintroducing the retired `foreground`, `primary`, `sidebar`, `card`, `popover`, `secondary`, `accent`, `hover`, or `unread` colour aliases. Layout-only variables such as `--sidebar-width` remain valid.

## Component layers

`PagePanelLayout` owns the shared rounded surface used for an optional navigation row, an optional flush banner and then content. When navigation is supplied, tabs or breadcrumbs sit above one divider; when omitted, the banner begins the panel without an empty header or divider. Content insets are standard (16px), wide (60px), or none; standard is the default. Announcement details use breadcrumb/wide mode, while University Policies uses the navigation-free banner mode. `TabbedPagePanel` composes this frame for its tabs, so Services uses the same structure while Profile and Finance retain their existing default spacing. The Design System Explorer shows breadcrumb, tab and navigation-free variants.

`QueryErrorState` is the shared student-facing read-failure state. It uses the same centred 12px-radius geometry as `EmptyState`, a neutral surface with the global destructive icon treatment, an assertive labelled alert, and an optional secondary **Try again** action. Route copy describes the information that is unavailable and what the student can do; it must not mention adapters, mock data, APIs or implementation details. Feature-owned follow-up actions may be composed beside retry when the route has an exact safe destination. The Design System Explorer includes the standard retryable state.

`TabbedPagePanel` supports opt-in `scrollable` tabs for longer lists, used by Services. It keeps a single row, standard 16px insets and the shared active indicator while revealing selected/focused tabs on navigation and container resizing. Reduced-motion preferences remove the indicator transition duration. Its optional `contentInset` delegates to `PagePanelLayout`; existing consumers retain the standard default. A consumer whose complete tab body is one self-framed table may explicitly select `none`, allowing that table to meet the panel edges without automatic content inspection. The Design System Explorer includes a constrained-width seven-tab example with keyboard navigation.

Services body content intentionally reproduces the source content region beneath the unchanged banner. Feature-owned `ServicesSectionContent` composes typed text, nested stack/split groups, media cards and `DataTable`; uncomposed sections continue to use `ContentSection`. Services selects the shared panel's no-inset mode, and its own body supplies 60px desktop padding, 32px section/column spacing and source-style dividers directly on the panel surface. It does not create a second page-sized rounded/gradient body container, nor alter `CardGrid`, `ContentSection`, `DataTable` or their other consumers.

The named Services container switches below 640px to 48px vertical/32px horizontal body padding and source phone order. Photos use centred cover cropping at 360px on wide containers and 220px on narrow containers; split-column photos stretch alongside text. Dish cards have overlaid captions, 110px desktop/140px narrow height, and three/one columns respectively. Illustrated benefit cards keep two columns and 60px icons. Failed media reserves the same geometry and exposes its alternative text. The department contact block is removed.

Source tables reuse semantic shared tables with feature-scoped cell borders, centered values and a contrast-checked caption strip using `--service-table-caption`. Hours use 40/60 columns; rates use equal columns. Font family, heading and body classes remain portal styles. The Design System Explorer contains the live Dining composition, the Accessibility empty-content treatment and a link to `/dev/services-content?slug=dining`, a query-backed isolated responsive canvas excluded from production. This canvas verifies content only, not full-portal mobile support.

| Layer                            | Responsibility                                                                                                                                       |
| -------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `components/ui`                  | Generic Radix/shadcn primitives without campus knowledge.                                                                                            |
| `components/system`              | Reusable portal patterns such as content sections, breadcrumbs, profile controls, issue cards, detail fields, tabbed panels, and community controls. |
| `components/features/<feature>`  | Feature-owned visual components. News, modules, timetable, and performance live here.                                                                |
| `components/portal-sidebar`      | The cohesive portal navigation and community/chat surface.                                                                                           |
| `app`                            | Route composition and application-level layout, including the main portal content.                                                                   |
| `contracts`, `services`, `mocks` | API boundary, validation, and development fixtures.                                                                                                  |

A component moves into `system` when it is used by multiple features or represents an approved portal-wide pattern. Single-use components remain next to their feature; generic library primitives remain in `components/ui`.

## Interaction and accessibility

The repeatable CAP-35 engineering audit uses `@axe-core/playwright` against WCAG 2.2 A/AA tags on representative production routes at 1280px and 1440px, both mock student scenarios and key dialog/Community states. Manual assertions cover focus restoration, keyboard access, forced colours, reduced motion and enlarged text. Automated results support the review but do not constitute formal conformance, screen-reader certification, physical-device testing or full mobile-shell acceptance. See [the dated audit report](audits/2026-09-09-accessibility-audit.md).

- The portal-wide menu header remains sticky while the main portal area scrolls, with a background layer that keeps its controls readable; its profile section shares the content's scrollbar-aware right gutter.
- The main header uses the shared page breadcrumb, which reflects the active top-level route and its navigation hierarchy. It replaces the former programme-and-year summary.
- The top-menu avatar is a direct Profile > Personal action. The sidebar's `Log out` control remains the sole visible frontend-only logout placeholder until authentication enters scope.
- Active student account issues add a solid destructive `Action required` button immediately to the right of the student's profile picture. It uses the same filled warning triangle treatment as destructive module issue cards, a pointer cursor, and no press-position movement; it navigates to Profile > Personal. Clear and attention states appear as live `StudentProfile` examples in the Design System Explorer.
- Interactive visuals must use a button, link, or appropriate accessible primitive.
- The chat composer uses a compact raised-surface menu-style icon Button for Add attachment, retaining the existing file-picker behavior.
- The floating portal sidebar keeps Navigation and Community as adjoining `surface` columns separated by a full-height shared border. Main navigation rows, accordion triggers, and Navigation's static bottom `Log out` placeholder use the same non-shrinking 36px raised-surface row treatment; the footer keeps its edge-to-edge divider and nested navigation links retain their compact child-row treatment. Expanded Navigation overflow stays in its scroll region rather than compressing row heights. An active Chat Composer uses the same footer structure, divider, padding, and 36px control row so it aligns exactly with Navigation's Log out footer.
- Enabled buttons, links, tabs, menu choices, and other button-like controls use the pointer cursor globally. Disabled controls keep their disabled affordance. Module cards, timetable class cards, and module-linked Calendar agenda cards use this shared interactive affordance for Module Details navigation.
- Keyboard focus must remain visible.
- Icons require accessible labels when they carry meaning and must be hidden when decorative.
- Hidden file inputs retain explicit accessible names even when a labelled button opens the native picker. Controlled dialogs restore focus to their originating action. Dynamic errors and completion states use the appropriate alert/status semantics already owned by their shared or feature components.
- Loading, empty, error, disabled, selected, hover, and focus states must be represented in the catalogue when supported.
- Colour must not be the sole carrier of status.
- The timetable presents one full-detail tooltip when either its class title or location is clipped; cards whose text fits do not show a tooltip. Online timetable classes additionally show the shared laptop indicator with an `Online Class` tooltip. Every class card is a keyboard-accessible Module Details action using its timetable record's module and semester IDs.
- `ArticleCarousel` uses native horizontal scrolling: trackpad sideways scroll and mouse drag move the same card strip, while the previous/next buttons provide an accessible alternative. Its title, accessible region label and View all action are configurable; Dashboard retains **News and Announcements** with View all, while the Announcement overview uses **Important Announcements** without the redundant link. Its scrollbar is visually hidden without disabling scrolling.
- The custom scrollbar applies globally to horizontal and vertical overflow. Forced-colour mode uses the operating system scrollbar colours to preserve accessibility.
- `IssueCard` is the reusable portal pattern for an issue summary. Warning cards use light content on `warning`; destructive cards use light title copy on `destructive-solid`. Severity is also expressed in visible text. The decorative alert icon is a light triangle with a small severity-coloured alert mark, centred in a 36px rounded square. Issue labels use regular 12px text, and the icon-to-copy gap is 8px. It supports start- and end-icon placement plus an optional accessible button action. Module assignment and exam issues use that action to open the exact learning-item detail; Online Forms uses a full-width warning card above its tabbed panel to open My Forms when reviewer updates are unread; static Profile examples retain the non-button presentation. Module cards use a non-interactive surface wrapper with separate module and issue actions, avoiding nested buttons while keeping full keyboard access. `OnlineClassIndicator` is the shared brand-colour laptop icon with its `Online Class` tooltip and explicit image semantics; Module cards and online timetable class cards use it. A current online timetable class switches the icon to the brand foreground for contrast against its brand background. Physical delivery remains visually unmarked but is included in the card's accessible name.
- `UnreadBadge` is the compact sidebar-specific count for programme and module conversation summaries. It uses the dark `background` surface, regular label weight, exact accessible unread copy, hides at zero, and caps visible three-digit counts at `99+`. It never displays roster size; Messages keeps its smaller avatar-attached unread treatment. The Design System Explorer shows the summary badge with other status controls.
- `DetailField` is the reusable record row used by Profile. Its `readOnly` variant top-aligns its high-emphasis value beside the label; `readOnlyStacked` places its label above the value for compact metadata columns; and `editable` vertically centres a supplied form control and optional destructive validation message. Every variant uses shared borders and 14px body-label typography; side-by-side variants use a 160px label column. They appear in the Design System Explorer.
- `DetailSectionCard` is the reusable transparent bordered wrapper for related detail rows. It has an 8px radius; its title and content areas each use 12px padding and are separated by a shared-border divider. Profile uses it for General, address, and emergency-contact groups; it appears in the Forms catalogue.
- `InnerSection` is the reusable quiet grouping inside a larger panel. Its default uses an 8px radius, border and 12px padding without adding a background or heading. Its opt-in borderless `raised` surface keeps related preparation content visually separate without adding another frame; Online Forms keeps the **Before you start** heading, explanatory copy, and any required-document list inside that surface, with eligibility guidance and the action bar as following siblings. The borderless `surface` variant supports the rounded Online Forms conversation, which can opt out of its default padding when a child composer needs edge-to-edge dividers. Module Details uses the default for metadata fields and the assessment outline; Module Registration composes it with `surface-subtle` to group the confirmation total. It appears in the Module detail patterns catalogue and Design System Explorer.
- `IconButton` is the reusable 36px square action primitive. It composes the bordered transparent secondary Button treatment, supports a native disabled state, and can add a shared Tooltip for an otherwise icon-only action. The Design System Explorer shows enabled and disabled examples. `FileLink` is the reusable underlined brand filename control with no icon; it changes to the high-emphasis title colour on hover. Until the Portal API provides an authorised file URL, its click is deliberately a no-op.
- Performance chart cards use Recharts, the quiet `surface-subtle` inset tier, shared 16px card padding, semantic chart tokens, system typography utilities, and the gray hover role for semester controls. Point values stay visually quiet until hover, when a semantic popover tooltip identifies the semester, metric, and value. Tooltip position changes are immediate rather than animated, so it follows the chart pointer without lag. The labelled chart container and pressed/disabled semester buttons provide the accessible chart experience; Recharts’ duplicate SVG focus layer is disabled to avoid a browser focus outline on mouse click. The selected-semester heading and active x-axis semester label use the high-emphasis `title` text colour. Academic Performance supplies an optional secondary `View breakdown` action aligned opposite the current value; dashboard cards omit the action. The detailed popup is a separate deferred production chunk.
- The performance loading state reserves the same two-card grid and uses the same quiet `surface-subtle` tier plus shared `Skeleton` surfaces while the near-viewport chart chunk loads. The Design System Explorer shows this state beside the completed chart card.
- `TabbedPagePanel` is the shared route-tab surface composed through `PagePanelLayout`: its tab row and default content region each own a 16px inset, with one `border` divider between them and clipped 12px corners. Opt-in scrolling tab rows use `scrollbar-hidden`, retaining pointer/keyboard scrolling and focused/active-tab visibility. Tabs have 4px gaps, regular-weight labels, a shared neutral active indicator that is measured before its first paint and then slides between tab labels with the same 200ms ease-out motion as the established segmented control, title colour for the active item, and Radix keyboard navigation. Selecting its active route tab calls the consumer again so it can restore that tab's default route.
- `TabbedPagePanel` accepts an optional `banner` slot immediately below its tab divider and before its content. `TabbedPagePanelBanner` is the shared 288px-high composition: cover image with centred cropping, no outer padding or individual radius, 32px text insets, a vertically centred 208px copy column, 24px `type-section-lg` title, 14px `type-body` description, and 16px copy gap. `textVariant` is dark (default) or light; optional `gradient` defaults to false and uses a contrasting white or black fade. `hideCopy` retains the image geometry without overlay title, description or gradient. These media text and overlay colours are intentional raw-colour exceptions. Missing or failed media uses `surface-subtle` with normal title/body tokens and preserves all text and height. Overflowing copy alone becomes a keyboard-focusable scroll region. The loading skeleton matches the fixed geometry. Optional image variants use shared `ResponsiveMedia`, with container-measured sizes and browser srcset selection. The Design System Explorer shows the dark default in a scrolling panel, a light variant with a gradient toggle, and a no-image/no-description fallback.
- `StudentSummaryCard` is the shared, read-only identity surface above every Profile and Academic `TabbedPagePanel`. Its borderless `surface` shell places an 80px student avatar beside the student's name, a shared divider, and a two-column academic row. Faculty and programme remain in the flexible left column; the vertically centred 32px `surface-elevated` Semester Badge stays at the far right. The shared loading skeleton reserves the same avatar, text, divider, and Badge geometry. Profile issues remain a separate Profile-only composition.
- `ProgrammeContextBar` is the shared route-context surface for students with more than one Programme Enrolment and for a selected completed, deferred, or withdrawn enrolment. It composes `SummaryCard`, the existing semantic Badge variants, and `DropdownSelect`; a single active enrolment renders no extra surface. Active multi-programme context is neutral, completed is success, deferred is warning, and withdrawn is destructive. The copy identifies the selected programme status and whether the student is reviewing history or waiting for Registry. Changing the selector updates `programmeEnrolmentId`, clears nested semester/detail state, and never exposes active-only actions for an inactive enrolment. Because it selects the owner for every route surface below it, `MainContent` places it immediately before the route-owned heading; this is the one documented shared context exception to the normal heading-first page anatomy. The Design System Explorer demonstrates active and completed selection without introducing a local dropdown or status colour.
- `SummaryCard` is the required shared surface for standalone compact summaries in Finance, Attendance, Module Details, Study Plan Programme Overview, Invoice Details, Module Registration and Graduation. It is compositional rather than page-variant based: `value` is required, while label, action, description, content-sized summary columns, per-column indicators/semantic tones, supporting content and `footerContent` are independent optional slots. The card owns its borderless 12px radius, default `surface-subtle` tier, 16px insets, 4px label-to-value gap, 8px description gap, tabular primary value, and vertically centred right-side action. Primary values use either 24px `type-section-lg` or metric emphasis; semantic headings supplied as values remain unstyled because the card owns that typography. Summary columns wrap with 24px horizontal and 16px vertical gaps, use muted `type-label-normal` labels above `type-body font-medium` values, and share one divider/padded detail region. Optional supporting content follows the columns by 20px inside that same region, as used by Module Registration's `CreditLoadBar`; `footerContent` creates a separate third section with its own flush divider and 16px inset, as used by Graduation department clearance, transfer-proof validation and documents. Footer titles use the normal `type-title` InnerSection-heading treatment and leave a 16px gap before their table or content. Graduation puts a large solid semantic status Badge in the SummaryCard action slot and follows related sections with an edge-to-edge `PageActionBar` for form, invoice and next-step actions. Convocation guidance and international Student pass guidance are separate borderless raised `InnerSection` siblings below their summaries because they are supporting sections rather than summary details. Omitted slots render no empty wrapper, gap, or divider. The `success`, `warning`, and `destructive` variants apply faded green, orange, and red surfaces while retaining readable title and metadata roles; pair a status Badge in these cards with the matching solid white-text Badge variant. The existing `raised` surface is the explicit stronger-elevation option; Module Attendance metric cards use it to sit above the surrounding tab surface. The legacy destructive surface remains reserved for attention states and supplies high-contrast copy. `StudentSummaryCard`, accordion-trigger metrics, table or popup totals, and chart cards are intentionally separate. Loading states compose the same card slots with Skeleton content. The Design System Explorer shows the approved slot combinations.
- Connected workflow timelines list only the milestones owned by the selected numbered step. They use the same semantic solid token for every representation of one state. Completed markers and connectors use `success-solid`, matching their `success-solid` badges; the current marker uses `warning-solid`, matching its `warning-solid` badge. Upcoming markers and dashed connectors stay neutral. Connectors extend to the next marker so the sequence reads as one continuous path.
- `DropdownSelect` composes the existing Dropdown Menu radio primitives into a full-width 36px selector. Its trigger uses `surface-raised`, `surface-hover`, title and muted text roles, and a neutral focus-visible surface change instead of a blue ring. Its reusable invalid state exposes `aria-invalid` and the same destructive border/ring treatment as `Input`; its disabled state is muted and non-interactive. Text `Input` uses the same 36px height, raised base surface, hover surface, and neutral focus treatment. Shared `Textarea` extends the same field states to resizeable long-form content with a 96px minimum height. Native read-only Inputs and Textareas retain readable/selectable values on `surface-subtle`, use muted text, and suppress hover surface changes without pretending the value is disabled. Default, invalid, disabled, and read-only examples appear in the Forms catalogue. Dropdown menus open with an 8px gap and inherit the shared elevated treatment.
- `DatePicker` is the shared controlled date-only field: a 36px raised-surface trigger with calendar icon, `en-MY` date label, matching invalid/disabled/focus states, and a `surface-elevated` DayPicker popover. Its calendar header keeps previous/next controls and adds portal-styled month and year dropdowns covering 100 years before through 20 years after the current year, extended to include an existing out-of-range value. Enabled day buttons use `surface-hover` on hover, `surface-elevated` while pressed, and a solid brand selected state with white text. It accepts nullable `YYYY-MM-DD` values, returns timezone-safe date-only values, closes after selection or clear, and restores focus to its trigger on dismissal. Non-form calendar views keep their existing headers. The Forms catalogue shows default, selected, invalid, disabled, and clearable states.
- `SemesterSelector` is the shared labelled, full-width semester dropdown field used by Academic Performance, Academic Modules, Academic Timetable, Academic Attendance, and GPA breakdowns. It owns the visible muted `Semester` label and its 8px label-to-control gap so a route cannot accidentally omit or restyle that field label. Separate 36px raised-surface `ArrowNavigationButton` controls sit on its left and right edges for one-step movement, while the field supports direct selection of every available semester. Main Academic pages keep future semesters enabled but pass `mutedSemesterNumbers` so the dropdown and selected value communicate unavailable data without blocking exploration. GPA breakdowns instead constrain selection and arrow bounds to published semester numbers. Its live example appears in the Design System Explorer Forms group.
- `EmptyState` is the reusable illustrated no-data pattern for a valid query with no content. It uses a contextual Lucide icon in a faded-brand tile, a short high-emphasis title, supporting body copy, and an optional action slot, on `surface-subtle` by default. Module Registration uses the default tier for its closed completed/missed feedback; its completed state places the Study Plan link in the action slot. The Dashboard's registration-week Timetable and Modules states place a lightweight `Go to Module Registration` link in that same slot. Its `surface` variant is used by the Dashboard's Timetable, Modules, and Academic Performance sections so those primary content cards sit one clear tier above the page background; Academic future-semester states keep the quieter default. Both variants share spacing, typography, and status semantics.
- Route-level page shells must be derived from comparable completed pages before feature composition begins. Use the repository-local `$cms-student-portal-page` skill for new pages and substantial shell restructures, inspect the established shell plus at least two relevant pages, and reuse their primary heading treatment, top-level spacing, content width, and loading/error geometry. Profile, Academic, and Finance use `pageSectionTitleVariants()` for the route heading and a 16px (`gap-4`) top-level hierarchy; raw `type-section-lg` is for nested feature/detail headings, not a substitute for that shared route title. Any intentional page-shell divergence must be documented and covered by a focused layout assertion.
- `ContentSection` is the single shared content-section composition. Its required `type` distinguishes the existing page treatment from the panel treatment: `page` retains the 18px `type-section` heading, 32px content gap, optional boxed/plain presentation, custom heading, and sidebar variant; `panel` owns the 16px `type-title` heading, stable 36px action/no-action header row, direct unboxed content, and 16px content gap. Generic section/header slots expose `data-type` and `data-has-actions` for reliable layout checks. Dashboard, community sidebar, Profile, and Academic routes use this component, and the Design System Explorer presents the page type beside both panel action states.
- `CardGrid` is the shared grid for repeated portal cards. Every variant uses a 16px gap: compact metric cards use responsive 160px-minimum columns, module cards responsive 224px-minimum columns, account-issue cards responsive 240px-minimum columns, lecturer cards responsive 288px-minimum columns, performance cards responsive 352px-minimum columns, and the Profile Portfolio variant uses four equal columns. The account-issue variant caps at four columns, then reduces its column count as its own available width narrows. Use it for independent repeated cards in dashboard sections and page panels; do not use it for intentional structural grids such as the timetable, calendar, detail fields, or the horizontally scrolling News strip. Its live variants, including the neutral Portfolio layout sample, appear in the Design System Explorer.
- Student Finance uses the shared destructive `SummaryCard` surface for a positive outstanding balance and the default raised surface for clear or credit balances; its secondary How to Pay button occupies the shared right-side action column and opens the shared `Popup`. Like Academic Module Details and Profile Portfolio Details, nested Invoice Details remains inside the parent `TabbedPagePanel` with Invoices active. Its inner breadcrumb begins `Invoices > Invoice Details`, followed by a SummaryCard identity whose divided, content-sized summary-items row owns Category, Semester, Issue Date, and Charge Amount. One full-width Invoice Activity `DetailSectionCard` uses the shared `DataTable` footer divider to separate chronological ledger rows from the net balance. Its compact bottom-right action row keeps eligible disabled Download on the left and Submit Transfer Proof on the right, side by side, and nested loading/read-error/unavailable states reserve the same tabbed-panel geometry. `DataTable` provides keyboard-addressable rows and opt-in footer separation, while semantic `Badge` variants communicate invoice/payment state. Positive outstanding values, charges, balances, and debits use `destructive`; account credits, confirmed payments, allocations, and credits use `success`; zero, pending, void, and reversed amounts remain neutral. The Finance proof picker remains feature-owned because its PDF/JPEG/PNG policy and pending-proof lifecycle are Finance-specific. Tables use contained horizontal overflow on narrower desktop layouts rather than widening the portal shell.

- `TooltipIcon` is the reusable system treatment for inline supporting help on an existing action. Its render-trigger API appends the information glyph inside one supplied focusable control, then uses the shared Tooltip surface for hover and keyboard-focus content. It must not add a separately focusable icon inside a button. Controlled `Popup` composition may omit the Popup's built-in trigger so one action can safely own both tooltip and dialog behaviour.
- Lecturer cards use the `CardGrid` lecturer variant with a 288px readable minimum. Each card uses `surface-elevated`, 8px vertical and 12px horizontal padding, a 48px avatar, body-size title-colour name, and the shared brand Button. Review remains enabled but is a documented no-op until its workflow enters scope.
- `ContentBreadcrumb` is the reusable nested-content trail. Its parent crumb is a keyboard-accessible action and the final crumb uses the current-page semantics from the breadcrumb primitive.
- `SegmentedBar` is the reusable proportional breakdown. It accepts labelled numeric segments, renders every segment in the opaque direct `brand` colour, sizes columns by their values, and repeats every percentage in text so colour and width are not the only carriers of meaning. Only the outer start and end segments are rounded; internal joins remain square.
- `SegmentedTabs` is the compact local-view switcher used by Module Details. Its container uses `surface-subtle` with the brand active indicator. Module Details uses it for Summary, Materials, and Attendance, and reuses read-only `DetailField` rows for its metadata. By default, multi-tab instances show brighter `muted-foreground` dividers only between adjacent inactive tabs, never on either side of the active tab. Selecting an already-active shared segmented tab calls its consumer again, allowing route-controlled views to restore their default tier. The Design System Explorer shows Summary, Materials, and the completed module Attendance composition with its metric grid and read-only calendar. Community is now one 18px-headed root with nested Back navigation, not another tab style.
- `PageActionBar` is the shared final action area for route-level panels and inner-page forms. It may carry a left-aligned status or validation summary. Its opt-in `edgeToEdge` mode lets the divider bleed across a standard 16px content inset while keeping its status and right-aligned actions inset; route-level Finance invoice, Profile form, Lecturer Review, Graduation, Immigration, Module Registration, and Online Forms actions use this mode wherever their parent owns that inset. Embedded action groups inside a nested column, such as Portfolio project details, retain their local divider. Do not use it for table-row controls, compact cards, or popup footers.
- `StudentNextActionControl` is the shared renderer for a route-facing next step. The feature must choose one explicit model outcome: an exact portal route rendered through `Button` and `Link`, an exact validated Community service opened through the existing Community behavior, or readable unavailable guidance with no misleading control. The renderer does not infer a destination or turn a campus-owned process into a portal action. Compose it in `EmptyState`, `SummaryCard.action`, or `PageActionBar` according to the owning page anatomy. The Design System Explorer shows all three outcomes.
- Default brand buttons retain a transparent border so they match bordered button heights. Shared Button variants use regular-weight labels and do not translate or scale when pressed. `secondary` is the bordered neutral action variant; it uses a transparent base surface and `surface-hover` on hover. `ProfileFormActions` composes these variants for Profile save/cancel actions. Shared `AddRowButton` and `RemoveRowButton` provide the 36px dashed add-row and plain trash actions used by Profile contacts and Module Registration rows. The Design System Explorer Forms group shows text inputs, dropdown selects, detail fields, and both row actions; overlays and feedback remain separate.
- `StatusIndicator` is the shared solid success/destructive check-or-X circle used by Study Plan requirements and credit validation. `CreditLoadBar` renders one eight-pixel segment per allowed whole credit, fills selected credits with `brand`, leaves remaining segments on a reduced-opacity raised surface, and pairs its text status with `StatusIndicator`. Only the two outside ends are rounded. Both appear in the Design System Explorer Actions and status group.
- `TimetableCard` has a registration mode in addition to the standard navigable timetable. Registration mode keeps Monday through Sunday visible, uses brand class blocks with accessible remove actions, and leaves empty periods as the standard reduced-opacity raised surface without add/no-entry controls. It receives exact selected Offering schedules and does not invent a second registration-only timetable model.
- The shared Profile shell composes its title, `StudentSummaryCard`, active-issue `CardGrid`, and `TabbedPagePanel` with the same 16px hierarchy as Academic routes. The card stays above the panel on Personal, Academic, Resume, Portfolio, Documents, and Privacy & Security; active issues remain above the tabs and use destructive or warning roles without profile-specific colour tokens. Profile > Personal then composes `DetailField`, `DropdownSelect`, `Input`, and shared button components inside the panel; detail labels use 14px body typography and form controls use direct neutral surface tiers.
- Profile > Resume composes the shared `Textarea`, panel-type `ContentSection`, bordered `DataTable`, subtle `EmptyState`, secondary Add `Button` with a leading plus icon, `RemoveRowButton`, and controlled `Popup` patterns. Its five major sections use 32px gaps, while every stable 36px section header keeps a 16px gap to its content. Table rows support pointer, Enter, and Space editing; independent trash controls open a confirmation Popup without activating the row. Add/Edit and confirmation footers retain the shared full-width button treatment.
- Profile > Portfolio composes the shared panel-type `ContentSection`, Portfolio `CardGrid`, `ContentBreadcrumb`, `Input`, `DatePicker`, `Textarea`, `Badge`, `Button`, confirmation `Popup`, and `MediaPreviewLightbox` patterns. `PortfolioProjectCard`, the dashed Add Project tile, PDF artwork tile, and `PortfolioArtworkPicker` remain feature-owned because their artwork, routing, and session-file behaviour is specific to Portfolio. Card detail actions and overflow menus are siblings, avoiding nested interactive controls.
- Profile's emergency-contact remove control uses the normal muted-to-title neutral action treatment, not the destructive colour, while its disabled state still communicates the required final contact.
- `Accordion` is the shared Radix-backed disclosure primitive. Its trigger row uses immediate `surface-hover` pointer and neutral focus treatment and retains that colour while expanded; disclosed content retains the consumer-supplied base surface. It has an instant state chevron and native keyboard and screen-reader semantics; consumers own their surface, padding, divider, and content composition. Module Materials uses one `surface-raised` item for both the trigger and disclosed rows, a padding-free content wrapper, and equal 12px padding on every inline row; shared dividers separate multiple rows. Grade accordions retain the same joined surface treatment. Only one item is open in the grade-list and Module Materials week compositions, and selecting it again collapses it. The Design System Explorer shows the expanded shared pattern.
- `LinkedList` is the reusable non-disclosure counterpart to an accordion trigger: each item is one full-width route link, a native action button, or a clearly unavailable row. Route items use `to`; action items use `onSelect`; items with neither retain the disabled presentation. All enabled rows share the raised surface, 16px inset, immediate neutral hover/focus treatment and end chevron. A row can use either the optional Lucide icon slot or a decorative thumbnail slot before its title/description column, each separated by a 12px gap; Announcements uses 112px-by-64px cover/fallback thumbnails. Titles use default body typography in the standard high-emphasis white token; optional descriptions use the muted normal-label treatment with the shared 4px title-to-description gap from Announcement rows. Leading visuals and the chevron are decorative; rows do not nest secondary interactive controls. The Design System Explorer shows icon, thumbnail, action and unavailable examples.
- `LearningItemRow` is the feature-owned, reusable Materials row for resource and assessment kinds. Rows do not show progress or lock-status pills—the parent week Accordion trigger is the single status location. Assessment labels append their percentage in brackets. Released assignments use a warning `Due by DD/MM/YYYY` Badge, while released quizzes and exams use `Scheduled on DD/MM/YYYY`; locked rows receive only release-safe metadata and suppress assessment weight and date. Every released row is one full-width keyboard-accessible button with immediate `surface-hover` feedback and no nested action buttons. Assignment and exam rows use their supplied exact-detail callback; released resources and quizzes remain enabled no-op placeholders until their workflows are approved. Locked rows are natively disabled, muted, and do not receive hover feedback. A direct locked URL uses the neutral shared `SummaryCard` with a secondary Locked Badge rather than a local layout. The Design System Explorer shows released and locked states.
- The Module Details heading composition places the API-supplied module code in muted body typography above its `type-section-lg` module name. The Design System Explorer mirrors this hierarchy after the breadcrumb.
- Horizontally overflowing News cards reuse `useHorizontalScroll` for responsive visible-card calculation, arrow state, trackpad scrolling, and pointer drag.
- `ContentBreadcrumb` accepts either its original parent/current pair or a reusable list of entries, allowing nested Materials details to retain keyboard-accessible return links without introducing a second breadcrumb primitive.
- `DataTable` is the generic semantic typed table for records and labelled calculations. Consumers supply columns, row identities, optional totals, empty copy, accessible table labels, and may request left, centre, or right alignment; equal-width columns; a visible terminal divider where a composition needs one; or accessible pointer/Enter/Space row activation. The default table omits its final body-row divider; its `bordered` variant remains otherwise borderless inside an unpadded rounded outer frame, applies `surface-raised` only to its header row, then adds 8px header padding, 12px-by-8px body-cell padding, and 16px outer-column insets. `frameClassName` is an explicit composition escape hatch for a parent panel that owns the frame. The component owns shared spacing, borders, numeric tabularity, immediate interactive-row hover, and horizontal overflow. Module Registration, Study Plan, grade assessments, GPA, and CGPA reuse it, and the Design System Explorer shows both default and bordered examples.
- `Popup` is the reusable titled overlay composition over the shared Radix dialog primitive. It owns a bordered header, content, and footer: header/content use 24px padding and the centred footer uses 16px. Footer actions flex to fill the available row width; when both a primary action and dismissal action are present, they share that width equally. The header's title/description group is one 8px-gap sibling of the absolute close control, which sits 16px from the top-right edge; descriptions use normal body typography and body colour. It uses the default `surface` background, a secondary footer dismissal button (`Back` by default; consumers may use `Cancel`), supported widths, accessible title/optional description, overlay, close control, keyboard dismissal, and focus return while consumers provide content. Optional controlled `open`/`onOpenChange` state lets a shared popup serve multiple triggers, as in Module Registration's title action and table rows. That editor composes the shared read-only Input treatment for every selected section schedule: each weekly class exposes Day and 24-hour Time, while Location is shown only for physical or hybrid delivery. Dialog, sheet, and lightbox overlays use a plain 50% black backdrop without blur. Its live example belongs to the Explorer's Overlays and feedback group.
- `MediaPreviewLightbox` is the shared deferred media-viewer dialog used by chat image thumbnails and Portfolio artwork. Raster images retain the existing zoom, pan, gallery navigation, keyboard dismissal, and focus-return behaviour. PDF items use the same modal shell and close behaviour with an embedded document viewer while omitting image-only zoom controls. The Design System Explorer exposes the shared image state; Portfolio supplies either its raster preview URL or session PDF object URL.
- Academic grade accordions use `surface-raised` within the tabbed panel and vertically centre their grade, module, credit, and chevron content. The supplied result outcome applies `success` or `destructive` to the visible grade label, while the accessible trigger label states the outcome so colour is not its only representation. Expanded content composes `DataTable` for assessment, weight, score, contribution, and total rows. These controls disclose results inline and do not navigate.
- Academic Study Plan composes the same shared panel-type `ContentSection`, `Accordion`, `DataTable`, `Badge`, and `Button` components rather than introducing another tab, disclosure, table, status, or action primitive. Its Programme Overview wrapper uses the shared `surface-subtle` tier. Semester items form one vertical list of joined `surface-raised` rows; every item starts collapsed, and a single item may be open at a time before collapsing again when selected. Each trigger normally keeps its fixed-width four-metric summary beside the semester name, at 12px gaps with shared-border dividers after Credits Attempted. Its component-width breakpoint moves that summary to a full-width four-column strip below the name before it can collide with the Badge or chevron; compact-strip labels then use a fixed two-line area, keeping labels top-aligned and values aligned without adding empty space to normal rows. It omits the period subtitle and duplicate load label, and leaves the Badge plus chevron at the far right. Expanded content contains only the fixed-layout module table, with a 120px Status column, compact 80px Credits and Grade columns, and a locally scoped last-row border removal. Its feature-owned eligibility requirements place a solid `success` check or `destructive` X after each value with a dark `background` glyph and accessible met/not-met label; the combined Programme Status changes only when all four requirements pass. The overview and module rows remain read-only, while the plain no-icon transcript Button is a documented no-op brand action in a dedicated right-hand Programme Status column.
- Academic Modules reuses the shared `ModuleCard` grid, including its spaced muted `type-label` module code above the title, with an online-class indicator aligned beside it at the card top, direct surface roles, online-class tooltip, lecturer text, and reusable issue cards. Community sidebar module rows and module-chat headers use the same code hierarchy. Dashboard cards use `surface`; cards inside the Academic page panel use the reusable `raised` variant with `surface-raised`, while both keep `surface-hover`. The shared semester selector controls which enrolment-backed cards are visible; empty semesters use `surface-subtle` without introducing feature-specific tokens. The Design System Explorer's live Module Card example reflects the same code hierarchy.
- The Calendar month grid wraps DayPicker behind a portal-owned UI primitive. Dashboard Calendar and Academic Attendance share `CalendarMonthSelector`, whose normal-flow three-column row places the previous control, centred month label, and next control together and owns the full-width `border` divider before the grid. The month label and in-semester day labels use the high-emphasis `title` colour. Compact day cards centre their body-style date labels and event markers horizontally, with date labels aligned to the top; their base uses the translucent `surface-raised` treatment from empty timetable periods and every enabled selected or hovered day uses `surface-hover`. Today is identified only by its brand-colour date label, without a separate fill or hover rule. The grid has 8px visible gutters in both directions. Active-semester boundary dates keep their translucent cards visible but use muted date text to communicate their disabled state, and custom day buttons retain the forwarded keyboard and ARIA behaviour. Agenda cards use `surface-raised` with 12px padding and present kind, optional separate-line module code, and title without descriptions; location and clock-icon time share one metadata row. Module-linked agenda cards are accessible buttons and use `surface-hover`; non-module events remain static. The outer surface owns no content padding: the compact month selector and grid each own an 8px inset, while the legend and selected-day agenda retain their roomier 12px horizontal inset. The details section uses 20px top padding and 16px bottom padding. Attendance uses this same calendar treatment at full container width, with the outer `surface` bounded by the shared `border` token. `ArrowNavigationButton` is the shared 28px previous/next control for the News carousel and Calendar month navigation, so their icon, hover, focus, and disabled states remain identical. The selected-day agenda and legend are shown with the layout-matched Calendar loading state in the Design System Explorer.
- Academic Attendance reuses the Calendar primitive and day-card treatment but replaces event dots with one 8px-high segment per scheduled class session. A single segment is fully rounded; multiple equal-width segments use 4px gaps and round only the outer left and right ends. `success` means Present, `destructive` means Absent, and `surface-elevated` means the derived Not taken state. The centred legend repeats these states with matching 8px-high rounded bars, and selected-day rows use the shared larger Badge size so colour is not the only carrier of meaning. Module Attendance uses the same component's explicit read-only mode: days become non-interactive labelled groups without hover/selection or a details region, while month navigation and the legend remain available.
- Desktop sidebar width and off-canvas motion uses the shared 200ms ease-out timing. Reduced-motion mode removes the duration while preserving the same final layout and controls.

Profile Documents reuses the existing Profile shell and one panel-type `ContentSection`. Student Documents uses title-only `LinkedList` actions with a document icon and decorative chevron; activation intentionally has no effect because document delivery remains outside this frontend phase. The former University Documents panel was removed when Student Handbook moved into CAP-55. Loading, empty, and error feedback remain inside the Profile panel. See [Profile Documents](profile-documents.md).

Profile Privacy & Security reuses the same panel inset and 32px section rhythm. Its information/access sections compose bordered `DataTable`; security guidance uses the feature-owned `PrivacySecurityGuidanceCard` in a responsive auto-fit grid. Each card owns a borderless raised surface, responsive 16:9 image, 16px text inset and a fixed-geometry accessible fallback when media fails. The borderless request surface combines neutral explanatory copy with the shared `Button`. The Design System Explorer includes the image-card state. See [Privacy & Security](privacy-security.md).

`TabbedPagePanel` coalesces mouse-down/focus selection into one change per pointer gesture and fires one reset action when the already active tab is selected. Its keyboard behaviour is preserved. The live Content sections example reports activation counts so duplicate events are visible. The main portal scroller resets on router location-key changes as well as path/query/hash changes, including reselecting the current sidebar destination.

## Review checklist

- Does the component use semantic tokens?
- Is an existing primitive or system component suitable?
- Is data passed through props or a feature query instead of imported from fixtures?
- Are all interactive states keyboard accessible?
- Is the component shown in the design-system catalogue if it establishes a reusable pattern?

## Community service identities

`ServiceMark` maps validated `StudentService.iconKey` values to a consistent circular icon treatment in Community rows, empty states and conversation headers. The `helpdesk` key uses the Lucide headset icon with the existing semantic blue service accent. The Design System Explorer displays all supported service identities together so contract and presentation additions remain reviewable.

## Lecturer review patterns

Academic's feature-owned `LecturerReviewHeader`, `ReviewQuestionnaireSection` and `ReviewRatingRow` reuse Avatar, ContentSection, native radio inputs, Textarea, Button, ContentBreadcrumb and Popup. The header uses a 96px avatar, 24px gap, `type-section-lg` name and `type-body` faculty. The page keeps the normal 16px panel inset and 32px section rhythm. Rating rows use `surface-raised`, 12px corners, 16px padding and portal body text; 36px targets contain 16px radio dots. Selection uses brand, invalid guidance uses destructive, and a visible outline surrounds the focused target. Below a 40rem feature-container width the rating controls stack below the question, so Community resizing does not compress text or overflow. This is desktop container adaptation, not full mobile support.

The submitted variant replaces editable dots with readable numeric and named ratings. Feedback preserves line breaks and wraps long text. The Design System Explorer includes editable, required-error and submitted examples. Module Details and Invoice Details informed shell/breadcrumb/state geometry; the review route intentionally replaces the nested module summary and segmented tabs, matching the supplied reference while retaining the outer Academic panel. `Popup` additionally accepts opt-in `onCloseAutoFocus` for controlled confirmations without a DialogTrigger; existing consumers retain Radix defaults. Review cancellation restores the submit button; successful submission focuses the receipt.

`Popup.dismissDisabled` is opt-in for an in-flight irreversible action: it disables close/cancel and prevents Escape/outside dismissal until the request settles. Existing consumers default to normal dismissal. Lecturer Review uses it only after the student confirms submission.

## Announcement articles

[Announcements](announcements.md) follows the University Policies page heading, root gap and navigation-free banner panel. Its overview uses the generated 288px campus-courtyard banner and two titled sections: **Important Announcements** for pinned notices and **All Announcements** for the complete publication-ordered collection. Both use the shared `LinkedList`; the pinned section is omitted when no notices are pinned. Each row uses a decorative 112px-by-64px cover/fallback thumbnail, title, summary and chevron as one keyboard-accessible route action. Errors, loading and empty states keep the heading, banner and panel geometry. Details retain `PagePanelLayout`: breadcrumb, divider, flush 288px cover, then a 60px content inset. Missing and failed media use the faded-blue geometry with one centred icon; present covers crop to fill it. Detail-cover loading is eager and body loading lazy. The Design System Explorer shows the list sections beside both detail variants.

The Design System Explorer shows query-backed breadcrumb/banner page panels for cover and no-cover articles, including inline media. `contentImageSchema` is shared with Services, whose export and data remain compatible. `useHorizontalScroll` has opt-in `dragLinks` support used by the announcement carousel: an eight-pixel drag threshold, pointer-click suppression following drag, and normal keyboard links. Default consumers remain unchanged. Standalone sidebar destinations now expose `aria-current=page` consistently with submenu destinations.

## University Policies catalogue

[University Policies](university-policies.md) uses the shared 16px page-heading rhythm and a navigation-free `PagePanelLayout`. Its 288px `TabbedPagePanelBanner` uses the existing Cyberjaya library image without overlay copy or a gradient. A full-width `SegmentedTabs` row follows in the panel content, preserving inactive-neighbour dividers and 24px to the active list. Each policy is a raised `LinkedList` action row with a decorative policy icon and chevron. The current no-op action deliberately keeps native pointer, Enter and Space behavior while CAP-55 detail content is unavailable.

## Graduation composition and completed-student state

CAP-15 reuses the Academic heading and StudentSummaryCard, the scrollable TabbedPagePanel, full-width SegmentedTabs, compact SummaryCard, panel ContentSection, DataTable, DetailSectionCard, existing status badges and Buttons. The four numbered Graduation steps and separate Complete page use the existing segmented control with inactive-neighbour dividers, a 32px control-to-page gap and completed-step check icons. Each active page owns its summary and sections, so no new shared step component is introduced. Clearance under review uses the shared faded-warning SummaryCard treatment; its department checks live in the SummaryCard footer with solid status badges. Step 4 uses separate Documents and applicable Student pass SummaryCards so each process owns its semantic variant; untouched Documents uses the default neutral surface. The final Complete tab uses a default neutral SummaryCard with a green checked icon above its title. Loading and error bodies stay within the Academic shell; text labels accompany all status colors.

URL-backed progress hubs reuse Graduation's state composition as well as its navigator. Each selected phase begins with one `SummaryCard`; the action slot uses student-facing status language, determining checks or progress history use `footerContent`, and an invoice total stays in the fee summary rather than a nested card. `PageActionBar` contains only actions owned by that phase. Completed phases name the next destination with **Proceed to Step N**; generic current-step actions and links belonging to later phases are excluded. A numbered business step may move through review, fee, processing, and collection sub-phases while retaining the same summary anatomy. Immigration keeps its case-specific five-, four-, and three-step structures. Its timelines use solid completed connectors with checked circles, dashed unfinished connectors with numbered circles, and semantic text badges. Student Pass status consistently maps valid to success/green, expiring or active processing to warning/orange, and expired or cancelled to destructive/red. Cards, badges, markers, and connectors use the same semantic family; feature code must not introduce alternate status colours. A completion page is included only when the product needs completed workflow content to remain browsable; CAP-50 instead removes the completed case from Applications and returns to its empty state. The repository page skill contains the maintained state matrix used when adding or restructuring another step workflow.

StudentSummaryCard accepts a null semester and renders **No current semester** rather than fabricating a current term; this variant is visible in Design System Explorer. Online Forms renders declared `select` choices through DropdownSelect and conditional required labels/errors through its shared FormFields renderer; gown size appears and is required when attending. The sixth template uses the same panel, field spacing, immutable submitted controls and Messages layout as other Online Forms.

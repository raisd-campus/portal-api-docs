# Frontend Logical Data Model

## What this model is

The data model is a version-controlled description of information required by the frontend. It is intentionally independent of database technology. The future backend may use one database, several databases, existing CMS tables, or service calls as long as the Portal API fulfils these contracts.

Open `/dev/data-model` during local development to inspect the live model. The explorer is rendered from the same registry used to validate field metadata and relationships.

## Status vocabulary

| Status        | Meaning                                                |
| ------------- | ------------------------------------------------------ |
| `mocked`      | Used by the frontend with development fixtures.        |
| `confirmed`   | Agreed with the future API/backend team.               |
| `unconfirmed` | Required or assumed by the UI but not yet agreed.      |
| `derived`     | Calculated by the frontend from other contract fields. |

All server-facing records use `Portal API` as their frontend source. Explorer ownership describes the logical business domain and record scope, not a mandated database. For example, Staff Member is institution directory data; it is not fetched from the student's private profile even when a lecturer appears on the student's module screen.

## Canonical aggregates and read models

CAP-51 adds campus-owned `onlineFormDefinitions` and student-owned `onlineFormSubmissions`. Definitions version their ordered typed fields, category, reviewer department, eligibility, and optional evidence kind for upload fields. Context responses expose selected-student evidence references keyed to the form field. Submissions reference the exact Programme Enrolment and optional Module Registration or Student Visa / Pass; immutable submitted snapshots retain the definition, latest-session prefills, owned-evidence references, answers, embedded replacement-file metadata and selected published-result display information. Status events and ordered case messages are embedded owned records; the Data Model Explorer exposes their relationships and derived summaries.

Student Visa / Pass records explicitly reference their Programme Enrolment so Online Forms can reject a pass belonging to another enrolment. Status distinguishes `valid`, `expiring`, `expired`, and `cancelled`; only cancelled records carry `cancelledAt`, and cancellation retains the original expiry. An enrolment may retain multiple historical pass records but only one valid or expiring current record. A shared selector prefers that current record, otherwise the most recently ended record. The catalogue returns the campus timezone independently of its template collection. Case revisions advance monotonically across changes, allowing the query boundary to reject older responses without replacing newer messages or status.

Online Forms queries expose the catalogue, template context and owned draft/case details. Session mutations save drafts with revision checks, submit once, withdraw open cases, append messages and acknowledge visible reviewer sequences. A catalogue includes derived unread counts without loading thread bodies; navigation counts distinct cases, not messages. Messages do not edit original form evidence. Responses are detached and Zod validated, and stale generations cannot mutate another student's caches. The form adapter reads the existing Personal Profile session overlay for current contacts rather than stale canonical contact fixtures. See [Online Forms](online-forms.md).

CAP-50 adds campus/programme-version `CampusImmigrationConfiguration` records plus enrolment-owned `ImmigrationCase`, `ImmigrationDocumentCheck`, and `ImmigrationCaseEvent` records. A case is `new`, `renewal`, or `cancellation`; cancellation records identify a student, graduation, or university trigger. Exact nullable relationships join the case to its application and medical-result Online Form submissions, Student Visa / Pass, and Finance Invoice. A new case snapshots immutable medical-result metadata from its exact case-linked Online Form after eVAL and before passport handover. Graduation-triggered cases are fee-waived cancellations without a duplicate form or invoice. Revisions and session generations gate staff and Online Forms mutations. `GET /api/v1/immigration` returns one detached route read model with applicability, availability decisions, current pass, joined case detail and medical-form status, a complete derived milestone projection, three to five case-specific step definitions, and the recommended step. Completed cases remain canonical but are excluded from the current-case projection. The Module Registration read model derives a validated confirmation-access code plus an independent Student Pass progress state that keeps the notice visible after eVAL and until endorsement completes; no additional gate record is stored. See [Immigration](immigration.md).

CAP-55 adds canonical campus-owned University Policy records with a fixed category, title, summary and explicit display order. `GET /api/v1/university-policies` resolves selected student → exact selected Programme Enrolment → campus, returns detached ordered summaries, and never falls back across campuses. An enrolled student at an unconfigured campus receives an empty collection; an unknown student or missing selected enrolment is rejected. Policy bodies, versions, effective dates, acknowledgements and publishing metadata are absent from this catalogue contract. See [University Policies](university-policies.md).

The mock Portal API starts with one canonical record graph that can hold multiple students. It is grouped into six aggregates so related information is understandable without pretending that these are physical database tables:

- **Student Record:** Student Profile, Emergency Contact, Academic Qualification, English Proficiency, Student Document, Student Resume, Resume Work Experience, Resume Education, Resume Skill, Resume Recognition, Portfolio Project, Student Visa / Pass, and deliberately limited Person Summary records.
- **Institution:** Campus, Campus Immigration Configuration, University Policy, Faculty, Staff Member, Programme, Programme Version, Academic Term, Grading Scheme, and Grade Band.
- **Curriculum:** Module, Curriculum Module, and Module Prerequisite.
- **Delivery:** Module Offering, Teaching Assignment, Class Schedule, Class Session, Module Week, Module Learning Item, and Module Assessment.
- **Student Journey:** Programme Enrolment, Study Period, Module Registration, Credit Load Override, Attendance Record, Learning Progress, Assignment Submission, Assessment Result, Module Result, Term Result, Immigration Case, Immigration Document Check, and Immigration Case Event.
- **Finance:** Finance Account, campus Finance Profile, effective-dated Campus Service Tax Profile, enrolment-owned Education Service Tax Exemption, Fee Plan, Fee Item, Invoice, Invoice Line, immutable Invoice Service Tax snapshot, Payment, Payment Allocation, Funding Scheme, Student Funding Award, Student Funding Benefit, Student Funding Renewal Condition, Financial Adjustment, and session-only Finance Payment Proof metadata.

Dashboard, Academic Context, Academic Performance, Academic Modules, Academic Study Plan, Profile, Calendar, community, and messaging responses remain smaller route-facing read models. Every student scenario uses the same selected-Programme-Enrolment canonical projections; differences live in records instead of scenario-specific service branches or static route DTOs. Messaging adds a normalized session record set—Conversation, Conversation Participant, Message, and Attachment—joined to the selected scenario's canonical community identities and groups.

The session-only academic completion transition stages final Assessment Results, Module Results and one Term Result against the exact final current Study Period. It validates complete earlier periods, exact result ownership, curriculum passes, earned-credit and CGPA thresholds, and the whole canonical graph before returning a replacement graph. On success, final Module Registrations, the retained Study Period and Academic Term become completed and the owning Programme Enrolment becomes completed. “No current semester” is therefore derived from the absence of a `current` Study Period; the final period is not deleted, so completed-student Performance, Modules, Study Plan and Graduation reads retain the full history.

Module Lecturer read models retain `moduleOfferingId` and `semesterId` alongside the Module and Lecturer IDs. The Data Model Explorer exposes both relationships so historical and repeat teaching assignments cannot collapse into one module-level lecturer. Timetable and attendance projections follow the student's exact registered offerings; attendance joins the owning registration and class session together. Canonical records own the co-lecturers shown in current module cards and Community.

All date-only contracts share real Gregorian calendar validation, including leap years, month ends, and rejection of day zero. Issuance timestamps are converted to the enrolment campus timezone before resolving effective Service Tax profiles or exemptions. Mock fixture creation retains a fixed reference instant, while transaction validation and submission/issuance timestamps use the current clock. Tests can inject that live clock independently of fixture generation.

The Explorer opens on a database-style Records view. Each logical table shows the actual validated mock rows, columns, primary/foreign-key hints, row counts, and clickable foreign-key values. Student filters isolate the private and transactional rows reachable from Alya or Rizal while leaving shared institution, curriculum, staff, and delivery tables visible. Fields, Relationships, Student Timeline, and Row JSON remain available alongside Records. The table names and layout are an integration aid, not a mandated physical database design.

Navigation definitions and design tokens are frontend configuration, not database entities, and therefore do not appear as tables.

Academic Module Registration is also a route-facing read/mutation model derived from the canonical Programme Version, Study Period, Curriculum Module, Module Prerequisite, Module Offering, Teaching Assignment, Class Schedule, Module Registration, Module Result, Grade Band, Credit Load Override, and Student Account Issue records.

Registration candidate derivation also reads Grade Band and published Module Result records. Within the active Programme Enrolment, a Module with at least one failed result and no later or alternative passing result is unresolved. If that Module has a current-term campus offering, the route projects it once as a `required-repeat` candidate before the recommended semester candidates. A passing result suppresses the repeat even though the failed attempt remains in history. Recommended dependants of an unresolved prerequisite stay in the response with `prerequisite-not-met`; a repeat selected in the same draft does not count as a completed prerequisite.

## Campus Services

Sections optionally use composed headings and recursive `stack`/two-child `columns` groups. Typed leaves include headings, paragraphs, lists, notes, photo/dish/icon image groups and rectangular tables. Campus-owned layout fields control cover position, stretching, narrow-container ordering, dividers and table column proportions. Nested block IDs remain unique within each page. Standard blocks without layout options retain their default rendering. The optional contact image carries the same validated media metadata as page content. These fields pass through the existing detail projection; no endpoint, relationship or cache-key changes are needed. The explorer examples are derived from the same canonical records and include the nested content.

Campus service information is a separate institution aggregate described in [Campus Services](campus-services.md). `CampusServicePageRecord` owns structured sections, local image metadata, a campus-unique slug and navigation order. Image metadata may carry validated responsive variants with source paths and dimensions, shared by banners and body media. A catalogue page may deliberately use a null image and empty sections while approved campus content is pending; the portal renders the common banner fallback and Services empty-content state. One optional `CampusServiceContactRecord` belongs to each campus for API compatibility; Services no longer renders contacts and its fixture image is null. Cyberjaya publishes six source-backed categories plus the Accessibility Support placeholder, for seven entries in total; Accommodation and Services Finance are not catalogue records, while student Finance remains separate. The read-only catalogue and page endpoints follow the exact selected Programme Enrolment, return empty collections for unconfigured campuses, and never substitute another campus's data. `ServiceSummary` is a derived catalogue used by sidebar, tabs and breadcrumb. The explorer includes all seven catalogue rows, both canonical records, campus relationships and field-specific endpoint usage.

Campus Service Page banner appearance is campus-owned structured content: dark/light text and an optional contrast gradient. Defaults normalize to dark text with no gradient. It travels through the existing service-detail contract and explorer sample; catalogue rows and campus relationships remain unchanged.

## Profile Documents

`GET /api/v1/profile/documents` returns a validated `DocumentsProfileResponse` with one student-owned collection of metadata-only `ProfileDocument` rows. Passport and academic-evidence filenames are projected directly from their existing Student Profile, Academic Qualification, and English Proficiency records. Additional canonical Student Document records reference their owning Student Profile. Library owner relationships and unique IDs validate with the canonical graph. The Student Handbook moved to the CAP-55 catalogue, so University Document records and the route-facing university collection have been removed. The Explorer includes the Student Document table, student filtering, field-specific endpoint usage, and derived row examples. View/Download remain no-op placeholders; no storage URLs or file contents are exposed. Full behaviour and identity rules are in [Profile Documents](profile-documents.md).

## Profile Privacy & Security

`CampusPrivacySecurityRecord` contains one optional campus-owned Privacy & Security publication: introduction, nullable review date, ordered information categories and ordered account-security guidance with validated responsive local imagery. The canonical graph validates a real campus owner, unique record IDs, one record per campus and unique nested IDs/display orders. `GET /api/v1/profile/privacy-security` resolves selected student → exact selected Programme Enrolment → campus and returns a validated `PrivacySecurityResponse` with that optional record and a derived ordered portal-access summary. Missing content produces a valid empty response; missing enrolment is an error, and no campus fallback or mutation exists. The Data Model Explorer exposes the canonical record/campus relationship and the derived access rows. See [Privacy & Security](privacy-security.md).

## Reference student scenarios

The current graph provides four selectable scenarios: Alya for first registration, Mei for a new Student Pass, Rizal for active mid-programme study, and Nadia for completed-study and Graduation history. Materials projects canonical weeks, file metadata, assessments and learning transactions rather than maintaining a parallel runtime graph. Resource completion is independent of elapsed teaching weeks. Assignment submission records update in the session graph, including withdrawal timestamps; existing registry relationships and explorer examples describe the same records. Module Attendance follows the selected enrolment's campus/programme configuration. Scenario query keys include session generation and Programme Enrolment, and every write captures the student, generation, and write revision before its asynchronous work. A commit succeeds only when all three still match; stale results are rejected before cache success callbacks with student-facing recovery copy. These changes add no endpoint or persistent storage.

The mock graph contains four deliberately fictional selectable student journeys plus nine directory-quality peer students used to model cross-year community membership. None is copied from a real student, and none may be treated as production data. Alya, Mei, Rizal, and Nadia share the Cyberjaya programme model while owning separate personal, enrolment, study-period, account-issue, academic-transaction, Immigration, Graduation, and Finance records applicable to their lifecycle stage. Alya is a Malaysian citizen; Mei, Rizal, and Nadia exercise different international Student Pass states. The peer records carry only the enrolment, current study-period, and module-attempt history needed to make community access traceable; they are not selectable scenarios and do not invent unrelated finance, attendance, or personal workflows.

### Community hierarchy

- The programme roster is derived from active Programme Enrolments across every Programme Version belonging to the same Programme. It currently contains Alya, Rizal, nine peers distributed across Years 1-3, and five unique lecturers reached through curriculum offerings and Teaching Assignments.
- Module rosters are derived from the exact Module Offering. Only `registered` Module Registrations and that offering's Teaching Assignments grant current access. A repeat registration is included exactly like a normal registration while active.
- Thomas Hassan preserves a completed failed DMP201 attempt and an active `attemptNumber: 2`, `registrationType: "repeat"` registration in the current DMP201 offering. He is therefore in DMP201 chat but not unrelated module groups.
- The Community read model normalizes the current person plus safe `CommunityPerson`, `CommunityGroup`, and `CommunityMembership` rows. The Messages directory is derived from personal `ConversationSummary` records, so a newly enrolled student can access programme history without receiving an invented direct-message history.
- Programme and Module DTOs do not carry community counts. Members totals and `+N` values are derived from membership rows returned in the same response; programme/module card badges instead show unread message counts derived from Message sender IDs and the current participant's `lastReadAt`.
- `StudentService` is a validated Community response record with a stable ID, display label, semantic accent and icon key. The current response includes `service-it-helpdesk` for both student scenarios. Sending to it creates the same normalized session-only Conversation, Participant and Message records as other service chats; no separate ticket or support-case entity is introduced for CAP-33.

### Alya Putri - first-semester registration week

- Alya is the default portal scenario. Her Semester 1 starts six campus-calendar days after the browser machine date, which keeps the frontend in the programme's registration window when the mock session is created.
- She has a complete Malaysian-citizen private profile, emergency contact, active Programme Enrolment, six Study Period positions, and her own empty Finance Account. She has no fabricated Student Visa / Pass record; Study Plan returns the explicit not-applicable branch for its passport/visa requirement.
- Semester 1 curriculum Modules, Module Offerings, Teaching Assignments, Class Schedules, and assessment outlines exist as institution-owned preparation records. The first two recommended modules demonstrate multiple class sections. Before confirmation, Alya has no Module Registration, so her Modules, Timetable, Attendance, results, learning progress, submissions, and invoices are valid empty collections.
- Her only active Student Account Issue is Module Registration, due when the rolling registration window closes. The completed Academic Module Registration route initially recommends all five eligible four-credit Semester 1 modules and a viable class section for each. The student may reduce the load to 12 credits, but cannot confirm below 12 or above the effective maximum of 20.
- The first confirmation atomically creates Alya's exact current-Study-Period registrations, one aggregated tuition Invoice, one separate registration-fee Invoice, their canonical lines, and any eligible award-linked credits, then resolves her registration issue. Dashboard, Modules, Timetable, Attendance, Study Plan, Profile, Finance, and Community re-project from those records. The registered period is permanently read-only and the whole session transaction resets on refresh.

### Rizal Hakim - mid-programme reference journey

Rizal retains the internally reconciled historical journey used to validate repeat attempts, published results, attendance, and finance:

- Rizal is an Indonesian international student enrolled in the six-semester, 120-credit Bachelor of Design (Hons) Professional Design (Visual Communication), Version 2025. Permanent-address data belongs to the private Student Record aggregate; lecturers come from Institution Staff Member records rather than from the student's profile.
- The curriculum contains 20 unique credits in every programme semester. Semester 3 also contains a second ART201 registration after the failed Semester 2 attempt, so the transcript has 24 attempted credits that term without turning the curriculum into 124 credits.
- The current date represented by the fixtures is 26 August 2026. Semester 4 is ongoing and its five registrations were confirmed during the registration week before teaching began. Recurring schedules generate the term's dated Class Sessions; elapsed sessions are completed, future sessions are scheduled, holiday exceptions are cancelled, and attendance exists only for completed sessions. Semesters 5 and 6 are plans, not completed registrations or results.
- Assessment Result and Module Result records exist only for completed Semesters 1 through 3. Every one of the 16 historical module attempts has six assessment results connected to its offering's assessment outline. Semester 4 has none.

| Programme semester | Journey state                          | Curriculum credits | Registered attempts | Official GPA | Official CGPA |
| ------------------ | -------------------------------------- | -----------------: | ------------------: | -----------: | ------------: |
| 1                  | Completed                              |                 20 |          20 credits |         3.27 |          3.27 |
| 2                  | Completed; first ART201 attempt failed |                 20 |          20 credits |         2.67 |          2.97 |
| 3                  | Completed; ART201 repeat passed        |                 20 |          24 credits |         3.39 |          3.33 |
| 4                  | Current; results not published         |                 20 |          20 credits |            — |             — |
| 5                  | Planned                                |                 20 |     No registration |            — |             — |
| 6                  | Planned                                |                 20 |     No registration |            — |             — |

The Semester 3 Term Result retains 64 transcript-attempt credits, 60 CGPA-counted credits, 60 earned credits, and 199.96 cumulative points. The failed and repeated ART201 attempts both remain visible, while only the passed replacement is included in the current CGPA policy.

The finance history is scoped to the same Programme Enrolment through its Finance Account. Invoices exist only for Semesters 1 through 4; planned future semesters do not create charges. Invoice Lines explain pre-tax charges, immutable Invoice Service Tax records snapshot the issuance-time decision, Payment Allocations join payments to invoices, and Financial Adjustments preserve ordinary and award-linked credits separately. Rizal's active scholarship posts a RM1,300 Semester 4 tuition credit before tax; RM702 SST on the remaining RM11,700 taxable tuition base leaves the reconciled Semester 4 tuition balance and account outstanding at RM1,852.

## Implementation locations

- `src/contracts/portal-records.ts` defines the canonical Zod contracts.
- `src/mocks/portal-record-fixtures.ts` builds the validated multi-student reference graph; rolling scenario dates are computed when a `MockPortalApi` session is created.
- `src/mocks/portal-scenarios.ts` contains scenario identity metadata, while `src/mocks/student-scenario-projections.ts` derives Alya's route read models from the canonical graph.
- `src/mocks/chat-fixtures.ts` joins normalized scenario-aware conversation records to canonical community people and groups. These records remain separate from the persistent canonical graph because chat mutations are explicitly session-only in this frontend phase.
- `src/services/mock-academic-module-registration-api.ts` derives eligible candidates and exact class sections, validates credit limits, prerequisites, duplicate modules, timetable clashes, Finance ownership, fee-plan currency, explicit Fee Items, and target-invoice absence, then stages the registrations, invoices, lines, award credits, and issue resolution as one validated graph transaction. The current logical Module Offering and route DTO intentionally have no seat-capacity, remaining-seat, or fee fields.
- `src/data-model/canonical-registry.ts` maps those records into Explorer metadata and relationships.
- `src/services/portal-runtime.tsx` selects one mock adapter for the current development session and scopes TanStack Query cache keys by scenario.
- `PortalApi.getPortalRecordGraph()`, `PortalApi.getFinanceStatement()`, `PortalApi.getAcademicStudyPlan()`, and `PortalApi.getImmigration()` expose cloned or student-scoped derived mock responses behind replaceable service boundaries. Finance proof submission and withdrawal return the refreshed Finance statement without mutating the canonical payment ledger and invalidate Immigration where invoice progress depends on it.
- Existing route responses remain purpose-built read models derived from the graph where their data overlaps. The frontend should not display the full graph when a smaller response is sufficient.
- Community projection logic joins Programme Enrolment, Programme Version, Curriculum Module, Module Offering, Module Registration, Teaching Assignment, Student Profile, and Staff Member records without importing fixtures into components.

## Canonical graph integrity

- Every canonical collection enforces collection-local unique record IDs before lookup maps or projections are built. A duplicate can therefore never silently replace another record during validation.
- The academic ownership chain is validated from Campus, Faculty, Programme, Programme Version, Academic Term, Module, and Curriculum Module through Programme Enrolment, Study Period, Module Offering, and Module Registration. An enrolment's campus must own its Programme Version through the Programme Faculty; periods and offerings must use Academic Terms at that same campus; registrations must join a curriculum module offering in the period's exact term and campus.
- A student may have at most one active Programme Enrolment. Historical completed, deferred, and withdrawn enrolments remain valid. Each enrolment may have at most one current Study Period, and Study Periods are unique by enrolment/term and enrolment/programme-semester.
- Offering sections are unique within module, term, and campus. A Study Period can register one Module Offering once, while the same offering can legitimately be shared by registrations owned by different students.
- Attendance joins one registration to a Class Session in the exact same offering and is unique by registration/session. Assessment Results must use an assessment from the exact registered offering; Module Results use a Grading Scheme from the enrolment campus; Assessment, Module, and Term Results each enforce their natural one-result ownership keys.
- All Programme Versions used by the permanent scenarios retain their own Curriculum Module and Module Prerequisite records. The 2024 cohort fixture therefore has a version-owned curriculum instead of depending implicitly on the 2025 rows.

## Change workflow

1. Identify the data required by a screen.
2. Add or change the Zod contract.
3. Update field metadata and relationships in the registry.
4. Add a valid mock fixture and sample payload.
5. Expose the data through `PortalApi` and a query hook.
6. Update this document when conventions or ownership assumptions change.

Compilation and tests must fail when schema fields are missing metadata, relationships point to unknown fields, or fixtures no longer satisfy contracts.

## API conventions

- IDs are opaque non-empty strings.
- Times are ISO 8601 strings with timezone information.
- Missing optional values are represented explicitly as `null` where the UI distinguishes unknown from empty.
- Collections return empty arrays rather than `null`.
- Student-specific API methods use the active scenario's stable `studentProfileId` plus an explicit owned `programmeEnrolmentId`, then traverse that enrolment's Study Period, Module Registration, and Finance Account ownership. They do not depend on a student's position in a fixture array. When a read omits the ID, the shared selector chooses the single active enrolment, then the newest deferred, completed, or withdrawn enrolment in that order; multiple active enrolments are invalid.
- Cross-domain mutations use one dependency registry. Module Registration confirmation refreshes Dashboard, Academic summaries/details, both Materials list and exact-item queries, Study Plan, Calendar, Community/chat, Profile, Finance, Online Forms, and Graduation within the current session scope. Tests execute the invalidation helper against real query-cache keys so a registry entry cannot exist without reaching its consumer shape.
- Attachment sizes use integer bytes; formatted labels are presentation concerns.
- API errors use a stable code, human-readable message, and optional field details.
- Community returns `currentPersonId` instead of a screen-owned current-user guess. Conversation contexts reference an exact Community Group for programme/module chats, a Community Person for personal chats, or a Student Service for service chats.
- `GET /api/v1/conversations` is a small summary read model containing context, latest activity, and derived unread count. `GET /api/v1/conversations/:id` returns only the active conversation's normalized participants, messages, and attachments. Participant records store `lastReadAt`; they do not persist a second unread counter that can drift from Message records.
- Message send/delete and read-marker changes mutate only the current `MockPortalApi` session. The API rejects self-directed personal messages and group messages from a person who is not in the exact Community Group. A first outgoing personal message creates the Conversation and participant rows; merely opening an empty direct-message screen does not.
- The timetable read model supplies a recurring weekday, campus-local start/end time, and inclusive effective date range. The frontend highlights a current class when the browser machine date, weekday, and campus-local time fall inside that schedule. This is presentation state, not a Portal API field, and it recalculates every minute.
- The canonical Class Schedule owns the recurring pattern. Calendar expands it for the requested campus month and overlays available dated Class Sessions: same-date sessions supply actual times, cancellations remove an occurrence, and off-pattern sessions add make-up classes. Planned offerings therefore appear immediately after registration even before session rows are materialized. Attendance continues to use dated Class Sessions only.
- Timetable class cards use their `moduleId` and `semesterId` to open the matching read-only Module Details offering.
- The timetable renders only weekdays that contain one or more valid scheduled entries; empty weekdays are not shown.
- `CalendarEvent` is a unified monthly read model for classes, assignments, exams, meetups, holidays, and registration periods. It belongs to one Semester and has a nullable Module relationship. Assignment and exam events additionally require the matching `moduleLearningItemId`; other event kinds keep it null. Assignment `startsAt` values are deadlines; Registration uses `startsAt` and `endsAt` for its opening and closing timestamps. All-day event `endsAt` values are exclusive.
- Calendar assignment and exam events use `moduleId`, `semesterId`, and `moduleLearningItemId` to open the exact learning-item detail. Class events open Module Details. Registration events are derived from the selected student's exact Study Period `registrationOpensAt` and `registrationClosesAt`; registrations, meetups, and holidays remain non-navigable because they do not identify a module offering.
- Calendar queries use a validated `semesterId` and `YYYY-MM` month plus the exact selected Programme Enrolment. The response returns the campus time zone, events occurring anywhere within that month, and the referenced modules needed by the agenda. A Registration interval is returned for every month it overlaps and expanded across each covered day in the UI. Class events use the schedule/session overlay above. Released assignment and exam events use the exact Learning Item and Assessment IDs shared with Materials and module issues; future items remain withheld.
- Modules declare an `online` or `physical` delivery mode. Dashboard and Academic Modules cards join ordered lecturer assignments and active module issues through stable IDs. Lecturer avatars are nullable because some source records may not provide one.
- `ModuleOffering` belongs to one Module and one Semester. It provides the summary, credits, optional weekly contact hours, optional classroom type, and optional fee/currency pair used by module details.
- `ModuleAssessment` belongs to one ModuleOffering and supplies its label, category, percentage, and display order. A module-detail response requires at least one assessment and validates that all weights total exactly 100%.
- `StudentAccountIssue` separately identifies active or resolved student-level fees, Student Pass, and module-registration issues; it is not a module-card `ModuleIssue`. Active issues aggregate across every enrolment owned by the selected student so the header count does not disappear while history is selected. Each action carries its owning `programmeEnrolmentId`; the destination switches context before opening the exact Finance, Registration, or Immigration state. Fee issues may supply `amountMinor` and `currencyCode`; date-based issues may supply `dueAt`. Student Pass actions include the active case's recommended Applications step and live progress so Profile never tells a student to start a case that already exists. The Profile projection reconciles `studentPassRequired`, `expiringVisa`, and `studentPassCancelled` with the shared current-pass selector. Natural expiry remains a destructive expired presentation, cancellation uses its own destructive issue, and a replacement pass resolves the cancelled issue. Module Registration confirmation resolves its matching mock-session issue, and an allocation-backed open invoice balance derives one aggregate Outstanding Fees issue.
- Each `ModuleIssue` references both its source `ModuleAssessment` and matching `ModuleLearningItem` through stable IDs; the server should derive it only for an assessment that has been released and remains relevant to the student. Assignment and exam actions use that learning-item ID to open the exact available detail without title matching or frontend ID reconstruction. Module issue countdowns are derived from the assessment's `dueAt` (or exam start) and the owning campus calendar timezone. Resolved issues are retained by the contract but omitted from the cards. The mock schedule is the shared source for Materials, module-card issues, and Calendar assessment events, preventing conflicting dates.
- Programme Version defines six semesters, 120 unique curriculum credits, a 12-credit minimum semester load, and a 20-credit standard maximum. Each prescribed semester contributes 20 curriculum credits. A Credit Load Override belongs to one Study Period and records the approved maximum, reason, approving Staff Member, approval time, and active/revoked status. Rizal's Semester 3 has an explicit active 24-credit override for the additional four-credit ART201 repeat without increasing the curriculum total.
- Programme `totalSemesterCount` defines the chart timeline, so all six programme positions remain visible. The Performance selector contains published terms only. As of 26 August 2026, Semester 4 is ongoing and has no Term Result, Module Result, or Assessment Result; the latest official dashboard and Performance values are Semester 3.
- `TermResult` distinguishes term credits attempted, cumulative transcript attempts, cumulative GPA-counted credits, earned credits, and cumulative points. Semester 3 therefore records 24 term credits and 64 transcript-attempt credits while using 60 credits in CGPA after the passed ART201 repeat replaces the failed attempt for CGPA policy. Both attempts remain visible as Module Results.
- Academic Performance uses a dedicated `GET /api/v1/academic/performance` read boundary. Its response combines the Programme, published StudentSemesterPerformance records, referenced Modules, StudentModuleResult records, semester-specific ModuleOfferings, their ModuleAssessment outlines, and StudentAssessmentResult records required by the page.
- Academic Context uses `GET /api/v1/academic/context` to join the current student profile to every owned Programme Enrolment and each enrolment's Programme Version, Programme, Faculty, campus, and nullable current Study Period. It returns the exact selected ID plus ordered context items with `active`, `completed`, `deferred`, or `withdrawn` status. The selected item supplies the programme identity and nullable current-semester summary required by the shared shell. `programmeEnrolmentId` is stable in route and query keys; switching it clears nested semester/detail selection. It does not expose private Personal Profile fields.
- `StudentModuleResult` links a student, semester, and module to a published grade label, explicit pass/fail outcome, official points, credits attempted, credits earned, and final points. The frontend uses the supplied outcome for status colour and never infers pass/fail or a point mapping from the grade string. Response validation reconciles credits earned with the outcome, final points with points times credits attempted, semester module totals with published GPA, and performance totals with published CGPA.
- `StudentAssessmentResult` belongs to one StudentModuleResult and references one ModuleAssessment. It supplies the student's unweighted `scorePercent` and official `weightedScore` contribution. Each published result has one score for every assessment in its 100% offering outline; a weighted score cannot exceed its assessment weight.
- Grade rows remain non-navigable. They expand inline to show the assessment outline, scores, weights, supplied contributions, and summed display total without deriving the official grade or pass/fail outcome. Academic GPA/CGPA popups reuse the supplied calculation inputs to explain published results; they do not define academic policy.
- Academic Performance returns the active semester plus every dated Semester so its page selector and charts share one six-semester timeline. It defaults to the latest published result, Semester 3. Current Semester 4 and future Semesters 5–6 are selectable but return contextual unpublished-result presentation states; only the GPA breakdown popup is restricted to published semesters.
- Academic Modules uses a dedicated `GET /api/v1/academic/modules` read boundary. Its response combines the Programme, `activeSemesterId`, dated Semester records, Enrolments, Modules, Lecturers, ModuleLecturers, and ModuleIssues required by the page.
- Academic Study Plan uses a dedicated `GET /api/v1/academic/study-plan` read boundary derived from the canonical graph. It joins the exact selected Programme Enrolment to its Programme Version, Faculty, Study Periods, Curriculum Modules, named Module Prerequisites, exact Module Registrations and attempts, published Module and Term Results, Finance Account and invoices, and Student Visa / Pass. The Data Model Explorer lists this access path on every contributing canonical field. Programme Version owns the 2.0 minimum graduation CGPA alongside the 120-credit requirement.
- The Study Plan response contains one six-semester curriculum position per recommended module plus an additional row for a repeat registration in its actual attempt semester. Completed semesters require a published Term Result; current, planned, or deferred semesters must not expose term or module results. Its `graduationEligibility` read model reconciles 60 / 120 earned Credit Hours and latest published 3.33 CGPA, requires a zero finance balance, and compares the Student Visa / Pass expiry with the final planned semester end date. Programme Status is `Eligible for Graduation` only when all four supplied checks pass; later clearance is not represented on Study Plan.
- Module Details uses a logical `GET /api/v1/academic/modules/:moduleId?semesterId=:semesterId` boundary. It returns only an offering owned through the exact selected Programme Enrolment and its non-withdrawn Module Registration, together with its Programme, Semester, Module, lecturers, timetable entries, and assessment outline.
- Module Materials uses a separate logical `GET /api/v1/academic/modules/:moduleId/materials?semesterId=:semesterId` boundary. The response returns the selected offering ID, campus time zone, ordered ModuleWeek records, release-aware ModuleLearningItemView records, the offering's existing ModuleAssessment outline, and progress/submission rows only for released items. A locked view contains identity, offering/week, title, type, order, and `availableFrom` only. Keeping this query separate avoids loading week content when the student views Summary.
- `ModuleWeek` belongs to one ModuleOffering and has inclusive `startsAt` plus exclusive `endsAt` boundaries plus an API-supplied teaching-topic title. The frontend compares those timestamps with machine time to label completed, current, and upcoming weeks; that derived label is not stored in the contract.
- `ModuleLearningItem` belongs to one ModuleOffering and the ModuleWeek containing its `availableFrom` timestamp. It remains the authoritative full record. The list projection adds `availability`: available views retain the full type-specific contract, while locked views redact summaries, file metadata, assessment identity, instructions, rules, deadlines, and locations. The exact logical `GET /api/v1/academic/modules/:moduleId/materials/:itemId?semesterId=:semesterId` boundary repeats ownership and release checks and returns either the same safe locked view or one full available item with its owned submission. `availableFrom` is authoritative for both boundaries. Assignment `dueAt` remains the hard exclusive mutation cutoff.
- `StudentLearningProgress` connects the current Student to one ModuleLearningItem with a server-shaped not-started, in-progress, completed, submitted, or graded display state. For the mock assignment flow, the returned submitted state is derived from StudentAssignmentSubmission rather than separately mutated.
- The canonical Assignment Submission belongs to the exact Module Registration and assignment Module Learning Item. The route-facing session response keeps only the fields required by the current frontend demonstration. Neither contains file bytes, a production URL, grading data, or persistent browser storage.
- Logical `POST /api/v1/academic/modules/:moduleId/materials/:itemId/submission` and `DELETE /api/v1/academic/modules/:moduleId/materials/:itemId/submission` boundaries demonstrate submit/replace/delete. Both validate the selected module and semester, assignment kind, release window, hard due cutoff, and file policy. A future backend must repeat these checks and own secure upload/storage; browser validation is not a security boundary.
- Materials response validation requires unique week numbers/orders, unique item IDs and week orders, matching offering/week references, available-item assessment/timing validation, each item release time inside its related week, progress only for unique available items, and at most one valid pre-due submission per available assignment. Empty item, progress, and submission collections remain valid; a materials response always has at least one teaching week and a complete 100% assessment outline.
- The Community response supplies `activeSemesterId`. Module chat contexts carry `communityGroupId`, `moduleOfferingId`, `moduleId`, and `semesterId` as stable identity alongside the display label, so access and detail links never depend on title matching.
- `Semester.semesterNumber` is the programme-relative selector position. The student-facing Semester read model also carries the selected Study Period's `registrationOpensAt` and `registrationClosesAt`; these are student-journey dates rather than fields owned by the institution's Academic Term. The canonical `StudyPeriod` owns a registration window from 08:00 seven calendar days before its Academic Term begins until 23:59:59 on the preceding day. A canonical `ModuleRegistration` belongs to one Study Period and one curriculum Module Offering in the same Academic Term and campus, preserves its timestamp, attempt number, normal/repeat type, and status, and is unique by period/offering. Route-facing Enrolments derive only from real registrations. Semesters 1–3 are completed, Semester 4 has five confirmed `registered` attempts, and Semesters 5–6 have no Module Registration yet. Their recommended modules remain Curriculum Modules used by Study Plan rather than fabricated student transactions.
- Attendance belongs to an exact Module Registration and dated Class Session for the same Module Offering, with one row per registration/session. Route-facing Attendance Sessions retain that registration as `enrolmentId`, so module attendance filters the exact normal or repeat attempt rather than matching display text. The current student-facing contract deliberately supports only `present` and `absent`. Complete mock attendance exists for Semesters 1–3 and only for completed Semester 4 sessions through 26 August 2026. Future scheduled sessions have no Attendance Record and appear as derived `Not taken` presentation state.
- Programme Version owns the minimum attendance threshold, currently 80%. `GET /api/v1/academic/timetable?semesterId=...` supplies the separate Timetable and Attendance frontend tabs with one shared semester read model. A valid future Semester 5 or 6 remains selected and returns empty collections. `GET /api/v1/academic/modules/:moduleId/attendance?semesterId=...` supplies the selected Module, Semester, exact Enrolment, campus timezone, threshold, and only that attempt's sessions. Both responses derive from one canonical session projection; without an actual registration there is no future Module Details attendance route.
- Prerequisites live on Curriculum Modules, not on a student's result. The mock chain includes VIS101 → GRD201 → GRD301, DMP101 → DMP201, TYP201 → TYP301, GRD301 plus RES301 → CAP401, and CAP401 → CAP402.
- Profile > Personal uses a dedicated `GET /api/v1/profile/personal` read boundary and a logical `PATCH /api/v1/profile/personal` update boundary. The response joins Student, Programme, StudentPersonalProfile, EmergencyContact, and StudentAccountIssue records required by the screen.
- Profile > Resume uses logical `GET /api/v1/profile/resume` and `PATCH /api/v1/profile/resume` boundaries. One Student Resume belongs directly to the selected stable Student Profile; Work Experience, Education, Skill, and Recognition rows belong to that Resume. The complete update payload uses nullable IDs for new rows, and the mock adapter assigns opaque IDs while rejecting IDs owned by another Resume.
- Resume dates use `YYYY-MM-DD`. A null end date means `Present`, and completed education or work cannot end before it starts. Resume Education is a manually maintained presentation record and is deliberately independent from the admissions-owned Academic Qualification records shown in Profile > Academic.
- The Resume mutation replaces the selected student's complete Resume read model only in the current mock adapter session. The scenario-scoped query cache reflects successful saves across navigation; refresh or student-scenario replacement restores canonical fixtures. Rizal has populated Phase 1 examples, while Alya has a null summary and empty child collections.
- Profile > Portfolio uses logical `GET /api/v1/profile/portfolio`, `POST /api/v1/profile/portfolio/projects`, `PATCH /api/v1/profile/portfolio/projects/:projectId`, and `DELETE /api/v1/profile/portfolio/projects/:projectId` boundaries. Each Portfolio Project belongs directly to one stable Student Profile, and every read or mutation starts from that selected student ID. Foreign project IDs are rejected.
- Portfolio Project records retain an opaque ID, project name, `YYYY-MM-DD` creation date, description, ordered optional tool labels, and one artwork filename, allowed preview MIME type, positive byte size, and preview URL. New projects require one non-empty JPEG, PNG, WebP, GIF, or PDF up to 25 MiB; edits keep existing artwork unless a replacement is selected. The mock adapter stores local file metadata and a temporary object URL only, revoking replaced or deleted session URLs without uploading bytes.
- Portfolio mutations update only the scenario-scoped Portfolio query cache and reset with a new mock API session. Rizal has three canonical projects; Alya has an empty collection. Projects sort by creation date newest first and then by name for equal dates.
- `StudentPersonalProfile` owns the student's personal details and nested permanent/current address value objects. Contact number, marital status, email, and both addresses are editable; legal identity and passport fields are read-only presentation values.
- `EmergencyContact` belongs to one StudentPersonalProfile. Update requests must contain at least one contact; new rows submit a null ID and the service assigns a stable opaque ID. The current mock adapter stores profile mutations in memory only.
- Profile > Academic uses the read-only `GET /api/v1/profile/academic` boundary. It filters canonical Academic Qualification and English Proficiency records by the active stable `studentProfileId`, orders them newest first, and returns empty arrays when either collection has no retained record.
- `AcademicQualification` belongs to one Student Profile and retains its international qualification labels, institution and country, optional field/start date, completion date, result, transcript, certificate, optional certified translation, and optional grading scale. `EnglishProficiency` belongs to the same Student Profile and retains a formal test name, examination date, text score, optional expiry date, and certificate. These records are admission evidence, not an eligibility or score-threshold engine.
- Qualification document filenames are read-only frontend placeholders. No file URL, storage location, upload state, or download mutation is represented by the current contract.

## Student Finance

`FeePlan` remains owned by one Programme Version and currency. Each `FeeItem` has a category, label, explicit minor-unit amount, nullable Module relationship, and explicit education-service-tax treatment. Tuition items must reference a Module; registration, books, resource, international-services, repeat-administration, and other items must not. Tuition, registration, resource, repeat-administration, and the general international-services fee are taxable education services in the Malaysian fixture. Books are explicitly exempt; future student-pass or visa items must carry their explicit treatment rather than relying on their label. The current module amounts retain the former RM650-per-credit outcomes as explicit fixtures and are never calculated from credits at runtime.

The canonical ledger keeps module-level tuition `InvoiceLine` records for reconciliation. `GET /api/v1/finance` derives a student-facing `FinanceStatementResponse` that aggregates all tuition lines on the same semester invoice into one `Semester N Tuition Fee` line. Module codes, names, quantities, and individual prices do not cross the Finance screen boundary. The Academic Module Offering response likewise contains no fee or currency fields.

`POST /api/v1/academic/module-registration/fee-preview` accepts one student-owned Study Period plus selected Module Offering IDs and returns only aggregated tuition, the separate registration fee, eligible award credit, SST, final amount due, and currency. `POST /api/v1/academic/module-registration/confirm` recalculates that quote and commits registrations, both invoices and their lines, eligible award credits, immutable tax snapshots, and issue resolution only after the entire staged graph validates. Every Invoice uses one globally unique `INV-######` number; `INV-000000` is reserved. The mock adapter atomically reserves consecutive numbers after the current highest suffix, while stable opaque Invoice IDs continue to own relationships and routes. Preview-only invoice placeholders never enter the canonical graph or Finance response. A second confirmation, pre-existing target invoice, invalid number, collision, or exhausted sequence is rejected without partial mutation; a production backend must own concurrency-safe permanent numbering.

A Finance Account belongs to one Programme Enrolment and uses one currency. A campus-owned Finance Profile supplies same-currency counter hours, fictional bank details, reference instructions, and expected posting days. An Invoice belongs to the account, gains a category and display title, and has a nullable Study Period so non-semester charges need not fabricate academic ownership. Canonical invoice lifecycle is `issued` or `void`; the response derives `unpaid`, `partially-paid`, `paid`, or `void` from confirmed allocations and invoice adjustments. Canonical `dueAt` remains validated for future use but is frozen out of the student Finance response, status, sorting, and UI.

Student citizenship is stored as an ISO country code alongside the retained nationality display text. `Student Fee Category` is derived relative to the enrolment campus and shown as Local or International, but Malaysian SST eligibility reads Malaysian citizenship directly rather than trusting that display category. An effective-dated Campus Service Tax Profile owns the Malaysian jurisdiction, registered supplier identity/address, SST registration number, rate, currency, and activation interval; campuses and invoice dates without an applicable profile do not charge SST. Non-overlapping profile periods are enforced per campus.

Each Invoice owns exactly one immutable Service Tax decision with a `charged`, `exempt`, or `not-applicable` outcome, taxable base, basis-point rate, tax amount, reason, optional campus tax profile, and determination timestamp. Invoice issuance resolves the effective campus profile, removes explicitly exempt Fee Items, subtracts eligible award-linked credits from their taxable invoice value, applies citizenship or valid full-sponsorship/diplomat evidence, and rounds the remaining 6% tax half-up to the nearest sen. `Invoice.totalMinor` is tax-inclusive and must equal its line subtotal plus the tax snapshot. Later payments, proof submissions, or award-status changes do not recalculate the snapshot. Tax correction documents, filing, and remittance remain outside the frontend prototype.

Education Service Tax Exemption evidence belongs to the Programme Enrolment and records a validity period, fee scope, sponsor/evidence details, and optional Funding Award relationship for fully sponsored fees or diplomat dependants. Malaysian citizenship is a derived exemption and does not require a separate evidence record. Partial institutional award credits reduce the taxable base; a fully covered eligible fee produces no SST and credits remain capped so they cannot create an account credit. These sponsor, diplomat-dependant, and award-netting branches implement the approved prototype policy, not settled production tax treatment: the RMCD Education FAQ expressly treats company-sponsored tuition and institution-funded full scholarships as taxable, so campus Finance or a Malaysian tax adviser must confirm or replace these branches before backend implementation.

A Funding Scheme defines a `scholarship` or `incentive`. A Student Funding Award assigns that scheme to exactly one Programme Enrolment with staff-controlled status, award date, and an ordered start/end Study Period range. Student Funding Benefits belong to that assignment and form a validated union: percentage coverage for one fee category, a fixed same-currency credit for one fee category, or a same-currency informational allowance with frequency. Student Funding Renewal Conditions support only minimum CGPA and minimum semester GPA in this phase. Renewable assignments require at least one condition.

Active fee benefits create invoice-linked credit Financial Adjustments in award-date then stable-ID order. The relationship from an adjustment to its exact Student Funding Benefit makes the credit auditable. Benefit value and invoice collectible balance cap each credit, so stacking cannot create account credit. Allowances cannot be linked to an adjustment and never affect balances. Completed assignments preserve their historical credit links. The student response projects benefits and applied-credit totals while exposing no award mutation.

Renewal evaluation finds the latest published Term Result owned by the same Programme Enrolment. Each condition is `met`, `not-met`, or `pending`; the combined award evaluation is not met when any condition fails, pending when evidence is unavailable, and met only when every condition passes. This read model does not alter the authoritative assignment status, suspend an award, or revoke a credit.

Invoice balances subtract confirmed Payment Allocations and invoice-specific credits, and add invoice-specific debits from tax-inclusive invoice totals. The Finance response exposes their combined open-invoice balance separately from the signed account ledger balance and the unallocated portion of each confirmed Payment. Account balance includes all non-void invoices, all confirmed Payments, and all account/invoice Financial Adjustments. Reversed Payments and pending proof metadata remain visible but do not reduce a balance. Student attention, Study Plan, and Graduation use the open-invoice balance; confirmed money remains unapplied credit until Finance links it to an Invoice. The graph contract verifies fee/tax treatment, invoice subtotal-plus-SST totals, tax profile/exemption ownership, invoice line ownership, allocation limits, adjustment ownership, currency agreement, and student/campus relationships.

A `FinancePaymentProof` is a derived session record attached to one student-owned unpaid Invoice. It stores transfer amount/date/reference plus one validated PDF, JPEG, or PNG filename, MIME type, positive byte size up to 10 MiB, submitted timestamp, and `pending` status. `PUT /api/v1/finance/invoices/:invoiceId/payment-proof` creates or replaces the one allowed proof; `DELETE` withdraws it. Neither operation creates a Payment or Payment Allocation, changes a balance, uploads bytes, or survives refresh. Development student-view switching retains proofs for the all-student Admin Actions workspace.

Alya's registration-week Finance Account starts active with no invoice, payment, allocation, adjustment, proof, award assignment, or outstanding balance. Her first session-only registration confirmation creates an aggregated tuition invoice from the selected modules plus a separate registration-fee invoice. As a Malaysian citizen she is exempt from the campus SST profile, so the standard three-module flow creates RM7,800 tuition and RM400 registration with RM0 SST, for RM8,200 outstanding; refreshing restores the original zero-invoice state. Rizal's historical account is reconciled as follows:

- Semester 1: RM13,000 tuition plus separate RM400 registration, RM700 resource, and RM400 international-services invoices; a RM300 tuition credit and RM14,200 confirmed payment clear the period.
- Semester 2: RM14,500 pre-tax charges plus RM870 SST, fully paid through two confirmed payments totalling RM15,370.
- Semester 3: RM17,400 pre-tax charges plus RM1,044 SST, fully paid by an RM18,444 confirmed payment.
- Semester 4: RM14,750 pre-tax charges. Registration, resource, and international-services invoices carry RM90 SST and are fully allocated; books are exempt. The active Creative Excellence Scholarship posts a 10% tuition credit of RM1,300 before RM702 SST is calculated on the remaining RM11,700 tuition base. RM12,390 confirmed payments leave only the tuition invoice outstanding at RM1,852.

The completed Early Confirmation Incentive owns the existing historical RM300 Semester 1 tuition credit. The Creative Excellence Scholarship covers Semesters 4–6, evaluates its minimum 3.00 CGPA condition as Met from the latest published 3.33 CGPA, and includes a RM480-per-semester study allowance as information only.

- One reversed payment remains visible but has no balance effect. Semesters 5–6 are planned and have no invoices.

The API supplies the signed account balance, open-invoice balance, unapplied confirmed credit, and derived outstanding, settled, or credit state. Dashboard and Profile derive exactly one outstanding-fees issue from the open-invoice balance while it is positive; the fee issue is not stored in the canonical Student Account Issue collection, does not duplicate for multiple invoices, and disappears only after every invoice is settled or void. Detailed UI behaviour and proof rules are recorded in [Student Finance](finance.md).

## Mock persistence

The mock API is session-only. Student scenario selection, chat, Personal-profile edits, Resume and Portfolio edits, Finance proof metadata, module-registration confirmation and its generated Finance records, Immigration cases/invoices/progress/medical-result metadata, and assignment-submission metadata remain in memory while the application is open and reset after refresh or browser restart. Switching the selected student retains the shared session graph while clearing student-scoped query caches so one student's cached response cannot appear for another. File contents for assignment, Finance proof, Online Forms and Immigration evidence are never retained or uploaded. This is deliberate: identity-aware persistence and secure file storage are backend responsibilities and are not required to prove the frontend contract.

## Lecturer evaluations (CAP-30)

`ReviewQuestionnaire` is a campus-owned, versioned canonical record with ordered sections and stable question IDs; at most one version is enabled per campus. `LecturerReview` belongs to Student Profile, Module Registration and Teaching Assignment, and contains a complete questionnaire snapshot, all integer 1–5 ratings, optional feedback and ISO submission timestamp. The graph rejects duplicate registration/assignment pairs, duplicate IDs, missing students, mismatched offerings/campuses and incomplete answers. Snapshots remain valid after the current questionnaire is retired; no destructive foreign-key dependency on the current version is required.

The replaceable Portal API exposes `getLecturerReview` and `submitLecturerReview` using module, semester and lecturer route identities. The selected student is taken from the session, never supplied as mutation authorization. Module Details includes `lecturerReviews` availability summaries (available/submitted/unavailable plus an optional explanation). Canonical and legacy lecturer aliases resolve to the same assignment before uniqueness checks. The API validates ownership and current-period eligibility again during submission and rejects changes/duplicates. Review timestamps use the owning campus timezone for display. All persistence is in-memory; server-enforced access, confidentiality/reporting policy and durable storage are backend responsibilities.

## Student Announcements (CAP-16)

Canonical `PortalRecordGraph.announcements` owns published campus articles and validates unique identities and campus references. `announcementRecordSchema` includes metadata, a required campus-owned `isPinned` editorial flag, nullable cover and ordered structured rich blocks; `contentImageSchema` is the shared Services/Announcements media contract. The `article` explorer read-model entry describes `announcementSummarySchema` (campus, department, pin state, optional cover and campus timezone). Full bodies are returned only by detail queries.

`getAnnouncements()` and `getAnnouncement({ announcementId })` project selected student → exact selected Programme Enrolment → campus. Dashboard uses the complete summary projection; the Announcement overview filters pinned summaries from the same newest-first collection without another endpoint or record set. No fallback is made for missing enrolments or unavailable campus content. Queries are generation- and Programme-Enrolment-scoped, read-only and return independent parsed responses. Both explorers include current examples. See [Announcements](announcements.md) for fields, rendering, fixture assumptions and backend responsibilities.

## CAP-15 graduation and completed-enrolment reads

The canonical graph includes Nadia Pratama: an owned 2023 curriculum, six completed study periods, 29 passing module results, 120 credits and 3.00 published CGPA, no current semester and a settled Finance account. Existing Alya/Rizal records remain intact. Read projections select one explicit owned Programme Enrolment and keep current-semester/context fields nullable. Restricted mutations still require active/current ownership; inactive enrolments retain readable history and status-specific Academic Support guidance.

`CampusGraduationConfiguration` joins campus, programme version and FeeItem with unique campus/version configuration. `GraduationClearance` links the student, enrolment, campus, clearance submission, three explicit department checks for Faculty, Finance and Library, approval timestamp, invoice, optional convocation submission, three document records and the nullable CAP-50 cancellation case used for international Student Pass progress. Finance clearance is a manual department decision even when the separate eligibility projection reports a settled account. Every attempt retains its own ID; only one active/completed clearance form and one approved process/invoice can exist per enrolment. Department/document/approval operations use revision checks and staged canonical graph validation. The shared Data Model Explorer exposes these records, relationships and `AcademicGraduationResponse`; its submitted example is separate from live scenario sessions.

CAP-15 contributes Graduation Clearance and Convocation Registration to the eight-template Online Forms catalogue and adds shared select options/conditional required fields. CAP-50 additionally owns a case-linked Medical Screening Result form outside that catalogue. The Finance proof projection adds retained pending/verified/rejected status, review timestamp/reason and nullable confirmed Payment ID. A verified proof posts a confirmed canonical Payment/Allocation; counter payments use the same ledger. All changes remain scoped to the in-memory student session. Developer commands are outside the production Portal API and excluded from production bundles. See [Academic Graduation](academic-graduation.md) for entry gates, atomic invoice/SST issuance and the academic/administrative boundary.

## CAP-50 Immigration and Student Pass

The canonical graph adds a fourth selectable student, Mei Lin, whose active Cyberjaya enrolment has no Student Visa / Pass and demonstrates the approved already-enrolled new-application assumption. Her Student Profile now retains nullable passport expiry and issuing-country fields; her Admissions-owned offer letter, academic qualification evidence, and English certificate support the reuse scenario. Rizal demonstrates renewal or student-requested cancellation against his exact current pass. Nadia's Registry approval creates the graduation-triggered cancellation for her completed enrolment. Alya remains a Malaysian citizen with no fabricated pass and no Immigration route access.

`CampusImmigrationConfiguration` is unique by campus and Programme Version and references three explicit international-services Fee Items plus the International Office name and passport collection location. `ImmigrationCase` owns the selected student, Programme Enrolment, campus, type, trigger, revision, status and timestamps. Its nullable form, pass, invoice and medical-result data are constrained by type: new applications gain a pass only at completion and require the medical-result snapshot from medical review onward; renewal/cancellation require the exact existing pass and reject a medical result; graduation creates a fee-waived cancellation without a form or invoice; and cancellation cannot record a renewed expiry. Case-owned document checks and events retain their own stable IDs and timestamps.

Starting a CAP-50 Online Form snapshots owned evidence from the selected Student Profile, Student Documents, Academic Qualifications, and English Proficiency records. Required-field validation accepts that snapshot or a case-specific replacement upload. Submitting creates Immigration review checks from the source actually used without changing the underlying Profile record, Finance, or Student Visa / Pass. Accepting every check issues one globally numbered Finance Invoice, line and immutable SST snapshot: RM2,650 for a new pass, RM954 for renewal, or RM53 for student-requested cancellation under the current 6% Cyberjaya assumption. Graduation cancellation is waived. Full payment gates later processing. Completion creates a case-owned unique pass or updates the exact renewed pass expiry. Reaching cancellation completion marks the exact pass `cancelled`, records `cancelledAt`, preserves its original expiry, and drives the separate cancelled-pass issue. An eligible later new application creates a second pass record and becomes current without changing the cancelled history.

`PortalApi.getImmigration()` resolves selected student → exact selected Programme Enrolment → campus configuration, current pass, newest active/historical case, Finance invoice, document checks, linked medical form, medical result and event history. It feeds a Student Pass overview and a separate Applications step page. The existing Online Forms start/save/submit/withdraw operations handle the case-linked medical form: they accept one non-empty PDF/JPEG/PNG of at most 25 MB only for the exact selected student's current new-pass case at eVAL approval, reject stale revisions or session generations, snapshot the exact `immigrationCaseId`, store metadata without file contents, and advance or reopen medical review. `PortalApi.getAcademicModuleRegistration()` traverses the selected active enrolment to citizenship, campus, exact pass and newest exact new-pass case before returning confirmation access and Student Pass progress. Both fee preview and confirmation repeat that live derivation before any Finance or registration write. Online Form mutations invalidate the full selected-session query scope, and development Admin Actions invalidate the shared affected views, so the registration gate and notice refresh with case submission, withdrawal, eVAL progression, medical review, and pass completion. Development-only staff commands validate student/case/revision/session ownership and remain absent from the production API and bundles. See [Immigration](immigration.md).

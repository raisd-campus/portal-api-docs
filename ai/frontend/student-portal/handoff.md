# Cross-session handoff

Read this after `AGENTS.md` when resuming the project in a new Codex chat or on another machine. It is a short current-state record; the linked project documents remain the source of truth.

## Current baseline

### Organization repository import — 21 September 2026

- `raisd-campus/student-portal` is the canonical repository for this frontend. The previous `faidzamin/cms-student-portal` remote is retained locally as `personal` for historical access; the organization repository is configured as `origin`.
- The organization starter commit and the portal's complete development history are joined by a normal unrelated-history merge without rewriting either lineage. Only `main` is published to the organization repository; the divergent local completion branch and checkpoint tag remain unpublished there.
- The imported application remains a frontend release candidate using the replaceable, session-only mock `PortalApi`. The combined repository README preserves the control-plane SDD-05 direction and the rule that prototype screens are not Live until their production API, persistence, authorization, and corresponding staff or lecturer workflows exist.

### Release delivery — 21 September 2026

- The Cyberjaya frontend release-candidate documentation has been rechecked against the completed implementation, including the Accommodation removal, lifecycle audit repairs, and Student Pass workflows. This delivery introduces no additional product behaviour or scope change.
- Final verification on 21 September 2026: `$env:PLAYWRIGHT_WORKERS='2'; npm run check:all` exited 0 with direct-token and Services-asset checks, warning-free lint, **689/689 unit tests across 86 files**, TypeScript/production build, production developer-tool exclusion, bundle budgets, and **228/228 Playwright checks (10.2m)** at 1280px and 1440px. Bundle validation reports a **340,900-byte** largest chunk and **192,954 gzip bytes** of eager JavaScript against the **194,816-byte** limit.
- The committed snapshot and its final verification result are recorded in Git history and the delivery report. The accepted frontend-boundary warnings below remain the current deferred work; they are not release-blocking defects for this prototype.

### Cyberjaya frontend release candidate: Accommodation removal and lifecycle audit — 16 September 2026

- Accommodation is removed from the active portal contract and UI: there is no `PortalPage` value, sidebar item, placeholder, Community service/icon, Services catalogue record, Finance category/exemption/ledger entry, or Graduation department/status branch. `/accommodation` replace-navigates to Dashboard. `/services`, `/services/accommodation`, `/services/finance` and unknown Services slugs replace to Transportation.
- Cyberjaya Services now has seven entries in order: Transportation, Dining, Wellness, Accessibility Support, Connectivity, Shopping and Recreation. Six are source-backed; Accessibility remains the approved campus placeholder. The active inventory is **34 images / 68 WebP variants / 6 active source categories**. The provenance manifest deliberately retains the retired Finance source capture separately, so it contains seven source-page URLs without treating Finance as an active category. Accommodation source-layout records, restoration metadata, originals/restorations and four public variants are deleted. The Accommodation-only split-table contract and renderer are also removed.
- Finance removes the Accommodation Fee Item, `INV-000019`, line, tax snapshot and allocation. `PAY-S4-002` is **RM6,390**. Semester 4 reconciles to **RM14,750 pre-tax charges**, **RM12,390 confirmed payments** and the unchanged **RM1,852 outstanding balance**. The canonical graph now ends at 18 historical invoices, so Alya's first registration creates `INV-000019` and `INV-000020`.
- Graduation clearance is exactly Faculty, Finance and Library. The campus applicability flag, Accommodation check, `not-applicable` clearance status and development action are removed. Approval requires all three checks to be cleared. Ordinary residential addresses and dated historical planning/audit/handoff evidence remain intact.
- The [student lifecycle release audit](audits/2026-09-16-student-lifecycle-release-audit.md) now has verdict **Ready with accepted warnings**. RC-01 through RC-17 are closed with permanent regression coverage: foundational graph ownership/uniqueness, generation and write-revision guards, readable deferred/withdrawn and multi-programme history, release-safe materials, unapplied-payment reconciliation, post-registration Calendar projection, live Student Pass issue destinations, the atomic completion transition, centralized query invalidation, historical lecturer projection, canonical route fixtures, assignment-deletion recovery, exact learning-item routing, campus-calendar deadlines, typed next actions, and product-safe errors.
- The selected Programme Enrolment is URL-backed and owned across Profile, Student Pass/eVAL, Module Registration, Finance, teaching delivery, attendance, materials, submissions, lecturer reviews, results/repeats, Online Forms and Graduation. Restricted writes require an active owned context; completed/deferred/withdrawn contexts retain readable history and explicit next-step ownership. Issue cards, header attention, local workflow notices and their resolution triggers now derive from the same canonical records.
- UI and flow repairs reuse the established shells and semantic patterns. Required actions route to an exact destination; waiting states name the university or government owner; unavailable actions render guidance rather than enabled no-op controls; loading/error/empty/completed states retain their route geometry. The Services form-entry loading skeleton now exposes a valid status role, and scenario/Immigration browser checks wait for the completed route transition before asserting the destination.
- Final verification: `$env:PLAYWRIGHT_WORKERS='2'; npm run check:all` **exit 0** on 16 September 2026. Direct-token and Services-asset checks, warning-free lint, **689/689 unit tests across 86 files**, TypeScript/production build, production developer-tool exclusion, all bundle budgets, and **228/228 Playwright checks (9.5m)** pass at 1280px and 1440px, including the retained 375/390/430/639/640/768 Services content widths. The largest chunk is **340,900 bytes**; eager JavaScript is **192,954 gzip bytes** against the **194,816-byte** limit. Focused stabilization passed the Services scenario route **6/6**, first-semester accessibility **2/2**, and renewal-fee transition **6/6** across both desktop widths.
- The repository change-surface audit reports **200 changed files** across contracts, canonical data, API/query runtime, routes/UI, explorers, documentation and tests with no obvious companion-layer gaps. Services validation passes at 34/68/6, and `git diff --check` reports no whitespace error.
- Accepted warnings are operational dependencies rather than open frontend defects: authentication/authorization, persistence and audit logging, live payments/uploads, production staff operations, notifications, document delivery and EMGS/STARS remain deferred. Campus owners must still confirm Immigration, Finance, curriculum, completion, Graduation and collection policy assumptions; automated desktop accessibility evidence is not formal certification. Direct Playwright runs emit the existing `NO_COLOR`/`FORCE_COLOR` notice, while Git reports CRLF-to-LF normalization notices for edited files. The next delivery stage is backend and campus-operational readiness. No commit or push had been requested or performed at the time of this 16 September baseline.

### Student Pass reapplication after cancellation — 16 September 2026

- CAP-50 now models `cancelled` separately from natural expiry and retains the original pass, expiry date, cancellation timestamp and cancellation-case link as history. One shared selector prefers the single valid/expiring pass and otherwise returns the latest cancelled/expired pass; graph validation rejects multiple simultaneously current passes.
- A completed student-requested cancellation on an active eligible Cyberjaya international enrolment exposes **Start New Student Pass Application** when no Immigration case is active and the form is configured. Graduation-triggered and university-directed cancellations, inactive enrolments and natural expiry remain under International Office review. The ordinary New Student Pass **Before you start** section includes the outside-Malaysia review condition and links to official Malaysian Immigration guidance.
- Reapplication reuses the existing five-step Immigration and Online Forms lifecycle. Each attempt creates a separate immutable submission, owned-evidence snapshot, case, document checks and Finance invoice; completion creates a case-owned Student Pass ID and number while leaving the cancelled pass untouched. Applications then returns to its neutral empty state and Student Pass selects the replacement valid record.
- Profile and the global attention count derive a destructive **Student Pass Cancelled** / **New application required** issue. Module Registration remains blocked in its distinct cancelled state until the replacement reaches eVAL approval, then follows the existing eVAL unlock policy until the new pass resolves the notice. Study Plan, Graduation, Profile Documents and other projections use the shared selector and never treat historical cancellation as current validity.
- Immigration retains the established shared page anatomy and semantic tokens: valid uses success/green, expiring and active processing use warning/orange, and expired/cancelled use destructive/red. The maintained step-workflow reference records the same status-to-component and action-placement rules; `quick_validate.py` reports the repository page skill valid.
- Final verification: `$env:PLAYWRIGHT_WORKERS='2'; npm run check:all` **exit 0** on 16 September 2026. Direct-token and Services-asset checks, warning-free lint, **580/580 unit tests across 75 files**, TypeScript/production build, production developer-tool exclusion, bundle budgets and **216/216 Playwright checks (9.2m)** pass at 1280px and 1440px. Eager JavaScript is **194,690 gzip bytes** against the unchanged **194,816-byte** limit. The focused cancellation/reapplication browser flow also passes **2/2** at both widths. Direct `npx playwright` runs retain the existing `NO_COLOR`/`FORCE_COLOR` environment warning.

### Immigration connection audit and cancellation status — 15 September 2026 (superseded cancellation behavior)

- The CAP-50 audit traced the exact enrolment-owned Student Pass through Immigration, Online Forms, Finance/Admin Actions, Profile issue cards, the global attention count, Module Registration, Study Plan and Graduation. Profile and dashboard issue projections derive Student Pass attention from the selected enrolment's pass records on every read instead of retaining a stale seeded warning. This baseline originally represented completed cancellation as expiry; the 16 September baseline above supersedes that behavior with the distinct historical `cancelled` state and eligible reapplication.
- Module Registration now treats an elapsed pass date as expired using the enrolment campus calendar even if an older record still says `expiring`. Study Plan and Graduation derive campus-local/international applicability by comparing citizenship with the exact enrolment campus rather than hard-coding Malaysia. The new-student dashboard receives the same API reference time as Profile and Immigration, static explorer fixtures use an explicit reference time, and personal-profile pass ownership now uses the exact read enrolment.
- Renewal Step 3 keeps its completed EMGS processing summary but removes the redundant one-row Case history. New Student Pass Step 3 retains its scoped EMGS/eVAL timeline, while renewal passport/endorsement history remains in Step 4. Student-facing Online Forms prefills use **Student Pass number** and **Student Pass expiry**; the Data Model Explorer includes `studentPassRequired` and describes Student Pass deadlines. Stale project-scope and Graduation documentation now match the implemented issue-resolution and cancellation milestone behavior.
- Regression coverage at this baseline verified the former cancellation presentation, renewal issue resolution, global count inputs, expiry-by-date registration blocking, campus-relative applicability, exact ownership, and the absence of renewal Step 3 history at 1280px and 1440px. The current cancellation/reapplication verification is recorded above.
- Final verification: `$env:PLAYWRIGHT_WORKERS='2'; npm run check:all` **exit 0**. Direct-token and Services-asset checks, warning-free lint, **578/578 unit tests across 75 files**, TypeScript/production build, production developer-tool exclusion, bundle budgets, and **216/216 Playwright checks (8.6m)** pass. Eager JavaScript is **194,673 gzip bytes** against the unchanged **194,816-byte** limit. Direct focused Playwright commands retain the existing `NO_COLOR`/`FORCE_COLOR` environment warning.

### Student Pass status surfaces and renewal-fee review — 15 September 2026

- Student Pass overview now uses the shared semantic SummaryCard variants consistently: valid is success/green, expiring is warning/orange, and expired is destructive/red. Renewal document checks describe university-owned academic and attendance items as **University assessment in progress**.
- Development-only Admin Actions now exposes the selected Immigration case's exact unpaid invoice in its Immigration panel, including invoice number and outstanding amount. A pending transfer proof can be verified there; otherwise the counter-payment shortcut selects the case invoice and pre-fills its amount and receipt reference in the existing Finance controls. Settlement continues through the Finance mutation and advances the paid case to its next Immigration stage.
- Focused desktop Immigration coverage exercises the completed valid-pass surface, expiring renewal surface, contextual renewal fee selection, counter-payment settlement, and the resulting Step 2 fee-settled state at 1280px and 1440px. Final verification: `$env:PLAYWRIGHT_WORKERS='2'; npm run check:all` passed with warning-free lint, **577/577 unit tests**, production build, developer-tool exclusion, bundle budgets, and **216/216 Playwright checks** in 9.7 minutes. Eager JavaScript is **194,670 gzip bytes** against the unchanged **194,816-byte** limit. No commit or push was performed.

### Immigration Step 5 scoped case history — 15 September 2026

- New Student Pass Step 5 now places its own six-stage Case history in the `SummaryCard` footer: **Passport requested**, **Passport received**, **Endorsement processing**, **Student Pass expiry updated**, **Passport ready for collection**, and **Passport collected**. The equivalent renewal passport-and-endorsement step uses the same scoped history. Earlier application, fee, EMGS, eVAL, and medical milestones remain excluded from this step.
- The history reuses the established connected timeline, including shared success/warning semantic tokens, solid completed connectors, dashed unfinished connectors, dates, and phase-specific badges. Browser coverage verifies the initial passport-request state and the later endorsement-processing state, including all six rows and the recorded-expiry milestone.
- Verification: `npm run build`, `npx vitest run src/tests/immigration.test.ts` (**9/9**), `npx playwright test tests/e2e/immigration.spec.ts` (**10/10** at 1280px and 1440px), `npm run check:bundle`, the change-surface audit, rendered 1440px screenshot inspection, and `git diff --check` pass. Eager JavaScript is **194,678 gzip bytes** against the unchanged **194,816-byte** limit. The direct Playwright run retains the existing `NO_COLOR`/`FORCE_COLOR` environment warning. No commit or push was performed.

### Immigration Step 4 Online Forms lifecycle — 15 September 2026

- New Student Pass Step 4 no longer embeds a one-off file uploader in the Immigration progress hub. After eVAL approval it presents one workflow `SummaryCard` and opens the case-linked **Medical Screening Result** Online Form. That form owns the same template, prefill, Before you start, draft, Save and close, resume, confirmation, immutable submission, withdrawal, status history and review presentation used by the Step 1 application form.
- The medical form is hidden from the general eight-template catalogue and becomes eligible only for the selected student's exact active new-pass case after eVAL. Starting a draft snapshots its `immigrationCaseId`; the required field accepts one non-empty PDF, JPEG or PNG under the shared 25 MiB limit. Submission atomically advances the same case to medical review, withdrawal returns it to the eVAL-approved Step 4 state, and staff acceptance completes the linked form before Step 5 becomes available. No Profile document is changed and no file contents are retained.
- Immigration reflects the linked form lifecycle as **Open Medical Screening Form**, **Resume Medical Screening Form**, **Medical result under review**, or **Medical result accepted**. Draft and review states link back to the exact form; accepted state keeps the form link secondary and adds primary **Proceed to Step 5**. The embedded uploader, direct Immigration medical-result Portal API mutation, and duplicate local validation path were removed.
- Contracts, canonical graph relationships, the Data Model Explorer, Online Forms context/submission/withdrawal behavior, Admin Actions review, Module Registration test setup, the route-page workflow skill, project scope, data model, design system, Immigration and Online Forms documentation, and desktop coverage are synchronized. The workflow reference now requires structured upload stages to use the existing Online Forms lifecycle instead of creating a second progress-hub uploader. `quick_validate.py` reports the repository page skill valid, and the change-surface audit reports no missing companion layer.
- Focused verification passes **97/97 unit tests** across five affected files and **10/10 Immigration Playwright checks** at 1280px and 1440px. Rendered Medical Screening Result draft and Immigration under-review screenshots were inspected. The first full-gate attempt reached **576/577 unit tests** and stopped on the unrelated Performance chart test's existing 5-second timeout; its immediate isolated rerun passed **7/7** without a product or test change.
- Final verification: `$env:PLAYWRIGHT_WORKERS='2'; npm run check:all` **exit 0** on the clean rerun. Direct-token and Services-asset checks, warning-free lint, **577/577 unit tests across 75 files**, TypeScript/production build, production developer-tool exclusion, bundle budgets, and **214/214 Playwright checks (9.0m)** pass at 1280px and 1440px. Eager JavaScript is **194,676 gzip bytes** against the unchanged **194,816-byte** limit. Direct Playwright commands retain the existing `NO_COLOR`/`FORCE_COLOR` environment warning. No commit or push was performed.

### Immigration five-step new-pass flow and unified timeline tokens — 15 September 2026

- New Student Pass applications now use five URL-backed steps: Application, Review and fee, EMGS and eVAL, Medical screening, and Passport and endorsement. Renewals use four steps and cancellations retain three. There is no Complete tab; completion removes the active case and restores **No application in progress**. Legacy `step=complete` parses as Step 5 and shorter case flows fall back to their recommended step.
- At eVAL approval, Step 4 becomes an orange action-required state with International Office instructions, the official EMGS guidance link, and the shared upload field. The student can submit one non-empty PDF, JPEG, or PNG medical result up to 25 MB. The mock API validates selected-student/enrolment/campus ownership, exact case revision, eVAL state, file policy, and session generation, stores immutable metadata only, and moves the case to **Medical result under review**. Admin Actions cannot bypass this student submission and accepts it before passport handover begins in Step 5.
- The Student Pass missing state keeps the shared empty-state layout and uses the warning surface/icon treatment. Case history is scoped to the selected numbered step: New Student Pass Step 3 shows only **EMGS processing** and **eVAL approved**, without repeating application, fee, medical or passport milestones; cancellation Step 3 similarly keeps only its cancellation-and-collection milestones. Every connector reaches the next marker. Completed markers and connectors use the exact `success-solid` token used by completed badges; the current marker uses the exact `warning-solid` token used by its badge. Upcoming markers/connectors remain neutral. These rules are recorded in the route-page workflow reference and design-system documentation.
- The Immigration contract, canonical example, Portal API/query mutation, route parser, Module Registration eVAL test path, Data Model Explorer metadata, project/scope/sidebar/data/design documents, workflow skill reference, and browser coverage are synchronized. Focused verification passes **62/62 unit tests** and **10/10 Immigration Playwright checks** across 1280px and 1440px; rendered screenshots were inspected for the missing-pass, timeline, medical upload/review, and passport states. The change-surface audit reports no missing companion layer and `git diff --check` passes.
- Final verification: `$env:PLAYWRIGHT_WORKERS='2'; npm run check:all` **exit 0** on 15 September 2026. Direct-token and Services-asset checks, warning-free lint, **576/576 unit tests across 75 files**, TypeScript/production build, production developer-tool exclusion, bundle budgets, and **214/214 Playwright checks (9.2m)** pass at 1280px and 1440px. Eager JavaScript is **194,779 gzip bytes** against the unchanged **194,816-byte** limit, leaving **37 bytes** of headroom. Direct ad-hoc Playwright commands retain the existing `NO_COLOR`/`FORCE_COLOR` environment warning.
- After the stage-scoped Case history follow-up, `npm run build` passes and the focused Immigration Playwright suite passes **10/10** at 1280px and 1440px. The rendered Step 3 screenshot was inspected and contains exactly the two EMGS/eVAL rows. The direct Playwright command retains the existing `NO_COLOR`/`FORCE_COLOR` warning.

### Immigration consolidated processing flow — 15 September 2026

- CAP-50 now uses three numbered Application steps: Application/document review, Review and fee, and one combined Student Pass processing step. The retired Step 4 and Complete URLs resolve to Step 3 while a case is active. Completion removes the case from the current Applications projection, so the page returns to the shared **No application in progress** state while the resulting pass remains on Student Pass.
- Full settlement of a new-pass or renewal invoice atomically records **EMGS processing started** in the same development transaction. The student therefore enters the active EMGS processing state immediately after Finance confirms payment; there is no separate not-started processing placeholder or staff advance action.
- Step 3 shows the entire case sequence as a connected vertical timeline. Completed milestones use checked circles, solid green connectors and semantic success badges; the current milestone uses the warning treatment; unfinished milestones use numbered circles, dashed connectors and neutral badges. New-pass labels preserve **EMGS** and **eVAL** casing and cover application, documents, fee, EMGS, eVAL, medical, passport, endorsement, expiry, ready-for-collection and collected states. Cancellation combines processing and collection in the same final step.
- New-pass eVAL and medical states include a separate raised guidance section that tells the student to wait for International Office scheduling details and links to official EMGS medical-screening guidance. Online Forms unread-reviewer attention remains on the catalogue/list pages and is hidden on template and submission details.
- The Finance invoice-detail action footer now uses the shared edge-to-edge divider. The route-level footer audit also aligned Profile forms and Lecturer Review; the embedded Portfolio detail action group retains its local column divider by design.
- The implementation, workflow skill reference, Immigration/Finance/Online Forms/data/design/sidebar/scope documentation and focused unit/browser coverage are synchronized. Final verification: `$env:PLAYWRIGHT_WORKERS='2'; npm run check:all` **exit 0** on 15 September 2026. Direct-token and Services-asset checks, warning-free lint, **575/575 unit tests across 75 files**, TypeScript/production build, production developer-tool exclusion, bundle budgets and **214/214 Playwright checks (9.2m)** pass at 1280px and 1440px. Eager JavaScript is **194,663 gzip bytes** against the unchanged **194,816-byte** limit. The accessibility sweep also corrected the dashboard calendar Suspense skeleton to expose its loading label through a valid status role. The focused Immigration and action-footer regression run passes **6/6**. Direct ad-hoc Playwright commands emit the existing `NO_COLOR`/`FORCE_COLOR` environment warning.

### Immigration step-flow consistency — 15 September 2026

- Immigration now maps each case state to the same step composition used by Graduation. Submitted applications present **Under review** in the SummaryCard action slot. Application/document checks occupy the card's flush-divided third section with solid semantic badges; accepted checks show an explicit International Office update instead of retaining an awaiting-review message.
- New-pass and renewal Step 1 no longer leak Finance actions. Once accepted, the green application summary keeps secondary **View Application Form** beside primary **Proceed to Step 2**. Step 2 is one fee SummaryCard with its invoice total and payment status, matching Graduation fee. Outstanding payment exposes only **View Immigration Invoice**; settlement makes that link secondary, unlocks Step 3, and adds **Proceed to Step 3**. Generic **Go to Current Step** actions are removed.
- Cancellation retains its approved combined **Request and fee** Step 1: document review changes into the fee-pending presentation before the step completes. Passport handover, government processing, cancellation, collection, historical steps, unavailable steps, and Complete now use phase-specific summaries and actions. Tabular case history lives in SummaryCard `footerContent`; independent arrival-medical guidance remains a raised `InnerSection`.
- The repository-local `cms-student-portal-page` skill now requires a state matrix for multi-step routes and links to `references/step-workflows.md`. The reference defines document-review, accepted, fee-pending, fee-settled, processing/collection, unavailable, and Complete archetypes; it also requires absence assertions for cross-step action leakage. `quick_validate.py` reports the skill valid.
- Required-gate follow-up corrected the existing selected Timetable location label from a 70% foreground treatment to the full semantic brand foreground after axe reported 3.02:1 contrast. The focused current-route accessibility sweep passes at 1280px and 1440px, and the isolated Services 768px retry passes at both projects.
- Final verification: `$env:PLAYWRIGHT_WORKERS='2'; npm run check:all` **exit 0**. Token and Services-asset checks, warning-free lint, **575/575 unit tests across 75 files**, TypeScript/production build, developer-tool exclusion, bundle budgets, and **214/214 Playwright checks (9.0m)** pass at 1280px and 1440px. The focused Immigration run passes **10/10**, including SummaryCard anatomy, phase-specific statuses, explicit next-step actions, and absence of leaked form/invoice/current-step actions. The change-surface audit reports no missing companion layer; eager JavaScript is **194,662 gzip bytes** against the unchanged **194,816-byte** limit.
- The first full browser run reached **211/214**: both accessibility projects exposed the deterministic Timetable contrast issue, and one unrelated Services 768px case missed its initial content load. The semantic contrast correction and focused Services retry passed, followed by the clean full rerun. Direct Playwright commands retain the existing `NO_COLOR`/`FORCE_COLOR` environment warning.

### CAP-50 follow-up UI refinement — 15 September 2026

- Profile presents the derived required-pass issue as **Student Pass Required** with **Start your Student Pass application**, replacing the generic action copy while retaining its Immigration destination and global attention count.
- Applications with no active case now reuse the neutral shared `EmptyState`, including an in-card standard brand underlined **View Student Pass** link. The former separate action bar is removed.
- Online Form template pages keep their required-document list inside the raised **Before you start** section. The shared `FileUploadField` shows uploaded and read-only files in raised centred tiles within its dashed boundary, keeps file sizes and remove actions there, uses the underlined shared link treatment for replacement, and does not repeat source or submission-selection labels. Finance continues to expose its replacement action through the same component.
- Verification: focused coverage passed (**30/30 unit tests** and **30/30 targeted Playwright checks** at 1280px and 1440px); the required `$env:PLAYWRIGHT_WORKERS='2'; npm run check:all` gate also passed with **575/575 unit tests** and **214/214 Playwright checks** in 8.6 minutes. The first full-suite attempt timed out only in the unrelated `performance.test.tsx` synchronization test; its focused retry passed, and the complete rerun passed.

### CAP-50 Malaysian Student Pass and Immigration — 15 September 2026

- Immigration now has URL-backed **Student Pass** and **Applications** inner tabs in the established `TabbedPagePanel`. `/immigration` opens the pass overview; `/immigration?tab=applications` opens application progress; valid legacy URLs containing only `step=1|2|3|4|complete` still open Applications. The route keeps the shared portal heading, content width, loading/error/unavailable geometry and Community-expanded desktop behavior.
- Student Pass shows a neutral empty state and application action when no pass exists. Existing passes use `SummaryCard` and `DetailSectionCard` for status, pass number, passport number and passport/pass expiries, followed by eligible renewal or cancellation actions in `PageActionBar`. A pass whose recorded expiry has elapsed is projected and presented as expired even when an older fixture still says expiring; expired cases provide International Office guidance and no ordinary renewal action.
- Applications owns the existing Graduation-style workflow: New Student Pass uses **Application**, **Review and fee**, **EMGS processing**, **Arrival and endorsement**, then **Complete**; renewal replaces the fourth stage with **Passport endorsement**; cancellation uses **Request and fee**, **Passport handover**, **Cancellation**, **Passport collection**, then **Complete**. With no current or completed case it shows the neutral shared empty state with an in-card standard brand underlined link back to Student Pass. The former three-card Available applications section is removed.
- Module Registration restores its Semester summary to the neutral presentation and renders a separate Student Pass `SummaryCard` only while an international student's new-pass process is incomplete. Before eVAL the notice links to Applications and confirmation/fee preview remain blocked at both UI and API boundaries. At `eval-approved` and later milestones the notice remains visible while confirmation unlocks; it disappears once the pass is created. The eVAL threshold remains a Cyberjaya prototype assumption requiring campus confirmation.
- Account attention now derives `studentPassRequired` for an international student with an active enrolment and no completed Student Pass. It contributes to the global **Action required** count, appears in Profile, opens `/immigration`, and resolves when the pass is created. The existing `expiringVisa` issue is presented as **Student Pass Expiring** or **Student Pass Expired** from its deadline and uses the same destination.
- Immigration Online Forms reuse university-held evidence by field. Mei's passport copy, photograph, recorded passport expiry/issuing country, offer letter, academic qualification files and English certificate come from canonical admission/profile records; only the health declaration is case-specific. Renewal reuses passport and photograph, while cancellation reuses the passport. Missing evidence restores the upload control; a replacement is snapshotted only into that immutable submission and never rewrites Profile Documents. Immigration document checks distinguish **On file** from **Submitted** evidence, with academic standing and attendance retained as university-owned renewal checks.
- `FileUploadField` now has distinct empty, uploaded, multi-file and read-only presentations. Filenames, sizes and accessible remove/replace actions stay inside the dashed container, the duplicate file list is removed, and helper/error text follows the 8px form rhythm. The DatePicker alone uses portal `DropdownSelect` month/year captions with previous/next navigation and keyboard support; its normal range is 100 years before through 20 years after the current year and expands to retain an out-of-range selected value.
- The validated contracts now include Immigration applicability, canonical nullable passport expiry/issuing-country facts, independent Module Registration Student Pass progress, `studentPassRequired`, field-keyed owned evidence and immutable evidence snapshots. Required-upload validation accepts an owned snapshot or case-specific upload on both client and API paths, while selected-student ownership, stale revisions, session generations and ordered Immigration milestones remain enforced.
- Accepted documents continue to issue Finance-owned international-services invoices with immutable SST snapshots: RM2,500 + 6% = RM2,650 for a new application, RM900 + 6% = RM954 for renewal and RM50 + 6% = RM53 for student-requested cancellation. Graduation-triggered cancellation reuses the same fee-waived CAP-50 case. Development-only Admin Actions remains outside `PortalApi` and production bundles and now opens the reviewed case directly in Applications.
- Final verification: `$env:PLAYWRIGHT_WORKERS='2'; npm run check:all` **exit 0** on 15 September 2026. Direct-token and Services-asset checks, warning-free lint, **575/575 unit tests across 75 files**, TypeScript/production build, production developer-tool exclusion, bundle budgets and **214/214 Playwright checks (11.0m)** pass at 1280px and 1440px. Eager JavaScript is **194,661 gzip bytes** against the unchanged **194,816-byte** limit, leaving **155 bytes** of headroom. The focused Immigration browser flow and owned-evidence Online Forms flow also pass. The final change-surface review covers contracts, canonical records, projections, API/query boundaries, cache invalidation, route/UI consumers, explorers, documentation and tests; `git diff --check` passes. Direct `npx playwright` runs emit the existing `NO_COLOR`/`FORCE_COLOR` environment warning; the required wrapper gate has no functional warnings.
- Scope remains frontend-only, dark-only and session-only. No live EMGS/STARS or Malaysian Immigration integration, government decision, upload storage, payment gateway, persistence, dependants, progression, variation, Graduate Pass or Special Pass workflow is implied. Document ownership, eVAL registration gating, fees, tax treatment, campus configuration and operational decisions remain prototype assumptions requiring campus approval.

### Graduation steps and completion — 14 September 2026

- Academic > Graduation now separates the eligible journey into four URL-backed numbered steps—Clearance, Graduation fee, Convocation and Documents/student pass—followed by a separate **Complete** tab. The shared segmented navigator marks completed steps with a check icon and defaults to the next incomplete page as the mock workflow advances.
- Each step owns its contextual `SummaryCard` and related detail sections. Explicit `?step=` selections support direct navigation and Back/Forward; invalid values fall back to the recommended step. The existing `/academic/graduation#documents` deep link selects Step 4. Ineligible students retain the previously approved compact Study Plan empty state.
- A submitted or under-review clearance uses the faded-warning SummaryCard until Registry gives its final approval, including after all applicable department checks are clear. Faculty, Finance, Library and applicable Accommodation each require an explicit department decision; a settled account remains an eligibility fact and no longer auto-clears Finance. Its form ID appears in the existing summary-column section, and its departmental checks appear in the flush-divided SummaryCard footer with a normal `type-title` InnerSection heading, a 16px title-to-table gap, and solid semantic badges; the former Clearance application detail card is removed. Clearance status occupies the right action slot as a large solid Badge, while the clearance-form or approved **Proceed to Step 2** primary action follows the Eligibility and clearance table in an edge-to-edge `PageActionBar`. `DataTable` omits its terminal body-row divider by default.
- The approved Clearance footer keeps secondary **View Graduation Clearance Form** beside primary **Proceed to Step 2**. Graduation Fee uses one SummaryCard: a large solid payment-status Badge occupies its action slot, Invoice total is a summary column, and the action footer owns invoice and next-step navigation. A pending transfer proof has one **Pending validation** Badge and uses the third flush-divided section only for its Finance explanation; settlement changes the invoice link to secondary and adds primary **Proceed to Step 3**.
- Convocation reads **Not started** before a registration exists and **Under review** after an attending response is submitted, then keeps Attendance, conditional Gown size and form ID in one SummaryCard. Its ceremony explanation is a separate borderless raised `InnerSection`, while the flushed footer owns Open/View Form and adds **Proceed to Step 4** after confirmation. A submitted **Not attending** response now completes Step 3 immediately, receives the green recorded treatment and defaults the hub to Step 4; the developer confirmation command rejects that branch as unnecessary.
- Step 4 now has separate main Documents and international Student pass SummaryCards. Untouched Documents uses the neutral default surface, active work uses orange, and the card becomes green as soon as all three records are collected regardless of passport progress. Student pass remains orange until passport return and then becomes green independently. Its sequence includes a distinct **Ready for collection** state after cancellation completion and before passport return. Its headline and single description line explain the next student or university action for every passport state; the redundant separate-process sentence is removed.
- After every applicable Step 4 condition passes, Graduation opens a separate **Complete** tab. Its default neutral SummaryCard places a green checked icon above **Congratulations!**, keeps milestone dates and the restored **Download Transcript** visual action, and leaves the completed process cards on Step 4. The transcript action remains an intentional frontend no-op; document generation/delivery remains outside scope.
- Eligibility, clearance, Finance and completion data rules are unchanged. The new non-attendance transition is derived from the existing immutable form response, while the Step 4 changes are presentation-only and consume the existing query response.
- Focused verification: all **21/21** Graduation unit tests pass. `npx playwright test tests/e2e/graduation.spec.ts --workers=2` passes **6/6** at 1280px and 1440px, including the separate manual Finance decision with a settled account, ordered progression through the passport-ready-for-collection state, the independent Documents/Student pass colors, Congratulations state, transcript action, attending review label, direct non-attendance transition and unavailable developer confirmation. It also retains the single pending-proof status, approved-clearance actions and actual Admin Actions container scroll.
- Pre-commit full-gate evidence: `$env:PLAYWRIGHT_WORKERS='2'; npm run check:all` **exit 0** on 14 September 2026. Token and Services-asset checks, warning-free lint, **562 unit tests across 74 files**, production build, production developer-tool exclusion, bundle budgets and **204/204 Playwright checks (9.3m)** pass at 1280px and 1440px. Eager JavaScript is **194,792 gzip bytes** against the unchanged **194,816-byte** limit, leaving **24 bytes** of headroom. `git diff --check`, focused formatting and the read-only change-surface audit pass with no companion-layer gaps. Direct ad-hoc browser commands emit the existing `NO_COLOR`/`FORCE_COLOR` environment warning.

### In-session Admin Actions navigation — 12 September 2026

- The development portal sidebar footer now includes **Admin Actions** above Log out. It uses client-side navigation to `/dev/admin-actions`, so a Nadia Graduation Clearance submission or Finance proof remains available for review after leaving the student page.
- The supported demonstration sequence is: choose Nadia in Student Scenarios, open the portal, submit Graduation Clearance, then select **Admin Actions** in the portal sidebar. Direct address-bar navigation and refresh still rebuild the frontend-only mock session and intentionally clear session mutations.
- The action retains its `UserRoundCog` icon in a development-only lazy chunk and sits directly above Log out within one padded footer group. The controls use only the standard button gap, with no divider or separately padded Admin Actions row. Focused browser coverage enters Admin Actions through the visible portal control immediately after Nadia submits clearance and asserts that her submission is still available before continuing the all-student review flow.
- Final verification: `$env:PLAYWRIGHT_WORKERS='2'; npm run check:all` **exit 0**. Direct-token and Services-asset checks, warning-free lint, **560 unit tests across 74 files**, production build, production developer-tool exclusion, bundle budgets and **202/202 Playwright checks (9.1m)** pass at 1280px and 1440px. The focused Graduation plus sidebar/chat-footer regression run passes **4/4**. Eager JavaScript is **194,811 gzip bytes** against the unchanged **194,816-byte** limit. The direct focused Playwright invocation emits the existing `NO_COLOR`/`FORCE_COLOR` environment warning; the completed full gate has no functional warnings.
- Follow-up footer and form-action-bar verification: the 1280px Graduation and sidebar/chat-footer flows pass, and the 1440px sidebar/chat-footer flow passes. `npm run build`, `npm run check:production`, and `npm run check:bundle` pass. Eager JavaScript is **194,814 gzip bytes**, leaving **2 bytes** against the unchanged **194,816-byte** budget. Direct Playwright invocations emit the existing `NO_COLOR`/`FORCE_COLOR` environment warning.

### All-student Admin Actions review workspace — 12 September 2026

- Admin Actions is no longer filtered by the student currently shown in the portal. Its student selector covers Alya, Rizal and Nadia, defaults to a student with a graduation process, and shows the selected student's clearance controls. Pending transfer proofs and open counter-payment invoices include the owning student's name and number.
- Every developer command now supplies the target student ID and retains the existing student/enrolment/campus/process/invoice ownership, revision and session-generation checks. The production `PortalApi` remains unchanged and the review implementation remains DEV-only.
- Scenario selection now changes the student view and clears scoped queries while retaining shared canonical Graduation and Finance mutations in the current in-memory mock session. This lets Nadia submit clearance, switch the portal to Alya, and still appear in Admin Actions. **Open Student Graduation** switches to the reviewed student before opening the hub. Refresh remains the reset boundary; no browser persistence or live staff workflow was added.
- Final verification: `$env:PLAYWRIGHT_WORKERS='2'; npm run check:all` **exit 0**. Direct-token and Services-asset checks, warning-free lint, **560 unit tests across 74 files**, production build, production isolation, bundle budgets and **202/202 Playwright checks (8.7m)** pass at 1280px and 1440px. The focused graduation flow covers Nadia submission, an Alya view switch, Nadia review, settlement and completion at both widths; focused unit coverage exercises all-student clearance/proof projection and cross-student ownership enforcement. The change-surface audit reports no obvious companion-layer gaps and `git diff --check` passes.
- Warning: direct Playwright invocations emit the existing `NO_COLOR`/`FORCE_COLOR` environment warning. Eager JavaScript is **194,802 gzip bytes** against the unchanged **194,816-byte** limit, leaving **14 bytes** of headroom. No timeout was increased and no test was skipped.

### Graduation and Online Forms state refinements — 12 September 2026

- Online Form template entries now keep only **Before you start** and its explanatory copy in the borderless raised `InnerSection`; eligibility/documents guidance and the separate bordered action bar follow it. The affected entry actions read **Start Form** and **Resume Form**. The shared Button primitive forwards authored label copy and does not force a title-case transform.
- Academic > Graduation now uses the same compact `EmptyState` pattern as a completed Module Registration for ineligible students. It explains that Study Plan and eligibility requirements must be completed and provides the **View Study Plan** link; the detailed graduation progress hub appears only once the entry gate passes.
- Verification: `npx playwright test tests/e2e/graduation.spec.ts --workers=1` passed **4/4** and `npx playwright test tests/e2e/online-forms.spec.ts --workers=2` passed **20/20**, both across 1280px and 1440px. `$env:PLAYWRIGHT_WORKERS='2'; npm run check:all` **exit 0**. The change-surface audit reports no obvious companion-layer gaps and `git diff --check` passes. Bundle validation reports **194,782 gzip bytes** of eager JavaScript against the unchanged **194,816-byte** budget, leaving **34 bytes** of headroom. The direct focused browser commands emit the existing `NO_COLOR`/`FORCE_COLOR` environment warning; the completed full gate has no functional warnings. Existing CAP-15 prototype assumptions and frontend-only boundaries are unchanged.

### CAP-15 Graduation clearance — 12 September 2026

- Academic > Graduation is now the shared-shell progress hub for eligibility, departmental clearance, the linked Finance fee, optional convocation, document collection and independent international passport progress. Online Forms owns immutable clearance/convocation submissions and Messages; Study Plan links to Graduation's Documents section. See [Academic Graduation](academic-graduation.md).
- Select **Graduation ready / Nadia Pratama** in developer Scenarios, submit clearance as the student, then use **Admin Actions** for Registry/department steps and Finance proof/counter-payment simulations. Nadia has six completed semesters, 120 credits, 3.00 CGPA, no current semester, cleared existing fees and valid initial pass coverage. Alya and Rizal keep their existing records.
- Approval atomically completes clearance and issues the RM500 sample base fee with the existing immutable SST snapshot (RM530 in Nadia's sample). Reviewed transfer proofs retain decisions; partial payments and counter receipts update the same ledger. Duplicate/stale/foreign actions fail without partial writes. Developer commands are outside `PortalApi` and their implementation is excluded from production builds; refresh still resets all transactions.
- Completed-enrolment reads retain the owned history in Graduation, Study Plan and Finance, and the owned campus in campus-information pages. Academic tabs reveal the active tab when Community narrows the panel. The mock Academic overview/detail, Finance proof and Chat operations are deferred to preserve the existing initial JavaScript budget; API behavior stays unchanged.
- Final verification: `$env:PLAYWRIGHT_WORKERS='2'; npm run check:all` **exit 0**. Direct-token and Services-asset checks, warning-free lint, **558 unit tests across 74 files (56.83s)**, production build (**1.99s**), production isolation and bundle budgets all pass. **202/202 Playwright checks (8.9m)** pass at 1280px/1440px, including graduation with Community open/closed/resized, keyboard controls, loading/retry/unavailable states, Back/Forward and no horizontal overflow. The focused Graduation/Finance/Portal API/graph run also passes **84/84**; the complete graduation desktop flow passes at both widths with a clean final-page axe scan. The change-surface audit reports no obvious companion-layer gaps, all **71** checked local Markdown links resolve, and `git diff --check` passes.
- Verification notes: the first full browser pass was **194/202**. Three legacy expectations needed to reflect the approved form gate, proof-history heading and second student-summary example; two tests needed explicit waits for save/submit navigation. Those cases and the complete suite now pass without removing tests or increasing timeouts. Direct focused Playwright invocations emitted the existing `NO_COLOR`/`FORCE_COLOR` environment warning; the final full gate emitted no warnings.
- Bundle warning: eager JavaScript is **194,786 gzip bytes** against the unchanged **194,816-byte** limit, leaving **30 bytes** of headroom. Academic read, Finance proof and Chat operation helpers are deferred to meet that budget. Future eager JavaScript growth requires further deferral or an explicit budget decision.
- Prototype assumptions: department rules, RM500 base fee/tax classification, gown sizes, collection location and passport sequence require campus approval. No live payment reconciliation, actual visa cancellation, production staff authorization, persistence, document delivery or mobile acceptance is implemented. The dated planning baseline and live checklist remain untouched. Next approved work item: none.

### Linked lists, Announcements, and Profile Documents refinement — 11 September 2026

- `LinkedList` titles now use the standard high-emphasis white body treatment, while descriptions use muted normal labels at the shared 4px title-to-description gap. The component continues to expose full-row route links, native actions, unavailable states, decorative icons and chevrons.
- Announcements now follows the University Policies composition: visible page title, a copy-free, gradient-free 288px generated campus-courtyard banner, then separate **Important Announcements** and **All Announcements** sections within a navigation-free panel. Both sections use clickable list rows with decorative 112px-by-64px cover/fallback thumbnails; the pinned section remains omitted when empty, and article routes, breadcrumbs, keyboard activation, history, loading/error/empty geometry and Community-width behaviour remain covered.
- Profile > Documents now presents only a Student Documents title and title-only `LinkedList` rows. Document activation is an intentional no-op while delivery remains out of scope; filenames, sources, table columns, and View/Download controls are no longer presented. Student Handbook remains absent from Documents and belongs only to University Policies.
- Final verification: `npm run check` **exit 0** with direct-token and Services-asset checks, warning-free lint, **539 unit tests across 73 files (47.73s)**, production build, production isolation, and bundle budgets passing. Eager JavaScript is **193,835 gzip bytes** against the unchanged 194,816-byte limit. The focused `LinkedList` and Announcements tests pass **23/23**, and the focused Announcements browser suite passes **8/8** at 1280px and 1440px. The change-surface audit reported no obvious companion-layer gaps and `git diff --check` passes. Next approved work item: none.
- Warning: `npm run check:all` runs Playwright with its default 10 workers because its wrapper does not forward worker arguments. Validation, lint, unit tests, build, production isolation and bundle checks passed, but its browser phase had **8 unrelated contention failures (190/198 passed)**. A direct full `npx playwright test --workers=2` run reduced that to one unrelated Services recovery timeout (**197/198 passed**); that exact case then passed in an isolated 1440px retry. No test timeout was increased or test suppressed. Playwright also emits the environment warning that `NO_COLOR` is ignored while `FORCE_COLOR` is set.
- Follow-up banner and list refinement: Announcements uses a locally generated high-resolution campus-courtyard image instead of the low-resolution One World Club/foosball image, while Policies retains the library image; both 288px surfaces render without overlay copy or a gradient. The generated PNG and 1600px/800px WebP variants are retained under `assets/announcements/` and `public/announcements/`; it is a synthetic frontend visual rather than a claim about the Cyberjaya campus. `TabbedPagePanelBanner.hideCopy` preserves the shared image geometry for this treatment. `LinkedList` now accepts a decorative thumbnail slot, demonstrated in the Design System Explorer and used by Announcements for cover/fallback thumbnails; its icon, title, description, chevron, route and native-action variants remain available.

### CAP-55 University Policies catalogue — 11 September 2026

- Added the standalone `/policies` destination immediately after Announcements. It uses the shared **University Policies** heading/breadcrumb, navigation-free `PagePanelLayout`, copy-free and gradient-free 288px Cyberjaya library banner, URL-backed category tabs, default inactive dividers, and raised title-and-summary policy cards.
- The 14 Cyberjaya catalogue entries are explicitly synthetic fixtures pending campus approval. Policy cards are enabled native actions for pointer/Enter/Space but intentionally leave the route unchanged. CAP-55 Student Frontend remains **Partial**; bodies, details, files, search, versions, effective dates, acknowledgements, notifications, publishing, backend data and mobile remediation remain outside this release. See [University Policies](university-policies.md).
- `UniversityPolicyRecord`, the campus projection, Zod response, Portal API/query, scenario scoping and both Data Model Explorer entries follow selected student → active Programme Enrolment → campus. Missing enrolment returns an empty collection, unknown students fail, and cross-campus fallback is prohibited. `LinkedList` now supports native action rows, while `PagePanelLayout` can omit navigation without creating an empty header or divider; both states appear in the Design System Explorer.
- Student Handbook moved from Profile Documents into the policy catalogue. The unused University Document record, campus relationship, route response collection and empty panel were removed; Profile Documents now contains only student-owned records.
- Final verification: `cmd /d /c "npm run check:all -- -- --workers=2"` **exit 0**. Token and Services-asset checks, warning-free lint, **538 unit tests across 73 files (48.25s)**, production build (**1.67s**), production isolation, bundle budgets and **204/204 Playwright tests (8.2m)** at 1280px/1440px all pass. Eager JavaScript is **194,756 gzip bytes** against the unchanged 194,816-byte limit. The focused unit set also passes **36/36 tests across 5 files**, and all **14/14** focused Policies/Profile Documents browser cases pass at both widths. The change-surface audit reports no obvious companion-layer gaps, all **58** checked local links across the 8 changed Markdown files resolve, and `git diff --check` passes. Next approved work item: none.
- Warning: the first bundle check exceeded the eager limit by 138 gzip bytes. Keeping the Policies loading frame local to its eager shell restored the established geometry without raising the budget; a later rebuilt chunk-hash combination exceeded the same limit by 3 bytes, so the sidebar uses the simpler existing open-book symbol and now passes with 60 bytes of headroom. The first `npm run check:all` browser phase also ran with Playwright's default 10 workers because npm did not forward the nested worker argument; 11 tests hit host-contention timeouts or missed initial data while all CAP-55 cases passed. The complete suite then passed with the wrapper invoked directly at two workers; no timeout was increased and no test was suppressed.

### Online Forms and shared page-pattern refinement — 11 September 2026

- Available Forms uses the shared 288px banner, full-width segmented category tabs and reusable `LinkedList` rows. Segment dividers appear only between inactive neighbours. Form descriptions and unavailable explanations are concise, and the optimized local WebP banner assets preserve the panel geometry.
- Form templates and cases retain the parent Online Forms shell and **Form Details** breadcrumb. Prefills use `DetailSectionCard`; preparation uses a borderless raised `InnerSection`; submitted answers remain visible in disabled field controls. Progress uses centred semantic stamps connected by a dotted line. Case messages sit 48px below and outside the form panel under a page-title heading, with a rounded borderless chat surface and edge-to-edge composer divider.
- My Forms uses a Messages column with warning-coloured singular/plural unread text. A separate warning `IssueCard` between the page title and tabbed panel replaces the tab counter and opens My Forms. Submitted status summaries use faded warning, success and destructive `SummaryCard` variants paired with solid white-text Badges. Completed and rejected outcomes remain in the status Badge and final Progress event without a duplicate banner.
- `PageActionBar` is the shared route-level completion area for Online Forms and the existing Lecturer Review, Profile, Portfolio and Finance consumers. Portfolio Add/Edit actions now sit below both form columns. The Design System Explorer and affected feature documents cover `LinkedList`, `InnerSection`, `PageActionBar`, the semantic SummaryCard/Badge pairings and their intended consumers.
- Latest verification before delivery: `cmd /d /c "npm run check:all -- -- --workers=2"` passes token and Services-asset checks, warning-free lint, **530 unit tests across 72 files (53.20s)**, production build (**2.04s**), production isolation, bundle budgets and **200 desktop Playwright tests (7.9m)** at 1280px/1440px. `git diff --check` and the change-surface audit pass with no companion-layer gaps. The existing documented white-on-orange warning `IssueCard` contrast exception remains unchanged; no new quality-gate warnings were introduced. Next approved work item: none.
- Warning: the initial unbounded `npm run check:all` invocation passed every non-browser stage but its Playwright phase failed 14 unrelated route checks under host saturation, including 30-second and 180-second timeouts. The unchanged complete suite then passed with the repository's established two-worker command; no timeout was raised and no test was suppressed.

### CAP-51 Online Forms — 10 September 2026

- Online Forms replaces Requests with Available Forms/My Forms tabs, campus category filtering, five sample templates and owned draft/submission details. Graduation, Immigration and Services Accessibility link to the same form journey; the legacy `/requests` URL replaces history. Privacy links to the available catalogue without introducing a sixth privacy template.
- Implemented session autosave/resume, current-profile prefills, owned published-result selection, embedded uploads, confirmation, immutable answers/documents, references and progress, permitted withdrawal and case conversations. Closed cases have no composer. Extra evidence stays in messages and never replaces original form documents. Seeded reviewer messages/outcomes demonstrate both sides; new student messages do not generate automatic replies or approvals.
- Badges count distinct owned forms with unread reviewer messages. Only messages displayed in a visible conversation advance the reviewer sequence watermark. Submitted appeal result metadata is snapshotted; historical same-campus submissions remain readable, and foreign-student/campus requests are rejected. Private caches and temporary resources are session scoped.
- Shared FileUploadField and chat presentation preserve Finance/Community behavior. Tab badges retain accessible labels; date/select errors link to their triggers and invalid uploads focus the visible picker. Both explorers, canonical graph and source documents are synchronized. See [Online Forms](online-forms.md).
- Final verification: `cmd /d /c "npm run check:all -- -- --workers=2"` **exit 0**; **519 unit tests / 68 files (49.72s)** and **198 Chromium browser tests (7.9m)** at 1280px/1440px pass. Token checks, all 36 Services images / 72 variants, lint, TypeScript, production build (**2.08s**), production isolation and bundle budgets pass. Eager JavaScript is **193,650 gzip bytes**, below the unchanged 194,816-byte limit. Exact log: `logs/cap-51-final-check-all.log`.
- Focused browser verification passes **18/18 (41.0s)** in `logs/cap-51-focused-browser.log`; captures in `logs/cap-51-browser/` were reviewed for catalogue, long forms and submitted conversations with Community open/closed/resized. Coverage includes all five forms, file validation, save/send failures, keyboard/focus, confirmation/cancellation, immutable submissions, withdrawal, unread badges, direct links, history, loading/error/empty states and scenario isolation. The full gate also passes existing Finance, Profile, Community, announcement, academic and isolated Services responsive regressions. No full-portal mobile or physical-device acceptance is claimed.
- Final review fixed queued visible-message acknowledgements, delayed case responses overwriting newer state, catalogue timezones when templates are absent, and exact visa/enrolment ownership. Ambiguous active enrolments fail closed. The initial gate exposed the eager-bundle regression; the Online Forms badge is now lazy, and the existing Announcement loading skeleton is separated from its real table module without changing appearance. Final change-surface review reports no companion-layer gaps (`logs/cap-51-change-surface.log`), all **126** checked local documentation links resolve, and `git diff --check` passes. **No quality-gate warnings remain.** The previously accepted white-on-orange IssueCard contrast exception is unchanged; no additional accessibility exclusions were introduced.
- CAP-51 Student Frontend is **Demo** for these five samples and session-only journeys. The next work requires separately approved campus form content and backend/reviewer workflow integration; no additional templates or staff screens are implied.
- Existing recovery package remains unchanged at `D:\Coding\backups\cms-student-portal\20260907T130310Z`. Google Drive remains unchanged. Mobile remains Not started and Backend Needs checking. Campus approval, live reviewer operations, file delivery/storage and dedicated privacy requests remain outstanding.

### CAP-39 Privacy & Security — 10 September 2026

- Added Privacy & Security as the sixth Profile tab at `/profile/privacy-security`. It retains the shared Profile summary/issues/panel and presents campus-owned information categories, a selected-student portal-access summary, ordered account-security guidance and the CAP-51 boundary for formal privacy requests.
- Follow-up presentation: the four borderless **Keeping your account secure** cards now pair their guidance with coordinated 16:9 security illustrations. The campus-owned contract supplies responsive 960px/480px WebP variants; cards retain their media geometry and accessible shield fallback if an image fails. The Privacy questions surface is also borderless, and its copy now directs students to Requests in present-tense student language while CAP-51 remains the documented workflow owner.
- Follow-up verification: focused Privacy/route units pass **19/19**, and the Privacy browser matrix passes **4/4** at 1280px/1440px with all four images loaded. Final `npm run check:all -- -- --workers=2` **exit 0** with **465 unit tests / 62 files (22.79s)**, production build **1.18s**, and **180 Chromium browser tests (7.1m)**. Token, Services-asset, lint, TypeScript, production-isolation and bundle checks pass with no application warnings; eager JavaScript is **194,644 gzip bytes**. Exact log: `logs/cap-39-security-cards-check-all.log`. The change-surface review reports no companion-layer gaps (`logs/cap-39-security-cards-change-surface.log`), and `git diff --check` passes.
- The working **Open IT Helpdesk** action uses a reusable app-level Community service request. It expands Community when closed, opens the existing IT Helpdesk conversation, preserves the Profile route and moves keyboard focus into the message composer. No Community contact, privacy form, consent switch, password/session control or policy acknowledgement was added.
- `CampusPrivacySecurityRecord`, `PrivacySecurityResponse`, the canonical campus relationship, Cyberjaya fixture, `PortalApi.getPrivacySecurity`, scenario-scoped query and both explorer examples are synchronized. Projection always traverses selected student → active Programme Enrolment → campus. Other campuses receive an empty Profile-shell state with no Cyberjaya fallback; missing active enrolments return a retryable error.
- CAP-39 Student Frontend is **Demo**, Mobile remains **Not started**, and Backend remains **Needs checking**. The page does not claim an approved legal basis, retention period, regulatory compliance or production security. Authentication, authorization, encryption, retention enforcement, backups, audit logging and security operations remain backend/institution responsibilities.
- Verification: final `npm run check:all -- -- --workers=2` **exit 0**, with **465 unit tests / 62 files (22.15s)** and **180 Chromium browser tests (7.1m)** across 1280px/1440px. Token and Services-asset checks, warning-free lint, TypeScript, production build (**1.20s**), production isolation and the unchanged bundle budgets pass; eager JavaScript is **194,651 gzip bytes**. The CAP-39 focused browser matrix passed **4/4** and covers Community open/closed/resized, focus, history, loading/error/empty states and overflow. Exact log: `logs/cap-39-privacy-security-check-all.log`. The white-on-amber IssueCard exception remains narrowly marked for the automated contrast audit; every other axe finding still fails.
- Verification history: the first complete run exposed an ampersand in a generated Explorer table name; the registry label now uses “and” and its focused model tests pass. The second run reached the existing eager-bundle limit by 191 gzip bytes. The Dashboard Calendar now follows the same lazy-loading pattern as the other Dashboard sections, keeps a loading skeleton and restores the existing budget without raising it. Final `git diff --check`, all **115** checked local links and the change-surface review pass with no companion-layer gaps (`logs/cap-39-privacy-security-change-surface.log`).
- Existing recovery material remains preserved. This delivery commits the accumulated CAP-33, CAP-35 and CAP-39 frontend work together on `main`; the live Google checklist remains unchanged for the owner to update separately.

### CAP-35 Accessibility Support and engineering accessibility audit — 9 September 2026

- Cyberjaya Services now contains eight campus-owned categories in this order: Accommodation, Transportation, Dining, Wellness, Accessibility, Connectivity, Shopping and Recreation. `/services/accessibility` presents **Accessibility Support** with a neutral image-free banner and the reusable empty-content state. It promises only that campus information, contacts and guidance will be published; it adds no invented service, policy, Community contact, form or request control. The source manifest records Accessibility separately from the seven website-derived categories.
- Added repeatable `@axe-core/playwright` coverage for the supported desktop route/state matrix at 1280px and 1440px, including both students, dialogs, Community, loading/error/unavailable states, reduced motion, forced colours and 200% text enlargement. The isolated Services suite also covers the Accessibility empty state at 375, 390, 430, 639, 640 and 768px. The durable engineering report is [CAP-35 Student Portal accessibility audit](audits/2026-09-09-accessibility-audit.md); this is not formal certification and does not expand the portal shell's mobile boundary.
- Remediation separates readable text colours from solid control surfaces, corrects muted/status/Services-table contrast, names hidden file inputs, restores Finance dialog focus, gives online indicators valid semantics, disables tab-indicator duration under reduced motion and keeps the fixed Services banner geometry under text enlargement. Warning IssueCards retain the approved amber surface with light copy as a documented product-owner contrast exception. The audit also found and fixed a Lecturer Review rapid-input state race that could discard earlier ratings.
- The live checklist's Student CAP-35 frontend status is **Demo** in the detailed sheet and **Frontend ready** in its simplified vocabulary; both were reviewed on **09/09/2026**. Mobile remains **Not started**, Backend **Needs checking**, and Applicant/Admin rows remain unchanged. Campus-provided Accessibility content and all request/case handling remain outstanding; CAP-51 owns future submissions, documents, tracking and staff responses.
- Final gate: `npm run check:all -- -- --workers=2` **exit 0**, **456 unit tests / 61 files (22.11s)** and **176 Chromium browser tests (7.0m)**. Token, Services-asset, lint, TypeScript, production build (**1.17s**), production-isolation and bundle checks pass with **no warnings**. `npm audit` reports zero known vulnerabilities. Exact log: `logs/cap-35-accessibility-final-check-all.log`. The final change-surface review reports no obvious companion-layer gaps (`logs/cap-35-accessibility-change-surface.log`), `git diff --check` passes, and all **109** checked local links in changed documentation resolve.
- Follow-up visual decision: warning IssueCards use white label/value copy and a white triangle on the unchanged amber `warning` surface. The focused card test (**5/5**) and production build pass. The focused axe matrix correctly reports this accepted WCAG 1.4.3 exception at **2.47:1** for small copy on Academic Modules and Profile warnings; it is documented in A35-03 and sits outside the earlier zero-violation gate claim.
- The existing recovery package remains preserved. CAP-33 and CAP-35 are included together in the 10 September `main` delivery.

### CAP-33 IT Helpdesk Community chat — 9 September 2026

- Community now includes a validated `service-it-helpdesk` entry for both current student scenarios. It uses the reusable headset `ServiceMark` identity and existing Community service-chat journey: empty state, compose, permitted attachments, send/error/delete behavior, same-session reopen and refresh/scenario reset.
- This is the approved CAP-33 student frontend **Demo**. Formal forms and case tracking belong to CAP-51. Ticket numbers, categories, priority, assignment, escalation, service-level reporting, persistence and staff operations are not implemented; Backend remains **Needs checking** and Mobile remains **Not started**.
- Contracts, fixtures, data-model registry/example, Design System Explorer, scope/sidebar/data/design documentation and API/browser coverage are synchronized. Focused verification: **58/58 unit tests** across the Portal API and data model; the complete `portal.spec.ts` desktop regression set passed **78/78 browser tests** at 1280/1440px, including the Helpdesk send, reopen and refresh-reset flow.
- Final gate: `npm run check:all -- -- --workers=2` **exit 0**, **455 unit tests / 61 files (24.34s)** and **164 Chromium browser tests (5.7m)** at 1280/1440px. Token, Services-asset, lint, TypeScript, production build (**1.36s**), production-isolation and bundle checks pass with **no warnings**. Exact log: `logs/cap-33-helpdesk-check-all.log`. The change-surface review reports no obvious companion-layer gaps, and `git diff --check` passes. CAP-33 is included in the 10 September `main` delivery.

### Restored portal delivery checkpoint — 8 September 2026

- This checkpoint delivers the restored-audit repairs, CAP-30 lecturer reviews, CAP-16 Announcements, the shared banner article panel, flattened Services content surface, and the final five-item Important Announcements overview. Their contracts, canonical projections, mock Portal API/query paths, route components, explorers, documentation, and regression coverage are included together on `codex/restored-portal-audit`.
- The preserved requirements, checklist planning records, audit evidence, and verified Excel backup are included as project records. The private recovery package remains unchanged at `D:\Coding\backups\cms-student-portal\20260907T130310Z`; `codex/student-portal-completion` remains separate and is not merged into this restored desktop prototype.
- Delivery gate: `cmd /d /c "npm run check:all -- -- --workers=2"` **exit 0**, **452 unit tests / 61 files (21.12s)** and **164 Chromium browser tests (5.4m)** at 1280/1440px. Token, Services-asset, lint, TypeScript, build (**1.18s**), production-isolation and bundle checks pass with **no warnings**. Exact log: `logs/announcement-important-flush-final-check-all.log`. The final change-surface review reports no obvious companion-layer gaps, all 29 checked current documentation links resolve, and `git diff --check` passes.

### Pinned Important Announcements — 8 September 2026

- The Announcement overview keeps an accessible `h1` but hides it visually, so **Important Announcements** is its only visible title. The carousel sits above and outside the separate table panel, matching the Dashboard section composition. Registration, examination preparation, Wi-Fi maintenance, student-record updates and campus transport guidance are the five pinned Cyberjaya fixtures; they retain the campus response's newest-first date/ID order. The section disappears when no notices are pinned, while Dashboard continues to show all ten.
- The complete table and its loading state use the existing flush panel mode. The panel supplies no content inset; `DataTable.frameClassName` removes the table's own outer border and rounding while preserving the raised header, cell insets, row dividers, overflow and keyboard row behavior. The final row omits its bottom divider. Retry and empty states remain padded.
- `ArticleCarousel` accepts a title, accessible region label and View all visibility while retaining its Dashboard defaults. The Important carousel has no redundant View all link; its cards, arrows, dragging and keyboard behavior reuse the existing implementation. A shared carousel skeleton reserves five cards outside the table panel during loading.
- Contracts, canonical fixtures, both explorer descriptions/samples, Announcement/design/data documentation and tests are synchronized. No endpoint, query key, route, detail layout, table content, editor, pin mutation or admin UI was added. Focused verification passes **49/49 unit tests (3.96s)** and **14/14 Announcement browser tests (23.1s)** at 1280/1440px. Reviewed open/closed Community captures are in `logs/announcements-visual/`.
- Final gate: `cmd /d /c "npm run check:all -- -- --workers=2"` **exit 0**, **452 unit tests / 61 files (21.12s)** and **164 Chromium browser tests (5.4m)** at 1280/1440px. Token, Services-asset, lint, TypeScript, build (**1.18s**), production-isolation and bundle checks pass with **no warnings**. Exact log: `logs/announcement-important-flush-final-check-all.log`. The change-surface review reports no obvious companion-layer gaps (`logs/announcement-important-flush-change-surface.log`), and `git diff --check` passes. Existing recovery work remains preserved.

### Announcement overview refinement — 8 September 2026

- This section records the earlier table-only intermediate state. **Pinned Important Announcements** above supersedes its overview padding/loading geometry while retaining the thumbnail table and reusable flush mode.
- The Announcement overview now uses a flush bordered table as its sole panel surface. Its ten rows each render a fixed 112px by 64px cover or faded-blue fallback thumbnail before the existing title/summary, plus a decorative right chevron. The row remains the single mouse/Enter/Space target; no nested action was introduced.
- `AnnouncementBanner` now owns page, card and thumbnail presentations. Thumbnail media uses existing local responsive assets, centred cover cropping, lazy loading, stable failure geometry and the same direct megaphone fallback. Dashboard cards remain 16:9 and detail banners remain flush at 288px.
- `AnnouncementsPageShell` has explicit `flush`, `padded` and `plain` modes. Populated and table-shaped loading states use flush mode; retry and empty states retain 16px padding; detail pages retain their shared plain article layout. Existing `TabbedPagePanel.contentInset="none"` is documented as an explicit option for table-only tab bodies; no existing consumer changed.
- The query-backed Design System Explorer example now includes cover and fallback overview rows. Announcement/design documentation and focused unit/browser coverage are synchronized. Contracts, canonical fixtures, Portal API methods, queries, routes, sorting and article content are unchanged.
- Final gate: `cmd /d /c "npm run check:all -- -- --workers=2"` **exit 0**, **450 unit tests / 61 files (22.23s)** and **164 Chromium browser tests (5.5m)** at 1280/1440px. Token, Services-asset, lint, TypeScript, build (**1.19s**), production-isolation and bundle checks pass with **no warnings**. Exact log: `logs/announcement-overview-complete-check-all.log`.
- Focused verification passed **25/25 Announcement/shared-panel unit tests** and **12/12 Announcement browser checks** before the final full gate. The first full browser attempt exposed one overpacked Announcement test that repeated all ten articles in each of three Community states, plus two unrelated load-contention failures; the test was split into a three-state overview check and a single all-ten article check without increasing timeouts or suppressing coverage. All **32/32** affected Announcement/audit checks then passed together, followed by the complete passing gate.
- Reviewed captures in `logs/announcements-visual/` confirm the flush table, cover/fallback rows, long-title wrapping and contained overflow at 1280px with Community open and 1440px with Community closed. The change-surface review reports no obvious companion-layer gaps (`logs/announcement-overview-change-surface.log`), and `git diff --check` passes. Existing recovery and uncommitted work remain preserved; no commit or push was requested.

### Shared banner article layout — 8 September 2026

- Added the shared `PagePanelLayout` system component with required navigation, a navigation divider, an optional flush banner, and `standard`, `wide`, or `none` content insets. `TabbedPagePanel` now composes this frame while retaining its existing 16px default content padding, tab selection, scrolling, history, and focus behavior. Profile and Finance remain on that unchanged default.
- Announcement details now render the breadcrumb first, followed by a full-width 288px banner and 60px content inset. The title and department/publication metadata sit below the banner, followed by a divider and the left-aligned 80-character article column. Image-backed, faded-blue no-image, failed-image, and loading banners all reserve the same geometry; dashboard cards retain their separate 16:9 banner presentation.
- Services now uses the same panel frame with `none` content inset. The former negative-margin wrapper was removed, while the Services renderer continues to own its established 60px desktop body inset and responsive container behavior. Existing banners, backgrounds, tabs, routes, image loading, and content structures are unchanged.
- Follow-up refinement: the Services body now inherits the shared panel surface directly. The redundant raised-gradient, rounded body wrapper below the banner was removed, eliminating the double-container appearance while preserving body insets, dividers, cards, tables, media, and responsive layouts.
- Follow-up verification: focused Services units **22/22 (4.26s)**, production build **passed (1.32s)**, and the seven-category desktop Services browser suite **18/18 (44.4s)** at 1280/1440px. The browser suite now asserts the flattened gap-free body root while preserving the banner, tabs, history, retry/error, campus isolation, responsive body inset, and overflow coverage. No warnings. Log: `logs/services-flattened-body-browser.log`.
- The Design System Explorer includes the shared breadcrumb/banner/wide-content example. Announcement, Services, and design-system documentation describe the current component ownership and geometry. No routes, contracts, fixtures, Portal API methods, query behavior, or announcement content changed.
- Verification passed: focused shared/Announcement/Services units **53/53**; focused Announcement browser checks **12/12**; combined Announcement/Services browser checks **30/30**. Visual captures at 1280px with Community open and 1440px with Community closed were reviewed in `logs/announcements-visual/`; representative Services, Profile, and Finance layouts were rechecked.
- Final gate: `cmd /d /c "npm run check:all -- -- --workers=2"` **exit 0**, **448 unit tests / 61 files (22.30s)** and **162 Chromium browser tests (5.7m)** at 1280/1440px. Token, Services-asset, lint, TypeScript, build, production-isolation, and bundle checks pass with **no warnings**. Exact log: `logs/page-panel-layout-complete-check-all.log`. The change-surface review reports no obvious companion-layer gaps (`logs/page-panel-layout-change-surface.log`), and `git diff --check` passes.
- Existing recovery, restored-audit, CAP-30, and CAP-16 work remains preserved and uncommitted on `codex/restored-portal-audit`. No commit or push was requested.

### CAP-16 student announcements — 8 September 2026

- Follow-up decision: published articles without an image use the shared `AnnouncementBanner` state in both dashboard cards and details. It reserves a 16:9 faded-blue surface with one centred megaphone and meaningful accessible label; this replaces the earlier no-banner layout without creating or importing placeholder photography. Image-backed notices retain their existing media behavior. Follow-up verification passed: **15 focused announcement unit tests (3.61s)**, production build (1.19s), and **10 announcement browser tests (22.4s)** at 1280/1440px; `git diff --check` also passes. The refined desktop capture is in `logs/announcements-visual/desktop-1440-closed-registration.png`.
- Completed the approved student viewing slice on `codex/restored-portal-audit`, preserving the recovery package and all earlier uncommitted audit/CAP-30 work. Announcements sits after Dashboard; the bordered list opens `/announcements/:announcementId` inside the same page shell. Dashboard cards and View all navigate to that same canonical content. No read/unread tracking or mutations were added.
- Ten synthetic Cyberjaya articles include five covers and five no-cover layouts, with three inline-image articles (including a cover-free one). The reusable feature header/body/media render escaped structured rich text, headings, lists, quotes, tables and safe links with Rubik/semantic portal styles. Existing local Services images are reused with inspected alt text; originals and delivery assets are unchanged. See [Announcements](announcements.md) for fixture assumptions and the page-reference matrix.
- Added campus-owned graph records, validated list/detail contracts, `PortalApi.getAnnouncements()` / `getAnnouncement()`, session-scoped queries, canonical dashboard projections, relationships and both explorer examples. Missing enrolments/campus content never fall back to Cyberjaya. Shared media validation was extracted without changing the Services interface. Carousel link dragging opts into an eight-pixel threshold with click suppression; other consumers retain defaults. Standalone navigation now exposes `aria-current` consistently.
- Final full gate: `cmd /d /c "npm run check:all -- -- --workers=2"` **exit 0**, **442 unit tests /60 files (21.04s)** and **160 Chromium browser tests (5.6m)** at 1280/1440px. Token/Services-asset/lint/TypeScript/build/production-isolation/bundle checks all pass, **no warnings**. Exact log: `logs/announcements-complete-check-all.log`. The 14 announcement unit cases and 10 announcement browser cases are included. Earlier focused announcement/data-model run: **36 passed**; initial browser run: **8 passed**, before adding the two state-flow cases.
- Browser acceptance covers all ten articles in Community open/closed/resized states, local image loads, both cover variants, inline images, tables, long text, failed media, keyboard table/card access, drag versus click, breadcrumbs, direct/unavailable URLs, history, scroll reset and loading/retry/empty states. Reviewed desktop screenshots in `logs/announcements-visual/`. Existing Finance, Profile, Academic, lecturer reviews and isolated responsive Services checks pass. No mobile-shell acceptance is claimed.
- Change-surface review: no obvious companion-layer gaps (`logs/announcements-change-surface.log`). `git diff --check` passes; all 80 local links checked in the affected feature/planning/design/scope documents resolve. Intermediate verification corrected a refined-schema composition error, an overly structural fixture assertion after removing a duplicate heading, and two lint warnings; the final gate was rerun without suppressing checks or increasing timeouts.
- CAP-16 Student frontend evidence is **Demo**; Backend remains **Needs checking**, Mobile **Not started**. Admin publishing/editor integration, audience rules, applicant notices, notifications, attachments, read acknowledgements and persistence remain outside scope. The live Google Sheet is unchanged. All work remains uncommitted; no commit or push requested. Next approved work item: none.

### CAP-30 lecturer reviews — 8 September 2026

- Implemented the approved lecturer-review slice on `codex/restored-portal-audit`, preserving the original recovery package and all prior uncommitted audit repairs. This is CAP-30; CAP-28 marks/lecturer feedback is unchanged. The live checklist has not been edited.
- Review opens a nested Academic/Modules page with the supplied six-section, 15-rating questionnaire, optional feedback, shared confirmation Popup, and immutable submitted readback. View Review replaces Review after success. Current registered modules permit new submissions; historical reviews remain viewable. Drafts and submissions are session-only.
- Added canonical campus questionnaires/review records, validation, Portal API methods, generation-scoped queries/mutation invalidation, registry relationships/samples and Design System examples. Canonical and legacy lecturer aliases resolve to the same assigned staff member; campus, student, semester and repeat ownership are checked at submission. Popup supports optional explicit focus restoration and blocking dismissal while a confirmed request is in flight; existing consumers retain defaults.
- Verification complete: `cmd /d /c "npm run check:all -- -- --workers=2"` **exit 0**, **428 unit tests /59 files (22.87s)** and **150 Chromium browser tests (5.6m)** at 1280/1440px. Token/Services-asset/lint/TypeScript/build/production-isolation/bundle checks pass with no warnings. Exact log: `logs/lecturer-review-final-check-all.log`. Focused lecturer-review/data-model tests: **39 passed** (`logs/lecturer-review-final-focused.log`). No timeouts increased or checks suppressed. The old no-op Review navigation assertion was replaced with the approved route-and-return behavior.
- Change-surface review reports no companion-layer gaps (`logs/lecturer-review-change-surface.log`); `git diff --check` passes. All **90** checked local documentation links resolve. Implementation and companion records are complete; no unresolved feature defects remain. Shared Popup consumers and existing Finance/Profile/academic flows pass the full browser gate.
- Reviewed desktop screenshots for open/closed Community, long text, failed avatars and submitted readback. Browser checks also exercise actual keyboard resizing, radio navigation, confirmation focus, draft restoration, history, scroll reset and refresh reset. Portal screenshots are captured with animations disabled for stable review in `logs/lecturer-review-visual/`.
- Backend remains Needs checking: durable storage, server authorization, confidentiality/reporting policy and broader course surveys are outside this student frontend slice. No commit or push requested for this work.

### Restored audit repairs — 8 September 2026

- User approved fixing all four audit findings on `codex/restored-portal-audit`. The original private backup and `codex/student-portal-completion` remain preserved; this does not resume the larger frontend-completion scope.
- Timetable now retains minutes and expands for early/evening classes. Scenario/session generation and the shared mutation guard reject stale saves before cache updates. Materials uses canonical weeks/resources/assessment IDs and actual learning progress, with assignment transactions synchronized into the session graph. Module Attendance resolves the selected enrolment's campus and programme settings, including the new-student path.
- Added normal-gate regression tests for the findings and related boundaries, plus a two-width browser test for exact class placement, keyboard navigation and history. Both explorers and relevant architecture/feature/design documents are synchronized. The prior audit report remains historical evidence and has a repair-status note.
- Verification complete: `cmd /d /c "npm run check:all -- -- --workers=2"` **exit 0**, **411 units /58 files (41.06s)** and **142 Chromium tests (5.2m)**. Token/asset/lint/TypeScript/build/production-isolation/bundle checks passed. Log: `logs/audit-fixes-complete-check-all.log`. No timeout increases or test suppression. The initial repair gate caught a registration-test fixture leaving orphaned learning records and a malformed-timezone formatting exception; both were corrected and the full gate rerun successfully.
- After the full gate, a small CSS-only short-class inset adjustment was verified with a fresh production build, production-isolation/bundle checks and **4/4 focused browser tests (15.5s)** at 1280/1440px. These assert visible label space, exact time columns, focus visibility, keyboard/history navigation and legacy-link isolation. Screenshots in `logs/audit-fixes-visual/` were reviewed. Logs: `logs/audit-fixes-final-build.log`, `logs/audit-fixes-final-browser.log`.
- Change-surface review reports no companion-layer gaps; `git diff --check` passes. All 39 checked local documentation links resolve; all 10 saved planning/requirement/output/scope files retain their backup checksums. No unresolved repair warnings. Original scope limitations remain: desktop prototype, two Cyberjaya scenarios, mock/session-only behavior and documented placeholders. All four findings are fixed; no further feature work is implied. Changes remain uncommitted; no push performed.

### Saved portal restored and audited — 8 September 2026

- At the user's request, restored the original saved working state onto `codex/restored-portal-audit`, based on `6811e886fce1e86c366bd92d66cb74ba5ce612d2`. All six recovery-package artifacts and 466 restored files were checksum-verified. Private backup `D:\Coding\backups\cms-student-portal\20260907T130310Z` remains unchanged; its `RESTORE.md` contains independent restoration instructions. Saved planning, requirements and output files were preserved.
- Newer work remains on `codex/student-portal-completion` at `9d492643d68a6adc026b2ea88e79627696ea7837` and its existing remote branch. No destructive reset or force push. The active preview is the restored desktop prototype, not the eight-campus/mobile completion. The rollback supersedes that implementation task; do not resume it automatically.
- [Audit report](audits/2026-09-08-restored-portal-audit.md) records four confirmed P2 issues: timetable minute/evening placement, stale Resume save after a session reset, Materials/canonical learning inconsistencies, and positional campus configuration in module attendance. Six isolated correctness assertions reproduce them; application code remains unchanged and no fixes were applied.
- Final `npm run check:all` with two browser workers **exit 1**: static/asset/lint/build/isolation/bundle checks and **398 units /57 files** passed; **137/138 Chromium tests passed (5.3m)**. The remaining assignment-navigation timeout has trace evidence of React Query failing to load with `net::ERR_NO_BUFFER_SPACE`. Unchanged focused rerun passes **2/2 (15.5s)**. An earlier chart timeout also passed its unchanged focused rerun **7/7**. Do not report a clean full-gate pass.
- Fresh 1280/1440px browser probes reproduce the timetable issues and show no document overflow on the tested desktop views. A 375px observation confirms full mobile support is absent in the restored shell. Detailed logs, commands, limitations and recovery provenance are in the report. Audit additions are separate from the original tests. No commit/push for this request; await direction on repairs.

### Student checklist frontend reconfirmation — 7 September 2026

- Read the user's latest **Daily Checklist (Simplified), rows 11–44**, and reviewed all 34 Student entries against application commit `6811e88`, source/contracts/mock services, the running portal and tests. [Row-by-row findings](planning/student-frontend-reconfirmation.md): 31 Frontend labels can stay within the prototype scope; recommend **Frontend ready → Partial** for row 11 profile/personal documents, row 23 study guides/lecture notes/assignment briefs, and row 29 self-paced learning. Missing document/resource interactions and learning completion UI are frontend gaps, distinct from pending backend integration.
- No live Sheet values, statuses, estimates, notes, filtering or sharing were changed. Final read confirms all 34 rows unchanged and zero cell notes. Backend remains Needs checking; Mobile remains Not started except the two Partial entries for the shell/device foundation and Services. The previous 19 combined notes were removed at the user's request. Existing Excel/baseline snapshots predate these latest edits and are historical, not refreshed current exports.
- `npm run check:all` **exit 1**: static/asset/lint checks, **398 unit tests / 57 files**, build, production isolation and bundle checks pass; browser suite **136 passed, 2 timed out (3.8m)**. Both timeouts are the 30-second registration scenario at 1280/1440px. Focused repeat using two workers and the unchanged timeout passes **2/2 (15.2s)** without code changes. Full-suite timing remains a warning; do not describe the full gate as passed. Logs: `logs/checklist-frontend-audit-check-all.log` and `logs/checklist-registration-recheck.log`.
- Planning notes and review updated; no application changes, commit or push. Next action: decide on the three suggested labels or explicitly narrow their feature scope. No feature implementation is authorised by this review.

### Simplified checklist consolidation — 7 September 2026

- Reviewed the user's current **Daily Checklist (Simplified)** one row at a time and reduced **112 rows to 82 delivery groups through 19 merges**. Counts: Applicant 6, Student 34, Lecturer 8, Admin / Staff 29, Shared System 5. All 55 underlying CAP IDs and nine campus requests remain covered; original scope/status/effort/next actions are retained in 19 Feature-cell notes and the hidden ID column. [Row-by-row review](planning/simplified-checklist-review.md) records all 112 decisions.
- Preserved user-entered **Frontend ready**, Done and effort values; these are frontend planning assertions, not newly verified live/integrated capability. Only rows sharing portal, department, all three statuses, milestone and owner were combined. Revision work/past papers is 3 Days from the user's existing 2 + 1; no efficiency saving was invented. Distinct workflows/statuses remain separate.
- Only the simplified tab changed. API comparisons confirm the detailed Daily Checklist, Timeline and Sources values/formats/notes are unchanged. All four tabs remain; the simplified table is `A4:M86`. User-hidden columns, widths, chip colours and four frozen header rows are preserved. Live browser checks covered wrapping, merge notes, effort values and Lecturer filtering (8/82, then reset).
- Verification: **1,118 simplified live cells and all 19 notes match**; four-tab Excel export passes **4,476 cells**, all notes, four tables, six dropdown columns, hidden-column configuration, no hidden rows and no formula/error cells. The refreshed backup is **54,573 bytes**, SHA-256 `9593e97e9ff749b8ac1940e51c497e6f6342dc9395e801307e255b51abbd9bac`. Export's five list-validation rules cover six dropdown columns; the initial verifier's rule-count assumption was corrected after inspecting the export. Source hashes remain unchanged.
- Planning notes, baseline and review are synchronized. The baseline keeps earlier code evidence separate from the new user-edited simplified snapshot. The user had already enabled anyone-with-link reader access; this was observed before editing and preserved, with no permission changes or messages sent. The earlier owner-only claims below are historical.
- Planning-only: no application changes/tests, commit or push. Final checks passed: 28 local Markdown links resolve, `git diff --check` passes and the read-only change-surface review reports no companion-layer gaps. Next action: maintain the simplified view; split a group if its subfeatures later need different statuses, owners or release milestones. The simplified/detailed tabs are not automatically synchronized.

### Daily checklist reorganised by portal — 7 September 2026

- Updated the [existing Google Sheet](https://docs.google.com/spreadsheets/d/1Yux9R8hIgcPrQ5OklKd9Oyq_04qJtaAhcxJvr_7NvYI/edit) in place after two live-read checks. It now has **112 delivery items across five portals**, linked to the same **55 capabilities**, all nine campus requests, 37 research domains, eight countries and **150 supporting records**. Groups: Applicant 8, Student 41, Lecturer 19, Admin / Staff 38, Shared System 6. Repeated CAP IDs link portal activities; they are not extra requirements.
- The 13-column daily view separates Portal, lead Department, Frontend, Mobile and Backend. Approved departments are Registry, Bursary, Finance, Quality Assurance, Faculty and Marketing; unclear ownership stays TBC. All 41 Student statuses/evidence are preserved; new lecturer/staff screens do not inherit Demo. Backend has 110 Needs checking entries; two non-software controls use Not applicable software statuses with explicit outstanding TBC operational checks. No Live status is assigned.
- Replaced phase codes and assumed week ranges with five named delivery milestones. The sequence follows admissions-first and integration dependencies, not government phasing. All unagreed dates, target weeks, named owners and backend commitments remain TBC. Required staff functions are assigned to the launch they support; check the old CMS before building replacements. [Planning notes](planning/daily-checklist.md), [baseline](planning/daily-checklist-baseline.json) and project scope are synchronized.
- Native status dropdown chips use all nine agreed colours in each of Frontend/Mobile/Backend. Other dropdowns and table cells stay neutral; zero conditional-format rules. Three native filter tables remain, with Daily identifiers frozen through Feature. Signed-in browser checks covered all three palettes, neutral cells, wrapping, horizontal scrolling and frozen identifiers. Lecturer filter showed 19/112 and was reset. Sources header contrast was corrected and rechecked.
- Exact data checks: **3,397/3,397 live cells and exported Excel cells match**; 262 typed dates; six dropdown columns/options; all 55 capabilities/112 work-item identities and source/request mappings reconcile. Four supplied files remain unchanged and every evidence path resolves. [Refreshed Excel backup](../outputs/01a06f1b-fd81-7131-8e1a-f7845e088984/student-portal-cms-daily-checklist.xlsx): **41,701 bytes**, SHA-256 `7e671934118a2ffa9fefcdb194520c606ffecf085536ec7879749a8ce7a21cfe`. Export has three tables, six validation rules, preserved frozen panes, no hidden rows, no formula/error cells. Google-native chip rendering is verified in Google; Excel is a data/structure snapshot.
- Sharing remains owner-only, `shared: false`; no permissions changed and no messages sent. Government-source limitations retain their earlier dated research; this update did not claim new legal verification. Old-CMS capabilities, unclear departmental ownership and dates still require confirmation.
- Planning artifacts only: no application changes, application tests, commit or push for this task. Final checks passed: all 26 local Markdown links resolve, `git diff --check` passes and the read-only change-surface review reports no companion-layer gaps. Initial CRLF normalization warnings were resolved by saving the edited tracked documents with LF endings. Next action: maintain the live sheet, assign owners, inspect old-CMS support and agree targets. Never overwrite later live edits by regenerating the dated baseline.

### Services heading cleanup — 7 September 2026

- Removed the campus label and its loading placeholder from the Services shell. The Services heading now leads directly into the panel with the existing 16px gap. Campus ownership, API data and navigation remain unchanged.
- Verification: final full gate passed **57 unit files / 398 unit tests and 138 browser tests (3.9m)** using `cmd /d /c "npm run check:all -- -- --workers=4"`. Token/asset checks, lint, TypeScript/production build, production dev-code exclusion and bundle budgets passed without build warnings. `git diff --check` and the change-surface review passed with no companion-layer gaps.
- Verification history: the initial focused 1280px capture timed out waiting for screenshot stability; its unchanged rerun passed. The first full gate found two outdated empty-campus label assertions (one per width); these now assert that the label is absent while preserving empty-state/campus-isolation coverage. The complete final rerun passed. Services behavior docs and the source-layout adaptation record are synchronized. Work is complete and approved for commit/push; no further feature work is approved.

### Services cleanup and image delivery — 6 September 2026

- Cyberjaya now has seven Services categories. Services Finance and all department blocks are removed; `/services/finance` uses the existing first-category replacement redirect. Student Finance and standalone Accommodation remain unchanged. The optional campus contact API record is retained with `image: null`.
- Scrollable `TabbedPagePanel` tabs use the shared hidden-scrollbar utility, retaining pointer/keyboard scrolling and active/focused-tab reveal. Shopping now uses the campus-owned light gradient, matching Transportation. Existing banners, compact copy and body arrangements remain intact.
- All 36 remaining source images were inspected and processed with the built-in AI editor. Fifteen results were retried: 24 restorations were accepted and 12 retain optimized originals because retries changed lettering or important details. Per-image attempts, decisions and provenance are in `assets/services/restoration-review.json` and the source manifest. All 43 historical originals and the 24 accepted masters are preserved outside `public`.
- `npm run optimize:services-assets` reproducibly emits 72 WebP variants: photos/banners at 1600/800 long edge (250/100 KB maximum), dishes at 640/320 (60/25 KB maximum). Delivery totals 6,118,820 bytes. Asset checks enforce dimensions, aspect ratios, byte budgets, checksums, provenance and no orphan public assets. Retired Finance/contact images are excluded from delivery.
- Optional validated image variants pass through the existing campus-owned read models. Shared `ResponsiveMedia` measures rendered width, including Community resizing, before supplying `srcset`/`sizes`; banners remain eager and body media lazy. Registry and query-backed developer examples are synchronized, using Dining instead of retired Finance.
- Services content remains the only supported mobile surface (375/390/430/639/640/768px); the portal shell is desktop-first. Historical source inventories retain all eight imported pages for provenance; current adaptations document the seven-category cleanup. Source rates/content remain an unconfirmed website snapshot, and original-fallback photos retain some source softness.
- Verification: final full gate passed **57 unit files / 398 unit tests and 138 browser tests (4.0m)**. Exact PowerShell command: `cmd /d /c "npm run check:all -- -- --workers=4"`; Playwright confirmed four workers. Token/asset validation, lint, TypeScript/production build, production dev-code exclusion and bundle budgets all passed with no build warnings. `git diff --check` and change-surface review passed. Coverage includes all seven pages at 1280/1440 with Community open/closed/resized, isolated 375/390/430/639/640/768px content, pointer/keyboard scrolling, image failures and responsive selection, campus isolation and existing student Finance/Profile flows.
- Image verification: all 36 originals and AI results were visually compared; compressed small variants and desktop banner crops were reviewed. Re-running the optimizer reproduced the entire manifest and every output checksum. The optimizer skips byte-identical writes after a Windows file-lock failure on an unnecessary source rewrite. Original fallbacks and historical source limitations remain documented above.
- Verification history: focused units passed 24 tests; an initial focused browser run passed 35/36 because its old scrolling assertion assumed eight tabs. The test now narrows content through Community and verifies pointer and keyboard scrolling. An earlier 10-worker full run, during final source normalization, had seven browser failures (timeouts and scenario-route assertions). The final clean four-worker run passed all 138 without increasing timeouts or weakening assertions. Banner captures now wait for responsive-image decode after resizing.
- Change-surface review: contracts, fixtures, registry/explorers, shared/feature UI, source assets, documentation and tests are synchronized. Existing API projections, graph ownership, query keys and dynamic navigation already derive the revised content; no endpoint or relationship changes are needed. AGENTS scope and mandatory workflow are unchanged. The cleanup is complete and verified; commit and push it. No further feature work is approved.

- Frontend-only, desktop-first, dark-only student portal. It uses a replaceable, student-scoped `PortalApi` with a session-only `MockPortalApi`; it has no live backend, authentication, physical database, or browser persistence.
- Read [project scope](project-scope.md), [data model](data-model.md), [design system](design-system.md), [sidebar behaviour](sidebar-behavior.md), [calendar behaviour](calendar.md), [Academic Performance](academic-performance.md), [Academic Modules](academic-modules.md), [Academic Timetable](academic-timetable.md), [Academic Study Plan](academic-study-plan.md), [Personal Profile](personal-profile.md), [Profile Resume](resume-profile.md), [Profile Portfolio](portfolio-profile.md), and [Profile Documents](profile-documents.md) before changing the related area.
- The project now uses Git and a private GitHub remote as its cross-machine source of truth. Use Node 22, as pinned in `.nvmrc`.

## Architecture and current decisions

- `components/ui` contains generic primitives; `components/system` contains shared portal patterns; `components/features/<feature>` contains feature-owned dashboard UI; `components/portal-sidebar` owns the cohesive navigation and community/chat surface; `app` owns app-level route composition.
- Components use direct shared design tokens. Retired colour aliases, including `primary`, `sidebar`, `card`, `popover`, `secondary`, `accent`, `hover`, `unread`, and `foreground`, are blocked by `npm run check:tokens`.
- The sidebar’s scroll-edge gradients use `surface`; do not restore an alias for them.
- Data changes must be synchronized across Zod contracts, the registry, mocks, `PortalApi`/queries, explorer samples, docs, and tests. Visual shared-rule changes must also update the Design System Explorer and its documentation.

## Last verified state — 2026-09-01

- Sidebar scroll fades were restored with the shared `surface` token and have regression coverage.
- Component folders were reorganized into system, feature, sidebar, and app layers without changing behavior.
- The active x-axis semester label in each performance chart now uses the shared high-emphasis `title` text colour; unit and desktop browser coverage verify the initial and newly selected semester states.
- Global `type-label` and `type-label-normal` utilities now use regular font weight (400), verified in the Design System Explorer at both desktop coverage widths.
- Performance charts now load within 400px of the viewport behind a layout-matched skeleton. Chat keeps a deferred entry that is warmed on browser idle or interaction intent, while active conversation details remain separately queried; stable React/router chunks keep the production build below Vite's per-chunk warning threshold.
- The dashboard now ends with a read-only active-semester Calendar. Its DayPicker-based month grid, monthly Portal API query, selected-day agenda, recurrence-aware mock events, and code/data loading are deferred until the section approaches the viewport. Calendar remains dashboard-only; no Academic route or event mutation was added.
- Calendar day cards are compact and centre their body-style date labels and event markers horizontally, with date labels aligned to the top. The card has four simple regions: month selector, grid, plain event legend, and selected-day details. The month label uses the high-emphasis `title` colour. The outer surface has no content padding; the compact selector and grid own 8px insets, while the legend and details retain their roomier 12px horizontal inset, with no negative-margin full-bleed layout. Details use 20px top padding and 16px bottom padding. The section heading has no redundant semester-range caption.
- Calendar days use the translucent `surface-raised` treatment from empty timetable periods. In-semester date labels use the high-emphasis `title` colour; disabled boundary labels are muted. The current date uses a faded brand background and brand text rather than a blue outline; that faded brand fill stays when today is also selected. Hovering today uses a brighter faded brand fill. Other selected days continue to use `surface-hover` at rest and on hover.
- Calendar day cells use equal visible 8px horizontal and vertical gutters. Out-of-semester dates remain disabled but keep visible translucent raised cards with muted date text.
- The calendar legend is a centred, non-interactive key without per-item pill containers; every event kind remains visible.
- Calendar agenda cards use 12px padding and omit descriptions; they show kind, an optional separate-line module code, title, then location and clock-icon time together in one metadata row. Upcoming Meetups uses the same 12-hour `en-MY` campus clock as the Calendar agenda.
- Group-chat identity cards show at most six overlapping avatars before the contained `+N` count badge, matching Community meetup cards. Counts above 99 display as `99+`, with the exact count retained in the accessible label.
- Personal chat-header `View profile` remains a disabled frontend placeholder and does not navigate to the profile route.
- The sticky top-menu avatar is a direct Profile > Personal action. Navigation's bottom `Log out` control is the sole visible frontend-only logout placeholder; no right-side profile panel, overlay, deferred profile chunk, or panel-specific Sheet extension remains.
- Active `StudentAccountIssue` records add the solid destructive `Action required` button immediately to the right of the profile picture, with the shared filled warning triangle. Selecting it opens Profile > Personal at the existing `/profile/personal-info` URL. The Profile submenu is Personal, Academic, Resume, and Portfolio; internal route identities keep Profile > Academic distinct from the main Academic group.
- The Action required placement refinement passed `npm run build` and its focused desktop browser flow at 1280px and 1440px.
- Profile > Personal is a completed frontend route inside the shared `TabbedPagePanel`; Profile > Academic provides the read-only qualification record described below, Profile > Resume provides the session-editable Phase 1 builder, and Profile > Portfolio provides the session-editable Phase 1 project builder. The page is lazy-loaded so its feature code does not enter the eager application chunk.
- Personal displays the current Student and Programme, detailed active account issues, locked identity/passport values, session-editable contact and address details, and one-or-more emergency contacts. `DetailField` supplies shared read-only and editable row variants; its examples and Profile-style start-icon issue card are in the Design System Explorer.
- Profile controls now use 14px body typography for the programme and field labels. Text inputs and the selector share a 36px raised-surface treatment with immediate neutral hover and no blue focus ring. `ProfileFormActions` and `AddEmergencyContactButton` compose the page actions; brand actions keep transparent borders for height parity, while Cancel uses the bordered secondary variant.
- The Profile identity strip uses a 14px medium-weight (500) title-colour faculty line above the muted programme line; the focused Personal Profile browser flow and production build passed after this refinement.
- The Profile identity strip now has a shared-border divider between the student name and academic summary, with a 4px faculty-to-programme gap; its focused desktop browser flow and production build passed.
- The Profile identity strip explicitly spans the full available tab-content width; focused desktop browser coverage verifies its width at 1280px and 1440px.
- The Profile identity strip's academic text column now flexes across all remaining space beside the avatar, so its divider and summary no longer stop at the text's intrinsic width; focused desktop coverage and the production build passed.
- Read-only DetailField rows top-align their value with a wrapped label, while editable rows retain vertically centred controls; the focused Personal Profile flow passed at both desktop widths after this refinement.
- Emergency-contact trash actions now use the neutral muted-to-title hover treatment rather than destructive red; the focused Personal Profile browser flow and production build passed.
- The Design System Explorer has one Forms group for text inputs, the dropdown select, shared DetailField examples, and the reusable Add emergency contact row; overlays and feedback are separate.
- Profile's General, address, and emergency-contact groups now use the reusable `DetailSectionCard`: transparent, 8px-radius, bordered groups with 12px title/content regions separated by a shared divider. The Forms catalogue includes the component.
- The DetailSectionCard production build and focused Personal Profile plus Design System desktop flows passed at 1280px and 1440px.
- Passport Photo and Scanned Passport now use the reusable underlined brand `FileLink` filename control without an icon. It is a visible frontend-only placeholder: clicks are intentionally no-ops until an authorised file URL is part of the Portal API contract.
- Profile issue cards now use the same immediate darker semantic hover treatment and pointer cursor as the Action required control. The shared `DetailField` label column is 160px, keeping longer labels readable before the value/control column.
- The issue-card/detail-field visual refinement passed `npm run build` and the focused Personal Profile Playwright flow at both 1280px and 1440px.
- The Add emergency contact row now explicitly overrides generic outline styling for a transparent 36px field-height row, regular title-colour text, and white dashed hover border; its focused desktop checks and production build passed.
- The Add emergency contact row hover was refined to use `surface-hover` with a muted dashed border instead of bright white; focused desktop checks and the production build passed.
- The mock Personal update boundary accepts only contact number, marital status, email, both addresses, and emergency contacts. It validates with Zod, assigns opaque IDs to new contacts, updates the TanStack Query cache, survives in-session navigation, and resets on refresh. The final emergency contact cannot be removed. No persistence, uploads, downloads, account resolution, or backend authorization was added.
- The `Action required` control retains its position while pressed and uses a pointer cursor.
- The account-attention production build and focused browser test passed at 1280px and 1440px.
- The Design System Explorer shows clear and attention variants of the real `StudentProfile` control. The Data Model Explorer now includes StudentPersonalProfile and EmergencyContact with their relationships, and StudentAccountIssue includes the nullable amount, currency, and deadline values required by Profile.
- Timetable hourly labels now start with a compact 1px by 16px vertical marker using `muted-foreground` at 40% opacity.
- The timetable-marker and stable-gutter production builds and focused dashboard browser tests passed at 1280px and 1440px.
- The main portal scroller reserves a stable scrollbar gutter, preventing page-width changes when moving between longer and shorter routes.
- Hovering a Messages directory card changes only its neutral surface; the person-name label retains its body colour.
- Academic Performance is now a completed frontend route. Its reusable `TabbedPagePanel` keeps the Academic tabs and 16px content region inside one divided surface; route changes stay synchronized with the sidebar and breadcrumb.
- The Academic heading and tabbed panel have a 32px gap. `TabbedPagePanel` now has clipped 12px corners, 4px tab gaps, regular-weight labels, and a neutral active indicator that slides between variable-width tabs.
- The global elevation scale now includes `surface-subtle` between `background` and `surface`. It is the direct quiet-inset tier for performance charts and empty states; grade rows, Calendar agenda/details rows, and attendance summary cards use `surface-raised`. Its swatch appears in the Design System Explorer.
- Academic Performance and Academic Modules use the shared `SemesterSelector`: a direct semester dropdown field with separate previous and next buttons at either edge. Main Academic selectors expose all six semesters; future Semesters 5–6 are muted but selectable and show page-specific empty states. Performance still limits its GPA breakdown popup to published results.
- Academic Modules uses the shared selector, defaults to the active Semester 4, and disables navigation at Semesters 1 and 6. Its list API response provides dated semesters and semester-linked enrolments together with the module, lecturer, assignment, and issue records needed to reuse `ModuleCard`. Semesters 1–4 show registered modules; Semesters 5–6 show a clear no-modules state.
- `StudentModuleResult` and the dedicated academic-performance Portal API response provide grade, explicit pass/fail outcome, points, credits attempted, credits earned, and final points without frontend grade interpretation. The Data Model and Design System explorers include the contract and reusable controls.
- The Academic Performance page was visually reviewed at 1440px and its route, selector, chart, grade, empty, tab, and explorer flows pass at both desktop coverage widths.
- Performance-chart tooltip position changes are not animated, preventing pointer-follow lag or jitter.
- Recharts’ duplicate SVG focus layer is disabled, preventing a browser focus border after clicking the chart while preserving the labelled chart container and semester controls.
- The performance focus-outline production build and focused browser test passed at 1280px and 1440px.
- The performance-tooltip production build and focused browser test passed at 1280px and 1440px.
- Selecting the university logo in the navigation sidebar returns the portal to Dashboard.
- Calendar month navigation and News carousel navigation now share the same `ArrowNavigationButton`, keeping the 28px hit area plus icon, hover, focus, and disabled styling consistent.
- Community resizing previews through CSS custom properties once per animation frame and commits state once at pointer end. Dashboard, navigation, and community render boundaries stay isolated during the 200ms sidebar transitions; reduced-motion mode removes the duration.
- `npm run check:all` passed: token audit, lint, 54 unit tests, build, production and bundle-budget checks, and 28 desktop Playwright runs at 1280px and 1440px. The largest JavaScript chunk is 340,745 bytes, eager JavaScript is 187,255 gzip bytes, the deferred performance chunk is 100,127 gzip bytes, and the deferred Calendar chunk is 23,172 gzip bytes.
- After the latest Academic visual refinements, `npm run check` passed: direct-token audit, lint, 54 unit tests, build, production check, and bundle budgets. All 28 desktop Playwright runs also passed at 1280px and 1440px (run in focused batches because the terminal stops a single long-running command at 30 seconds). The largest JavaScript chunk is 340,753 bytes, eager JavaScript is 187,521 gzip bytes, the deferred performance chunk is 100,131 gzip bytes, and the deferred Calendar chunk is 23,174 gzip bytes.
- Verification is now proportionate: small isolated visual/CSS changes use a production build plus focused coverage; `npm run check:all` remains mandatory for data/API/contract work, interaction or user-flow changes, shared-component refactors, substantial visual work, and every commit or push.
- The Personal Profile implementation passes `npm run check:all`: direct-token audit, lint, 61 unit tests, production build and checks, bundle budgets, and 30 desktop Playwright runs across 1280px and 1440px. The latest shared-control refinement keeps the largest JavaScript chunk at 340,736 bytes, eager JavaScript at 189,419 gzip bytes, the deferred Performance chunk at 100,129 gzip bytes, the deferred Calendar chunk at 23,173 gzip bytes, and the deferred Profile chunk at about 3.94 kB gzip.
- The Academic Modules implementation passes `npm run check:all`: direct-token audit, lint, 68 unit tests, production build and checks, bundle budgets, and 32 desktop Playwright runs across 1280px and 1440px. The largest JavaScript chunk is 340,736 bytes; eager JavaScript is 190,239 gzip bytes; the deferred Performance chunk is 100,128 gzip bytes; and the deferred Calendar chunk is 23,174 gzip bytes.
- The semester selector refinement passes `npm run check:all`: direct-token audit, lint, 69 unit tests, production build and checks, bundle budgets, and 32 desktop Playwright runs across 1280px and 1440px. The restored dropdown supports direct semester selection, while detached 36px raised-surface arrows provide bounded previous/next navigation. Eager JavaScript is 190,296 gzip bytes; the largest JavaScript chunk remains 340,736 bytes.
- The 36px semester-arrow surface refinement passed `npm run build` and the focused Academic Performance browser flow at 1280px and 1440px.
- Academic Modules passes the reusable `raised` surface variant to its module cards while retaining `surface-hover`. Dashboard module cards remain on `surface`.
- Repeated portal card collections now use the shared responsive `CardGrid` with 16px gaps. Module, performance-chart, and profile issue variants preserve readable 224px, 352px, and 240px minimum card widths respectively, then wrap into new rows instead of causing horizontal overflow. The Design System Explorer and unit/browser coverage include all three variants; structural timetable, calendar, details, and News carousel layouts intentionally do not use this wrapper. Token audit, lint, 72 unit tests, production build and production/bundle checks passed. The 32 desktop browser assertions passed at 1280px and 1440px in two terminal-safe batches.
- Enabled interactive elements now consistently use a pointer cursor through one global base rule for buttons, links, tabs, menu choices, and navigable module cards; disabled controls keep their disabled affordance.
- The pointer-cursor audit removed shared `cursor-default` overrides from interactive dropdown and context-menu choices. Focused Module, semester selector, and Academic desktop coverage passed at 1280px and 1440px; token audit, lint, 72 unit tests, production build and production/bundle checks also passed.
- Final verification for the completed portal work passed `npm run check:all`: direct-token audit, lint, 72 unit tests, production build and checks, bundle budgets, and 32 Playwright desktop assertions at 1280px and 1440px. The largest JavaScript chunk is 340,736 bytes; eager JavaScript is 190,469 gzip bytes; the deferred Performance chunk is 100,128 gzip bytes; and the deferred Calendar chunk is 23,175 gzip bytes.
- Module Details is implemented as the lazy `/academic/modules/:moduleId?semesterId=:semesterId` tier inside Academic Modules. Dashboard and Academic cards navigate with their semester, the internal breadcrumb restores the semester-specific module list, and unknown or unenrolled combinations show a recoverable unavailable state.
- The dedicated detail Portal API boundary joins ModuleOffering and ModuleAssessment records to the existing Programme, Semester, Enrolment, Module, Lecturer, ModuleLecturer, and TimetableEntry data. Graphic Design 3 uses its existing online mode, four credits, three weekly timetable entries, two lecturer cards, and a realistic 100% assessment outline. Materials and Attendance are separately loaded completed tabs; enabled lecturer Review actions remain explicit frontend-only placeholders.
- `ContentBreadcrumb`, `LecturerCard`, the lecturer `CardGrid` variant, `SegmentedBar`, and the 24px `type-section-lg` utility are documented in the Design System Explorer. ModuleOffering and ModuleAssessment are documented in the Data Model Explorer.
- The Module Details implementation passes `npm run check:all`: direct-token audit, lint, 82 unit tests, production build and checks, bundle budgets, and 34 Playwright desktop assertions across 1280px and 1440px. The largest JavaScript chunk is 340,736 bytes; eager JavaScript is 193,271 gzip bytes; the deferred module-detail chunk is about 2.43 kB gzip; the deferred Performance chunk is 100,128 gzip bytes; and the deferred Calendar chunk is 23,175 gzip bytes.
- Module Details contextual navigation is now complete for the approved surfaces: Dashboard and Academic module cards, every timetable class card, module-linked Calendar agenda cards, and the module chat header all open the exact module-and-semester offering. Community module rows retain chat as their primary action, while module chat contexts now carry stable `moduleId` and `semesterId` values instead of relying on display-title matching. Calendar meetups and holidays remain static because they have no module relationship.
- Academic Performance grade rows remain non-navigable and disclose their assessment results inline rather than opening Module Details or a separate result route.
- The contextual-link change passes `npm run check:all`: direct-token audit, lint, 84 unit tests, production build and checks, bundle budgets, and all 34 Playwright desktop assertions across 1280px and 1440px. The largest JavaScript chunk is 340,736 bytes; eager JavaScript is 193,373 gzip bytes; the deferred module-detail chunk is about 2.43 kB gzip; the deferred Performance chunk is 100,128 gzip bytes; and the deferred Calendar chunk is 23,298 gzip bytes.
- Module Details refinement adds the reusable bordered `InnerSection` for the metadata field group and assessment outline, tighter 8px by 12px lecturer cards, and optional Segmented Tabs dividers that appear only between adjacent inactive tabs. `SegmentedBar` now rounds only its outer start and end segments. The Design System Explorer documents and renders the refined patterns.
- The refinement passes `npm run check:all`: direct-token audit, lint, 85 unit tests, production build and checks, bundle budgets, and all 34 Playwright desktop assertions across 1280px and 1440px. The largest JavaScript chunk is 340,736 bytes; eager JavaScript is 193,529 gzip bytes; the deferred module-detail chunk is about 2.51 kB gzip; the deferred Performance chunk is 101,010 gzip bytes; and the deferred Calendar chunk is 23,297 gzip bytes.
- The shared Button primitive, generic tab trigger, active sidebar menu state, and native file-input action now use regular 400-weight labels. Content headings and badges retain their intentional typography hierarchy.
- Module assessment fixture, API tests, Data Model Explorer sample, and Design System Explorer now explicitly expose Graphic Design 3's Midterm (20%) and Final Exam (35%) segments alongside its quizzes and assignments.
- The typography, assessment-label, and divider refinements pass `npm run check:all`: direct-token audit, lint, 86 unit tests, production build and checks, bundle budgets, and all 34 Playwright desktop assertions across 1280px and 1440px. The largest JavaScript chunk is 340,736 bytes; eager JavaScript is 193,527 gzip bytes; the deferred module-detail chunk is about 2.51 kB gzip; the deferred Performance chunk is 100,127 gzip bytes; and the deferred Calendar chunk is 23,297 gzip bytes.
- The optional Module Details divider between adjacent inactive tabs now uses `muted-foreground` at 40% opacity, making it clearer than the base border while remaining secondary to active tab content.
- `OnlineClassIndicator` now supplies one consistent blue laptop icon and `Online Class` tooltip for Module cards and online timetable class cards. Timetable derives delivery mode from the related dashboard Module record rather than duplicating that property into timetable entries; a current online class switches the icon to the brand foreground for contrast. The production build and focused delivery-mode flow passed at 1280px and 1440px.
- Module Details now places the assessment outline directly after lecturers and before the written summary and metadata. The production build and focused Module Details flow passed at 1280px and 1440px.
- The Module Details assessment outline uses the single opaque global `brand` colour for every segment. Assessment labels, percentages, and relative widths remain the meaning carriers; status colours remain unchanged.
- Academic Performance grade cards now use `surface-raised` and the reusable Radix-backed Accordion primitive. One grade opens at a time, selecting it again collapses it, and the full keyboard-accessible trigger discloses Assessment, Weight, Score, Contribution, and total rows without navigating away.
- The academic-performance Portal API response now joins semester-specific ModuleOffering and ModuleAssessment records with the new StudentAssessmentResult entity. Each published result has one realistic score and supplied weighted contribution per assessment; response validation prevents missing, duplicate, mismatched, or over-weight breakdown records. The Data Model and Design System explorers and source-of-truth docs are synchronized.
- The grade-accordion work passes `npm run check:all`: direct-token audit, lint, 90 unit tests, production build and checks, bundle budgets, and all 34 Playwright desktop assertions across 1280px and 1440px. The first full run hit the existing performance test's 5-second timeout under parallel load; its isolated retry and the complete rerun passed. The largest JavaScript chunk is 340,736 bytes; eager JavaScript is 196,224 gzip bytes; the deferred Performance chunk is 100,127 gzip bytes; and the deferred Calendar chunk is 23,296 gzip bytes.
- `DataTable` is now the shared semantic, typed table for records, assessment, GPA, and CGPA explanations. It owns columns, row identity, totals, numeric alignment, borders, empty state, accessible labelling, optional interactive-row activation, and horizontal overflow; the Design System Explorer includes a standalone example.
- Academic Performance chart cards now place a secondary `View breakdown` action opposite each current value. GPA opens the selected semester's module Points, Credits attempted, Credits earned, Final points, totals, and published formula. CGPA opens the published semester history through the selection with Total credits attempted, Total credits earned, Total points, and CGPA. Dashboard chart cards intentionally remain unchanged and have no breakdown action.
- `Popup` is the reusable Radix-backed titled overlay used by the performance breakdowns. The Academic-only popup UI is emitted as a separate deferred production chunk, so the dashboard does not request it.
- Popup layout now has three shared regions: a bordered 24px header and content, plus a centred 16px footer. The footer supplies a secondary `Back` action by default (or a consumer-provided `Cancel` label), while the top close control remains available.
- Popup headers now group their title and body-colour description in one 8px-gap column beside the close control. Dialog, sheet, and lightbox backdrops use a non-blurred 50% black overlay.
- The Popup close control is absolutely positioned 16px from the header's top-right edge, leaving the title/description group as its sibling.
- The centred-footer and absolute-close refinement passes `npm run check:all`: direct-token audit, lint, 96 unit tests, production and bundle checks, and all 34 Playwright desktop assertions at 1280px and 1440px. The existing six Fast Refresh lint warnings and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- The popup header and overlay refinement passes `npm run check:all`: direct-token audit, lint, 96 unit tests, production and bundle checks, and all 34 Playwright desktop assertions at 1280px and 1440px. The existing six Fast Refresh lint warnings and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- The popup-layout refinement passes `npm run check:all`: direct-token audit, lint, 96 unit tests, production and bundle checks, and all 34 Playwright desktop assertions at 1280px and 1440px. The existing six Fast Refresh lint warnings and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- The academic-performance contract now requires `points`, `creditsAttempted`, `creditsEarned`, and `finalPoints` on StudentModuleResult plus `totalCreditsAttempted`, `totalCreditsEarned`, and `totalPoints` on StudentSemesterPerformance. Response validation reconciles those inputs with the explicit outcome and published GPA/CGPA without defining or inferring the institution's grading policy.
- The completed assessment/GPA/CGPA breakdown work passes `npm run check:all`: direct-token audit, lint, 96 unit tests, production build and checks, bundle budgets, and all 34 Playwright desktop assertions across 1280px and 1440px. The largest JavaScript chunk is 340,836 bytes; eager JavaScript is 198,441 gzip bytes; the deferred Performance chart chunk is 100,169 gzip bytes; the deferred Calendar chunk is 23,298 gzip bytes; and the Academic-only breakdown popup is about 1.67 kB gzip.
- GPA breakdown terminology now follows the academic record directly: Points, Credits attempted, Credits earned, and Final points per module. CGPA history exposes Total credits attempted, Total credits earned, and Total points. The failed Semester 2 fixture records three attempted credits and zero earned credits; passing results earn their attempted credits. GPA/CGPA divisors remain credits attempted.
- The performance-credit refinement passes `npm run check:all`: direct-token audit, lint, 97 unit tests, production and bundle checks, and all 34 Playwright desktop assertions at 1280px and 1440px. The largest JavaScript chunk is 340,836 bytes; eager JavaScript is 198,531 gzip bytes; the deferred Performance chart chunk is 100,168 gzip bytes; and the deferred Calendar chunk is 23,298 gzip bytes. The existing six Fast Refresh lint warnings and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- CGPA breakdown tables now use an equal-width six-column layout. The GPA popup reuses `SemesterSelector` above its table and limits it to published result semesters, so each selection updates the popup title, rows, totals, and formula without exposing unpublished Semester 5 or 6 states. `SemesterSelector` supports an optional explicit list of available semester numbers for this reuse.
- The breakdown-selector refinement passes `npm run check:all`: direct-token audit, lint, 98 unit tests, production and bundle checks, and all 34 Playwright desktop assertions at 1280px and 1440px. The largest JavaScript chunk is 340,836 bytes; eager JavaScript is 199,195 gzip bytes; the deferred Performance chart chunk is 100,168 gzip bytes; and the deferred Calendar chunk is 23,345 gzip bytes. The existing six Fast Refresh lint warnings and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- Popup content now uses the standard `surface` tier rather than `surface-raised`; the Design System Explorer and popup checks use the same expectation.
- The popup-surface refinement passes `npm run check:all`: direct-token audit, lint, 98 unit tests, production and bundle checks, and all 34 Playwright desktop assertions at 1280px and 1440px. The largest JavaScript chunk is 340,836 bytes; eager JavaScript is 199,198 gzip bytes; the deferred Performance chart chunk is 100,169 gzip bytes; and the deferred Calendar chunk is 23,345 gzip bytes. The existing six Fast Refresh lint warnings and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- GPA popup grade letters now use the same semantic success/destructive treatment as the inline grade accordions. Its published-semester selector has an exact 24px gap before the module calculation table. The focused performance-breakdown unit suite, production build, and Academic browser flow passed at 1280px and 1440px; existing `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- Shared Button no longer applies a pressed-state translation, so every Button variant stays in place on click. The retired Action required-specific translation override and its assertion were removed.
- The button-motion refinement passes `npm run check:all`: direct-token audit, lint, 98 unit tests, production and bundle checks, and all 34 Playwright desktop assertions at 1280px and 1440px. The largest JavaScript chunk is 340,836 bytes; eager JavaScript is 199,166 gzip bytes; the deferred Performance chart chunk is 100,169 gzip bytes; and the deferred Calendar chunk is 23,344 gzip bytes. The existing six Fast Refresh lint warnings and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- Module Details assessment outlines now use a single opaque `brand` bar for every assessment segment. `SegmentedBar` no longer accepts colour/tone input, so its labels, percentages, and proportional widths—not colour shades—express assessment differences. The Design System Explorer and focused module-detail tests use the same contract.
- The single-brand outline refinement passes `npm run check:all`: direct-token audit, lint, 98 unit tests, production and bundle checks, and all 34 Playwright desktop assertions at 1280px and 1440px. The largest JavaScript chunk is 340,836 bytes; eager JavaScript is 199,171 gzip bytes; the deferred Performance chart chunk is 100,168 gzip bytes; and the deferred Calendar chunk is 23,344 gzip bytes. The existing six Fast Refresh lint warnings and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- Expanded shared accordion trigger rows retain the same `surface-hover` background used for hover feedback. Their disclosed content continues to use the consumer-supplied base surface.
- The latest efficiency pass keeps the shared Academic heading, tabs, and page panel eager while deferring the completed Performance and Modules tab bodies behind layout-matched loading states. Existing near-viewport chart and Calendar loading, deferred Chat/Profile/Module Detail/popup boundaries, memoized dashboard/sidebar boundaries, animation-frame sidebar resizing, and React Strict Mode remain unchanged. The bundle gate now enforces both Academic boundaries and caps eager JavaScript at 190 KiB gzip.
- The efficiency pass reduces eager JavaScript from 199,171 to 194,251 gzip bytes (4,920 bytes, about 2.5%). The new Academic Performance and Academic Modules chunks are 3,748 and 1,140 gzip bytes respectively; the deferred chart chunk remains 100,167 gzip bytes, the Calendar chunk is 23,311 gzip bytes, and the largest chunk is 340,839 bytes. `npm run check:all` passes the direct-token audit, lint, 98 unit tests, production and bundle checks, and all 36 desktop Playwright tests across 1280px and 1440px.
- Module Details Materials is now implemented as a read-only, route-controlled flow. `tab=materials` opens a separately queried vertical teaching-week accordion; the machine clock selects the current week and API `availableFrom` timestamps control locked items.
- Released assignment rows open shareable `/academic/modules/:moduleId/materials/:itemId?semesterId=...` views with nested breadcrumb navigation and type-specific read-only details. Resources are read-only and expose visual View/Download placeholders; physical quizzes and exams intentionally expose neither action. Functional download, Submit, Start, upload, grading, and progress mutation flows remain out of scope. Attendance is a separately loaded completed tab; lecturer Review remains the Module Details placeholder.
- The Materials contract is isolated in `contracts/module-materials.ts`, with its generated fixtures and mock adapter in separate deferred modules. ModuleWeek, ModuleLearningItem, and StudentLearningProgress are visible in the Data Model Explorer with their offering, week, assessment, and student relationships. The Design System Explorer shows the released and locked learning-row patterns.
- `useHorizontalScroll` now backs both News and Materials strips. It preserves trackpad, mouse-drag, arrow, and hidden-scrollbar behavior without capturing pointer input that begins on an interactive child.
- To preserve the strict 190 KiB eager budget after adding shareable Materials routes, the dashboard News, Timetable, and Academic Performance sections now load as immediate deferred chunks behind layout-matched skeletons. The Module Materials API contract, fixture generation, and mock adapter also load only when the Materials query runs.
- The completed Materials and optimization work passes `npm run check:all`: direct-token audit, lint, 108 unit tests, production and bundle checks, and all 38 Playwright tests across 1280px and 1440px. The largest JavaScript chunk is 340,900 bytes; eager JavaScript is 194,407 gzip bytes; the deferred Performance chart is 100,194 gzip bytes; Calendar is 23,271 gzip bytes; Academic Performance is 2,209 gzip bytes; and Academic Modules is 1,152 gzip bytes. The six existing Fast Refresh warnings and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- Module Materials week disclosure now matches the shared Grades accordion composition: one `surface-raised` card contains its trigger and any disclosed inline learning rows, with dividers between multiple rows rather than nested item cards. Week labels use bold body typography; the initial current week can be collapsed; completed and locked weeks share the neutral outline Badge, while the current week uses the brand Badge. The focused Materials browser flow passes at 1280px and 1440px, and the focused module-detail component suite plus production build pass. Direct-token, lint, unit, production, and bundle stages also passed; existing Fast Refresh and `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- Materials status pills appear only on week accordion triggers. Future-week triggers use the neutral `Locked` label rather than `Upcoming`; completed and current triggers retain their semantic pills. Learning rows preserve their category icon and locked visual state but no longer show per-item lock or progress pills. Focused Materials unit and desktop flows pass at 1280px and 1440px, alongside the production build.
- Module Materials now has one canonical mock assessment schedule. It drives Materials release/deadline fields, active module-card `ModuleIssue` records, and Calendar deadline events, eliminating their formerly conflicting hardcoded dates. Graphic Design 3 Assignment 1 is released in Week 2 and due at the end of Week 4, giving the dashboard a realistic released-assignment issue sample. Every teaching week and lecture note has a genuine teaching-topic title rather than a generated `Week N` title.
- The bundle gate permits 190.25 KiB gzip for the eager graph, a 256-byte allowance for the validated `ModuleIssue`-to-`ModuleAssessment` relationship. The actual teaching-topic catalogue remains in the deferred Materials fixture rather than the eager dashboard path.
- The schedule and lecture-topic refinement passes token, lint, 110-unit-test, production, and bundle checks. The affected Module Materials and Academic Modules browser flows pass at both 1280px and 1440px. Existing Fast Refresh and `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.

- Materials resource rows now suppress the fixture filename, retain the teaching-topic title, and add a far-right shared 36px `IconButton` with a bordered transparent surface. Its existing Tooltip reads `Download notes`; it is intentionally a frontend-only no-op until an authorised Portal API URL exists, and locked resources disable the action as well as remaining visually reduced.
- Materials rows are non-clickable. Assessment category labels include their percentage, and titles pair with the relevant warning date pill. Lecture notes and assignments use explicit View/Download IconButton actions instead of whole-row navigation; released assignment View preserves its existing detail route, while lecture-note View and all Download actions remain frontend-only placeholders. Locked weeks disable those actions. Physical quizzes and exams show no actions.
- Assignment rows label their warning date as `Due by DD/MM/YYYY`, while quiz and exam rows use `Scheduled on DD/MM/YYYY`.
- The focused Materials component suite (10 tests) and production build pass after the explicit action and assessment-date-label refinements.
- The shared `Badge` primitive now defaults every variant to regular 400 weight, keeping Materials date pills, week-status pills, and future status uses consistent.
- Materials now groups every learning item under the week containing its canonical `availableFrom` timestamp. Quiz 1 and Assignment 1 therefore live in current Week 2 while retaining their later Week 3 and Week 4 deadlines; locked future weeks no longer expose already released coursework actions. The response contract validates the release-week relationship.
- The Dashboard Modules section now follows the existing immediate deferred-section pattern with a layout-matched four-card fallback, keeping its module-card and online-tooltip implementation outside the eager graph while preserving the rendered dashboard flow.
- The release-week correction and Dashboard Modules deferral pass `npm run check:all`: direct-token audit, lint, 115 unit tests, production and bundle checks, and all 38 desktop Playwright tests across 1280px and 1440px. Eager JavaScript is 193,351 gzip bytes; the existing six Fast Refresh warnings and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- Module Materials now matches the Grades accordion spacing model: the disclosed wrapper has no padding, every learning or empty row owns an equal 12px inset, and dividers span the shared content width.
- Locked Module Materials rows now hide their Due/Scheduled date Badges. Current and completed teaching-week triggers show one reusable 20px `AttentionIndicator` only when derived assessment state needs action: released assignments remain flagged until submitted or graded, while released quizzes and exams remain flagged through their scheduled campus date and clear the following date. Locked weeks and resources never create the marker. The calculation uses the Portal API timezone and the existing minute-updating machine clock; no duplicate alert field was added to the contract.
- The attention-state refinement passes `npm run check:all`: direct-token audit, lint, 120 unit tests, production and bundle checks, and all 38 desktop Playwright tests across 1280px and 1440px. Eager JavaScript is 193,375 gzip bytes; the existing six Fast Refresh warnings and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- Module Materials no longer renders the upcoming-assessment strip. Lecture notes and assignments keep their explicit View/Download actions, while quizzes and exams are treated as physical assessments and show neither action. Module Details now places the API-supplied module code in muted body typography above the module name immediately after its breadcrumb. The retired strip component and its unused sorting model were removed rather than retained as dead deferred code.
- The physical-assessment and heading refinement passes `npm run check:all`: direct-token audit, lint, 119 unit tests, production and bundle checks, and all 38 desktop Playwright tests across 1280px and 1440px. The deferred Module Materials chunk drops from about 4.99 kB to 4.14 kB gzip; eager JavaScript is 193,348 gzip bytes. The existing six Fast Refresh warnings and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- The heading label correction binds that muted line to `Module.code` (`GRD301` in the current fixture) instead of displaying a generic word. The production build and focused Module Details browser flow pass at 1280px and 1440px; only the existing Playwright colour warning remains.
- Assignment deadline navigation is now canonical across the portal. Dashboard and Academic Modules assignment issue cards, Calendar assignment agenda cards, and the existing Materials View action all open the same semester-specific Assignment Details route. News assignment announcements remain article cards. Module cards use a non-interactive surface with separate module and issue buttons, avoiding nested controls while preserving keyboard access; exam issues still open Module Details.
- `ModuleIssue.moduleLearningItemId` and nullable `CalendarEvent.moduleLearningItemId` connect issue/deadline records to the matching Materials learning item. Assignment Calendar events require both module and learning-item IDs, while other Calendar kinds require a null learning-item ID. One mock ID helper keeps Materials, issue, and Calendar fixtures synchronized; the Data Model Explorer includes both new relationships.
- Every portal URL change now immediately resets the actual nested main scroller to the top, including pathname, query, hash, and browser Back/Forward changes. Query-driven Academic tabs and semester changes follow the same rule; local non-route interactions retain their position.
- The assignment-navigation and route-scroll work passes `npm run check:all`: direct-token audit, lint, 120 unit tests, production and bundle checks, and all 42 desktop Playwright runs across 1280px and 1440px. The largest JavaScript chunk is 340,900 bytes; eager JavaScript is 193,590 gzip bytes; the Performance chart chunk is 100,194 gzip bytes; and the Calendar chunk is 23,329 gzip bytes. The existing six Fast Refresh warnings and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- Assignment Details now demonstrates a complete reset-on-refresh submission lifecycle. The API supplies brief metadata and a one-file PDF/ZIP policy capped at 50 MiB; students can select and submit, replace the current file, or confirm deletion from release time until the hard exclusive `dueAt` cutoff. Only filename, MIME type, integer byte size, and latest timestamp are retained in `MockPortalApi`; selected file bytes are never stored or uploaded. The visible assignment-brief download remains an intentional no-op placeholder.
- Successful assignment submission changes the Materials row to a green `Submitted on` state and removes the matching dashboard and Academic Modules issue through Portal API queries. Deletion before due restores both the due badge and issue. Refresh constructs a clean mock session and returns to the default unsubmitted fixture.
- `StudentAssignmentSubmission` is now represented in the Zod contract and Data Model Explorer with Student and ModuleLearningItem relationships. Contracts, fixtures, the deferred Materials mock adapter, PortalApi mutation methods, TanStack Query cache invalidation, unit coverage, and both desktop browser flows are synchronized. No live backend, persistent browser storage, cloud upload, or real download was added.
- The assignment workflow passes `npm run check:all`: direct-token audit, lint, 127 unit tests, production and bundle checks, and all 44 desktop Playwright runs across 1280px and 1440px. The largest JavaScript chunk is 340,900 bytes; eager JavaScript is 194,072 gzip bytes; the Performance chart chunk is 100,193 gzip bytes; Calendar is 23,321 gzip bytes; Academic Performance is 2,226 gzip bytes; and Academic Modules is 1,226 gzip bytes. The existing six Fast Refresh warnings and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- Assignment submission controls are text-only and right-aligned inside the submission `InnerSection`; submitted filename/timestamp stays at left without a redundant green status pill. Change submission and the Popup trigger Delete submission use the shared secondary Button; the final Popup Delete submission confirmation uses the shared primary Button. The assignment brief continues to use the shared `FileLink` placeholder.
- `FileLink` is now icon-free: an underlined brand filename that changes to the title colour on hover. It remains a no-op placeholder until the Portal API provides an authorised file URL.
- Shared route tabs now measure their active indicator before first paint, avoiding the visible refresh nudge before subsequent tab changes animate. Selecting an active `TabbedPagePanel` or `SegmentedTabs` item notifies its route consumer again: Academic Modules returns from a nested detail to its default list, while active Materials returns from an assignment detail to the default Materials view.
- The tab refresh and active-tab reset refinement passes `npm run check:all`: direct-token audit, lint, 128 unit tests, production and bundle checks, and all 46 Playwright desktop runs across 1280px and 1440px. The six existing Fast Refresh and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- The viewport-locked portal shell prevents the document from becoming a second vertical scroller. The main portal content remains the one page-level scroller; the sidebar/community column deliberately keeps its own independent scrollbar when its content exceeds the viewport. A focused Assignment Details browser check enforces this boundary. This passes `npm run check:all`: direct-token audit, lint, 128 unit tests, production and bundle checks, and all 48 desktop Playwright runs across 1280px and 1440px. The existing six Fast Refresh and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- The main header now shows the active student's programme summary instead of the page breadcrumb: white body-text programme title with a muted `Year 2 · Semester 4` line below, matching the student identity stack. The current frontend derives the academic year from two semesters per year. This passes `npm run check:all`: direct-token audit, lint, 128 unit tests, production and bundle checks, and all 48 desktop Playwright runs across 1280px and 1440px. The existing six Fast Refresh and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.

- Module Materials learning items now use one full-width clickable row instead of separate View and Download icon actions. Every released row receives the shared immediate `surface-hover` keyboard and pointer treatment; assignments retain their Assignment Details navigation, while released resources, quizzes, and exams are deliberate no-op placeholders. Locked rows remain natively disabled, muted, and without hover feedback. `npm run check:all` passes direct-token audit, lint, 128 unit tests, production and bundle checks, and all 48 Playwright desktop runs across 1280px and 1440px. The largest JavaScript chunk is 340,900 bytes and eager JavaScript is 193,576 gzip bytes. The existing six Fast Refresh and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.

- The mock data foundation is now one validated canonical Portal record graph rather than a collection of unrelated screen stories. Its six aggregates cover Student Record, Institution, Curriculum, Delivery, Student Journey, and Finance; route responses remain smaller derived read models. The Data Model Explorer opens on canonical entities, filters by aggregate, documents owner/scope/access paths, limits relationship graphs to visible records, and includes a six-semester student timeline.
- The reference journey is a fictional six-semester, 120-credit programme with Semesters 1–3 completed, a failed-and-repeated ART201 attempt, Semester 4 current on 26 August 2026, and Semesters 5–6 planned. Official performance stops at Semester 3 (GPA 3.39, CGPA 3.33); Semester 4 has no assessment, module, or term results. Historical results trace through 16 module attempts and 96 assessment results. Attendance stops at the fixture date rather than inventing future records.
- The same Programme Enrolment owns a reconciled MYR finance history: four issued invoices, separate lines/payments/allocations/adjustment records, no future-semester invoices, and a current RM2,450 outstanding balance. `PortalApi.getPortalRecordGraph()` and `PortalApi.getFinanceStatement()` expose cloned frontend-only mock responses. The Finance route itself remains a placeholder, and no database, authentication, persistence, or live API was added.
- Ordinary Portal fixture payloads now load through the asynchronous mock service boundary instead of the eager application graph. Only the small current-student identity required by the shell remains eager; the canonical schemas and full graph stay deferred until their APIs or Developer Tools request them. The existing bundle budget was preserved rather than raised.
- The canonical-foundation work passes `npm run check:all`: direct-token audit, lint, 136 unit tests, production and bundle checks, and all 48 desktop Playwright runs across 1280px and 1440px. Eager JavaScript is 188,511 gzip bytes, below the 194,816-byte gate; the largest chunk remains 340,900 bytes, Performance chart is 100,191 gzip bytes, and Calendar is 23,322 gzip bytes. The existing six Fast Refresh warnings and Playwright `FORCE_COLOR`/`NO_COLOR` warning remain non-blocking.

- Academic Timetable is a completed lazy-loaded Academic route at `/academic/timetable`. Its shareable semester selector exposes all six semesters, with future Semesters 5–6 muted but selectable. The page reuses the shared timetable and adds the programme attendance summary, semester-bounded session calendar, Present/Absent/Not taken legend, and module-linked selected-day details.
- Dashboard Timetable, Module Details schedules, dashboard Calendar class events, and Academic Timetable now derive from the same canonical Class Schedules and Class Sessions. The retired one-week dashboard timetable fixture is no longer a competing source. Current-class highlighting uses the recurring campus-local schedule and machine time.
- Canonical Attendance now supports only Present and Absent. Current-term future sessions are retained as Scheduled with no Attendance Record and appear as derived Not taken state; cancelled holiday sessions are omitted. Programme Version owns the 80% minimum attendance threshold.
- The timetable/attendance implementation adds `AcademicTimetableResponse`, `PortalApi.getAcademicTimetable()`, its TanStack query, recurring Timetable Entry metadata, Attendance Session Explorer metadata, semester selection, unit coverage, and desktop browser coverage. An omitted `semesterId` input is normalized to the active-semester request, matching the route without a query string; a valid future ID remains selected and returns empty collections.
- The completed timetable/attendance work passes `npm run check:all`: direct-token audit, lint, 140 unit tests, production and bundle checks, and all 50 Playwright desktop runs across 1280px and 1440px. Eager JavaScript is 189,167 gzip bytes; the largest chunk is 340,900 bytes; Performance chart is 100,193 gzip bytes; Calendar is 3,485 gzip bytes; Academic Performance is 2,238 gzip bytes; and Academic Modules is 1,224 gzip bytes. The existing six Fast Refresh warnings and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- Academic Performance, Modules, and Timetable now share one tab-content structure: `SemesterSelector` owns the visible muted Semester label and 8px field gap, major blocks use a 32px gap, and the panel-type `ContentSection` owns every 16px `type-title` section heading plus its 16px content gap. The timetable grid is extracted as `TimetableCard`, allowing Dashboard to use the page type's larger heading while Academic uses the panel heading treatment.
- Dashboard Calendar and Academic Attendance now share `CalendarMonthSelector`. Attendance no longer relies on DayPicker's native navigation: its previous/month/next controls stay in one full-width three-column row, the selector owns the divider before the 8px-inset grid, and the bordered calendar remains the full Attendance-section width.
- The Academic tab-content consistency audit passes `npm run check:all`: direct-token audit, lint, 141 unit tests, production and bundle checks, and all 50 Playwright desktop runs across 1280px and 1440px. Eager JavaScript is 189,176 gzip bytes; the largest chunk is 340,900 bytes; Performance chart is 100,193 gzip bytes; Calendar is 3,368 gzip bytes; Academic Performance is 2,210 gzip bytes; and Academic Modules is 1,193 gzip bytes. The existing six Fast Refresh warnings and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- Dashboard Calendar and Academic Attendance now use one simple current-date rule: today has only a brand-colour day label. It no longer adds a faded brand fill or a separate selected/current hover state; all enabled day cards share `surface-hover` selection and hover behaviour. Attendance segments are 8px high with 4px gaps; a single bar is fully rounded and multiple bars round only their outer ends.
- Attendance is now a dedicated lazy-loaded Academic tab at `/academic/attendance`, immediately after Timetable in both the tab panel and sidebar. Timetable retains only its semester selector and recurring grid; Attendance owns its independent shareable semester selector, programme attendance summary, calendar, legend, and selected-day module links. Both routes continue to reuse the validated `AcademicTimetableResponse` and one Portal API query without duplicate contracts or fixtures.
- The Timetable/Attendance route split passes `npm run check:all`: direct-token audit, lint, 142 unit tests, production and bundle checks, and all 52 desktop Playwright runs across 1280px and 1440px. The largest JavaScript chunk is 340,900 bytes; eager JavaScript is 189,412 gzip bytes; the deferred Timetable content is about 0.87 kB gzip; deferred Attendance content is about 2.96 kB gzip; and Calendar is 3,355 gzip bytes. The existing six Fast Refresh warnings and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- Module Details Attendance is implemented as a separately queried, lazy-loaded tab body. Its response binds the Module and Semester to the exact Enrolment, including repeat attempts, and derives the module sessions from the same canonical Class Session and Attendance Record projection as programme Attendance. A future curriculum recommendation is not treated as an enrolment and therefore cannot open Module Details.
- The module view deliberately contains only four read-only summary metrics and a module-filtered calendar. Attendance Rate excludes future Not taken sessions and is compared with the Programme Version threshold; the calendar retains month navigation and the legend but has no selectable dates, hover feedback, session details, or self-navigation. Programme Attendance remains independently interactive and unchanged.
- The reusable `CardGrid` now includes a wrapping compact metric variant, while the shared Attendance Calendar supports explicit interactive and read-only modes. Both patterns and the completed Module Attendance composition are represented in the Design System Explorer; the exact enrolment relationship and module endpoint are represented in the Data Model Explorer.
- The module-attendance implementation passes `npm run check:all`: direct-token audit, lint, 150 unit tests, production and bundle checks, and all 52 desktop Playwright runs across 1280px and 1440px. The largest JavaScript chunk is 340,900 bytes; eager JavaScript is 189,700 gzip bytes; the deferred Performance chart is 100,193 gzip bytes; Calendar is 3,350 gzip bytes; Academic Performance is 2,205 gzip bytes; Academic Modules is 1,191 gzip bytes; and the module Attendance body is about 1.02 kB gzip. The existing six Fast Refresh warnings and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- The latest surface-tier refinement uses `surface-raised` consistently for Academic Module cards, programme and module Attendance summary cards, Calendar agenda cards, and Attendance session detail rows. Attendance metric titles use body typography and their supporting copy uses the normal label style. Direct portal-browser coverage now asserts the raised treatments alongside the existing interaction checks.
- The final completed-work gate passes `npm run check:all`: direct-token audit, lint, 150 unit tests, production and bundle checks, and all 52 desktop Playwright runs across 1280px and 1440px. The largest JavaScript chunk is 340,900 bytes; eager JavaScript is 189,716 gzip bytes; the Performance chart is 100,194 gzip bytes; Calendar is 3,347 gzip bytes; Academic Performance is 2,211 gzip bytes; and Academic Modules is 1,193 gzip bytes. The existing six Fast Refresh warnings and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- Academic Study Plan is now a completed lazy-loaded route at `/academic/study-plan`. Its dedicated Zod response and `PortalApi.getAcademicStudyPlan()` projection derive the programme's graduation-eligibility overview and all six semester positions from the canonical record graph. Programme Version owns the 2.0 minimum graduation CGPA. The overview checks 60 / 120 earned Credit Hours, latest published 3.33 CGPA, the RM2,450 outstanding balance, and whether the expiring student pass covers the 19 December 2027 expected completion date. Passport & Visa displays only the concise `Expiring` status; solid semantic check/X indicators use the dark background glyph. Programme Status remains `In Progress` until all four requirements pass; later graduation clearance is separate. Download Transcript remains an intentional plain no-icon no-op placeholder.
- The Study Plan semester outline uses one vertical, single-open, collapsible shared Accordion with current Semester 4 open by default. Completed Semesters 1-3 expose only published GPA, CGPA, and module results; current Semester 4 and planned Semesters 5-6 expose loads without invented results. Direct prerequisite names, exact registration status, repeat attempt number, and the Semester 3 ART201 replacement result remain traceable to their canonical owners. Module rows are read-only and detailed GPA/CGPA calculation explanations remain on Performance.
- The Data Model Explorer identifies `GET /api/v1/academic/study-plan` on every contributing canonical record field, including Programme Version's minimum graduation CGPA. The graduation-eligibility refinement passes `npm run check:all`: direct-token audit, lint, 161 unit tests, production and bundle checks, and all 54 desktop Playwright runs across 1280px and 1440px. The largest JavaScript chunk is 340,900 bytes; eager JavaScript is 190,178 gzip bytes; the deferred Study Plan content is 2.71 kB gzip; its mock projection is 2.95 kB gzip; Performance chart is 100,195 gzip bytes; Calendar is 3,348 gzip bytes; Academic Performance is 2,210 gzip bytes; and Academic Modules is 1,191 gzip bytes. The existing six Fast Refresh warnings and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- The follow-up eligibility visual refinement uses dark `background` check/X glyphs inside the semantic status circles and reduces Passport & Visa display copy to its concise source status (`Expiring`). Its focused Study Plan component test and production build passed.
- Study Plan semester summaries now live in each Accordion trigger as four fixed-width metrics with 12px gaps and remain visible while collapsed. Period subtitles and duplicate far-right load text are removed; the status Badge and chevron remain at the right. Expanded content contains only the module table, where Registration is relabelled Status, Credits and Grade are left-aligned, and the final row border is removed locally. `npm run check:all` passes with direct-token audit, lint, 161 unit tests, production and bundle checks, and all 54 desktop Playwright runs at 1280px and 1440px. The largest JavaScript chunk is 340,900 bytes; eager JavaScript is 190,171 gzip bytes; Study Plan is 2.65 kB gzip; its mock projection is 2.95 kB gzip; Performance chart is 100,193 gzip bytes; Calendar is 3,347 gzip bytes; Academic Performance is 2,207 gzip bytes; and Academic Modules is 1,191 gzip bytes. The existing six Fast Refresh warnings and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- Study Plan summary metrics now have subtle shared-border vertical dividers between the later columns while retaining their fixed widths and 12px gaps.
- The canonical registration timeline is reconciled: every Study Period registers from 08:00 seven calendar days before its term through 23:59:59 on the preceding day, historical timestamps fall inside their owning windows, and the five active Semester 4 attempts are `registered` rather than pending confirmation. Planned Semesters 5-6 remain undated, while current-semester results remain unpublished. `npm run check:all` passes with direct-token audit, lint, 162 unit tests, production and bundle checks, and all 54 Playwright desktop runs across 1280px and 1440px. The largest JavaScript chunk is 340,900 bytes; eager JavaScript is 190,175 gzip bytes; Performance chart is 100,194 gzip bytes; Calendar is 3,350 gzip bytes; Academic Performance is 2,211 gzip bytes; and Academic Modules is 1,193 gzip bytes. The existing six Fast Refresh warnings and Playwright `FORCE_COLOR`/`NO_COLOR` warnings remain non-blocking.
- Study Plan module tables now use the shared `DataTable` fixed layout, so the independently expanded semester rows keep the same columns: Status is 120px, Credits and Grade are 80px, and the remaining space serves Module and Prerequisites. The focused component test, production build, and Study Plan desktop flow passed at 1280px and 1440px.
- Study Plan prerequisite cells now mirror Module hierarchy: their code and minimum grade label appears above the prerequisite module name. The focused component test, production build, and Study Plan desktop flow passed at 1280px and 1440px.
- The main header has been reverted from the programme-and-year summary to the shared route breadcrumb. It follows the active top-level page and supersedes the older summary-header note above. The production build and focused Dashboard-to-Finance desktop flow passed at 1280px and 1440px.
- The Study Plan Programme Overview now places its existing no-op Download Transcript primary Button in its own right-hand Programme Status column, above the graduation-eligibility requirements. The focused Study Plan component test, production build, and desktop Study Plan flow verify the placement without changing the action's frontend-only scope.
- Study Plan semester triggers retain the original beside-name four-metric layout where their own component width permits. A component-width breakpoint moves the same metrics to a full-width strip below the title in constrained portal columns; Credits Attempted remains divider-free, while later metrics retain shared-border dividers.
- Study Plan metric labels reserve a common two-line area only after the responsive compact strip activates: labels then top-align even when one wraps, and values align on the same row below without adding space to the normal beside-name layout.
- The Study Plan Programme Overview wrapper now uses `surface-subtle` instead of the darkest `background` tier.
- Performance chart loading skeletons now match their loaded chart cards by using `surface-subtle` rather than `background`.
- Shared `SegmentedTabs` containers use `surface-subtle` for Module Detail local-view controls. Community no longer uses a segmented tab switch.
- Academic routes now share an `AcademicContextBar` between the Academic title and tabbed panel. Its dedicated `GET /api/v1/academic/context` contract and canonical mock projection expose only Faculty, Programme, and current programme semester; the UI derives Year from the two-semesters-per-year rule. The refined borderless `surface` banner is a compact icon-plus-programme identity composition: its faded-brand-blue icon precedes a muted body line of `Faculty · Year (Semester)`, with the programme as the title below. It has no nested panel, divider, pill, redundant label, or local font-weight override.
- Academic Context verification completed direct-token audit, lint with the existing six Fast Refresh warnings only, 37 focused contract/API/Explorer/component tests, production build, production-asset and bundle checks, plus the shared Academic Performance, Modules, and Timetable browser flows at 1280px and 1440px (six browser runs). The full `npm run check:all` and full-suite reruns were started, but this terminal ends a serial Playwright/Vitest run after 30 seconds before its final summary; no test failure was reported before that host limit.
- The Academic Context visual refinement passed its two focused component tests, a production build, and the Academic Performance browser flow at both 1280px and 1440px. The programme title remains fully visible rather than being clipped in the wide side-by-side treatment.
- The completed Academic Study Plan, shared Academic Context, and design-system refinement batch passes the full `npm run check:all` gate: direct-token audit, lint, 167 unit tests, production and bundle checks, and all 54 Playwright desktop runs at 1280px and 1440px. Current bundle budgets remain within limits: largest chunk 340,900 bytes; eager JavaScript 191,024 gzip bytes; Performance chart 100,192 gzip bytes; Calendar 3,371 gzip bytes; Academic Performance 2,209 gzip bytes; and Academic Modules 1,191 gzip bytes.
- Warning cleanup separates shared variant factories, sidebar context, and Person Stack formatting helpers from their component modules, so lint is now warning-free. The cross-platform `scripts/run-playwright.mjs` launcher removes conflicting terminal colour variables before starting Playwright workers and the Vite server; browser checks no longer emit the `FORCE_COLOR`/`NO_COLOR` warning. Mixed CRLF/LF source files were normalized to the repository's required LF form. The clean final `npm run check:all` gate passes 167 unit tests and all 54 desktop browser runs; current eager JavaScript is 191,467 gzip bytes, with the largest chunk at 340,900 bytes.
- Academic Performance, Modules, Timetable, and Attendance now use one future-semester policy. All six programme semesters remain selectable through the shared selector; future Semesters 5 and 6 use muted dropdown/trigger text and render a shared illustrated, context-specific `EmptyState`. Performance defaults to the latest published Semester 3, treats current Semester 4 as unpublished, and keeps the GPA popup published-only. Future Timetable and Attendance requests preserve the selected semester and return empty collections instead of falling back to Semester 4.
- The canonical journey no longer fabricates future student registrations. Semesters 5 and 6 keep their Curriculum Module recommendations in Study Plan but have no Module Registration, Enrolment, student timetable, or module attendance route until registration exists. Contracts, mock projections, Portal API behaviour, Data Model Explorer metadata, Design System Explorer examples, documentation, unit coverage, and desktop browser flows were synchronized.
- The completed semester-consistency work passes `npm run check:all`: direct-token audit, warning-free lint, 169 unit tests, production and bundle checks, and all 54 Playwright desktop runs at 1280px and 1440px. Bundle budgets remain within limits: largest chunk 340,900 bytes; eager JavaScript 191,541 gzip bytes; Performance chart 100,194 gzip bytes; Calendar 3,293 gzip bytes; Academic Performance 2,452 gzip bytes; and Academic Modules 1,300 gzip bytes.
- Shared Module Cards, Community sidebar module rows, and module chat headers place the API-supplied module code in the shared spaced muted `type-label` style above the module name. Module Card online-class indicators align beside the code at the top edge rather than beside the title. The module chat header resolves its code through the stable Module ID; members panes resolve exact Module Offering membership, while Community card badges now show derived unread messages rather than membership totals.
- Module Details now wraps its module-code and module-name identity block in the existing bordered `InnerSection`, matching the nested grouping treatment used for the outline and read-only metadata.
- The canonical record graph now holds two students without copying the institution catalogue. Alya Putri is a complete Indonesian first-semester student whose rolling Semester 1 starts six campus-calendar days after the mock session is created; she owns a profile, emergency contact, visa/pass, Programme Enrolment, six Study Periods, registration issue, and empty Finance Account. She has no Module Registrations, results, attendance, learning progress, submissions, or invoices before registration. Rizal Hakim remains the reconciled Semester 4 historical scenario.
- The portal now starts with Alya through a `PortalRuntimeProvider`. The development-only `/dev/scenarios` screen can replace the in-memory `MockPortalApi` with Rizal or Alya and clears scenario-scoped TanStack Query caches. Selection resets on refresh and does not add local storage, authentication, a live API, or a database. The Playwright server explicitly starts with Rizal so the established regression suite remains stable; a dedicated browser flow verifies switching to Alya.
- Student-specific API projections now start from the active stable student ID and traverse Programme Enrolment, Study Period, Module Registration, and Finance Account ownership. Finance is stored as normalized canonical record collections and projected into a selected-student statement. Alya receives truthful empty Performance, Modules, Timetable, Attendance, and finance data; Dashboard Calendar contains only her canonical registration period. Her Profile and Study Plan remain complete. Module Details is unavailable until she owns a registration.
- The Data Model Explorer now opens on a database-style Records tab with logical snake-case table names, actual validated rows, row counts, PK/FK hints, clickable foreign keys, and All/Alya/Rizal filters. Fields, Relationships, Student Timeline, and Row JSON remain available. This remains a frontend logical contract, not a prescribed backend schema.
- The reusable illustrated `EmptyState` now covers Alya's empty Dashboard modules, performance, and timetable as well as existing Academic future-semester states. Module Registration and Graduation remain placeholders; the next approved feature is the actual Module Registration experience for Alya's registration week.
- The completed multi-student foundation passes `npm run check:all`: direct-token audit, warning-free lint, 172 unit tests, production and bundle checks, and all 56 Playwright desktop runs at 1280px and 1440px. Bundle budgets remain unchanged and pass with a 340,900-byte largest chunk, 193,532 eager gzip bytes, 100,193-byte Performance chart gzip, 3,293-byte Calendar gzip, 2,435-byte Academic Performance gzip, and 1,350-byte Academic Modules gzip.
- Shared Profile account issues now use the `CardGrid` issue variant with a four-column maximum. At narrower panel widths, its 240px readable card minimum and shared 16px gaps reduce the row to three, two, or one columns without horizontal overflow; the Design System Explorer receives the same shared variant automatically.
- Alya's active Module Registration Profile issue now uses the accessible `IssueCard` action to open Academic > Module Registration. Fee and visa issue summaries remain non-actionable because their workflows are still outside the approved frontend scope.
- Alya's registration-week Community does not borrow Rizal's private-message directory, meetup, or attendee fixtures. Modules, Messages, and Upcoming Meetups use the compact shared `SidebarEmptyState`: muted body text only, left-aligned inside a transparent bordered wrapper with 12px padding and no icon or description. Her programme conversation is available immediately through programme membership.
- The Alya Community empty-data/API refinement passes `npm run check:all`: direct-token audit, warning-free lint, 173 unit tests, production and bundle checks, and all 56 Playwright desktop runs at 1280px and 1440px. Bundle budgets remain within limits: largest chunk 340,900 bytes; eager JavaScript 193,598 gzip bytes; Performance chart 100,194 gzip bytes; Calendar 3,291 gzip bytes; Academic Performance 2,437 gzip bytes; and Academic Modules 1,351 gzip bytes. The subsequent border-only empty-state visual refinement passed its focused unit test, production build, and the Alya scenario browser flow at both desktop widths.
- Community membership is now a normalized, canonical read model rather than independent display counts. The programme group derives 11 actively enrolled students across programme years plus five lecturers assigned anywhere in the programme curriculum; both Rizal and newly enrolled Alya therefore see the same truthful 16-member programme roster. The five current module groups derive only confirmed registrations in each exact Module Offering plus its assigned lecturers. Thomas Hassan's active DMP201 second attempt demonstrates that an active repeat registration grants access only to that offering.
- `CommunityResponse` returns the current person plus safe people, groups, and memberships. Stable Community Group, Programme, Module Offering, semester, person, and service IDs drive chat contexts; `ConversationSummary` records independently drive unread badges and the private Messages directory. Alya can read and message the programme group immediately while her direct-message directory remains empty.
- The Data Model Explorer now exposes Community People, Community Groups, and Community Memberships as validated logical tables with their Programme, Module, Module Offering, Semester, and Person relationships. The canonical graph includes nine non-selectable peer students across Semesters 2, 4, and 6, with only the academic history needed for roster eligibility; it does not create unrelated finance or attendance workflows for them.
- The completed community-hierarchy work passes `npm run check:all`: direct-token audit, warning-free lint, 177 unit tests, production and bundle checks, and all 58 Playwright desktop runs at 1280px and 1440px. Bundle budgets remain within limits: largest chunk 340,900 bytes; eager JavaScript 194,507 gzip bytes; Performance chart 100,192 gzip bytes; Calendar 3,291 gzip bytes; Academic Performance 2,434 gzip bytes; and Academic Modules 1,352 gzip bytes.
- Community and chat now use one `Community` root heading with no segmented tab switch. Programme, module, member, personal-message, and service selections replace that root and Back unwinds the active layer. Programme and module badges are derived unread-message counts that exclude the current student's own messages; opening a conversation clears only that conversation for the active frontend session.
- Chat data is normalized into Conversation, Conversation Participant, Message, and Message Attachment records behind the replaceable `PortalApi`. Sidebar summaries are queried separately from one active conversation detail, and first-contact personal conversations enter the Messages directory only after the student sends the first message. The API rejects self-messaging and ineligible group targets; member lists retain the current student as a disabled row, order lecturers before students, and label Lecturer, Student, and You roles explicitly.
- Chat composition remains frontend-only and reset-on-refresh. Per-conversation drafts, selected attachments, and scroll positions survive sidebar layer changes during the session. The existing attachment count, per-file, total-size, and safe-inline-preview policy is unchanged.
- Chat implementation is deferred from the eager graph, preloaded during browser idle time or interaction intent, loads only the selected conversation detail, memoizes message and participant projections, and defers the image lightbox until opened. The bundle gate now enforces a 20 KiB gzip cap for the chat entry; the verified entry is 4,714 gzip bytes.
- The completed chat-flow and optimization work passes `npm run check:all`: direct-token audit, warning-free lint, 182 unit tests, production and bundle checks, and all 58 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 194,460 eager gzip bytes, 100,193-byte Performance chart gzip, 3,293-byte Calendar gzip, 4,714-byte Chat gzip, 2,434-byte Academic Performance gzip, and 1,349-byte Academic Modules gzip.
- `EmptyState` now has a documented `surface` variant with the shared 12px radius. Dashboard Timetable, Modules, and Academic Performance use it so valid no-data cards sit above the page background; the default `surface-subtle` variant remains in use for quieter future-semester Academic states and the Design System Explorer.
- The dashboard empty-state refinement passes `npm run check:all`: direct-token audit, warning-free lint, 183 unit tests, production and bundle checks, and all 60 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 194,481 eager gzip bytes, 100,191-byte Performance chart gzip, 3,293-byte Calendar gzip, 4,726-byte Chat gzip, 2,435-byte Academic Performance gzip, and 1,349-byte Academic Modules gzip.
- Calendar now includes Registration as a sixth validated event kind. Its purple `registration` token is documented and visible in the Design System Explorer; the centred legend, event markers, icon map, API contract, and Data Model Explorer use the same category. The mock adapter derives the full opening-to-closing interval from the selected student's canonical Study Period, so Rizal and Alya receive their own semester-specific registration period without screen-level fixture duplication.
- The registration-calendar refinement passes `npm run check:all`: direct-token audit, warning-free lint, 183 unit tests, production and bundle checks, and all 60 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 194,535 eager gzip bytes, 100,193-byte Performance chart gzip, 3,325-byte Calendar gzip, 4,723-byte Chat gzip, 2,430-byte Academic Performance gzip, and 1,347-byte Academic Modules gzip.
- Registration is now represented as the complete Study Period interval rather than a closing-date point. The student-facing Semester DTO carries its owning Study Period's opening and closing timestamps; Calendar navigation begins at registration opening, every covered date is selectable and receives the purple marker, and an interval crossing a month boundary is returned in both affected month queries. Registration agenda details show the full opening-to-closing range and remain read-only.
- The completed registration-window correction passes `npm run check:all`: direct-token audit, warning-free lint, 185 unit tests, production and bundle checks, and all 60 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass unchanged with a 340,900-byte largest chunk, 194,811 eager gzip bytes, 100,194-byte Performance chart gzip, 3,330-byte Calendar gzip, 4,726-byte Chat gzip, 2,437-byte Academic Performance gzip, and 1,348-byte Academic Modules gzip.
- Dashboard sections now render in this deliberate sequence: News and Announcements, Academic Performance, Timetable, Calendar, then Modules. The stable-URL dashboard browser flow asserts that order at both supported desktop widths.
- The dashboard-order refinement passes `npm run check:all`: direct-token audit, warning-free lint, 185 unit tests, production and bundle checks, and all 60 Playwright desktop runs at 1280px and 1440px. Bundle budgets remain within limits: largest chunk 340,900 bytes; eager JavaScript 194,812 gzip bytes; Performance chart 100,192 gzip bytes; Calendar 3,329 gzip bytes; Chat 4,723 gzip bytes; Academic Performance 2,435 gzip bytes; and Academic Modules 1,347 gzip bytes.
- Academic Module Registration is now a completed lazy-loaded route for Alya's rolling Semester 1 registration week. It starts with five recommended eligible four-credit modules, supports separate Module and Class Section selections including alternate sections, validates a 12-credit minimum and 20-credit standard maximum, and keeps list removals, Monday-to-Sunday timetable removals, the segmented credit bar, reset, and confirmation popup synchronized from one draft.
- Programme Version now owns minimum and maximum semester credits. A canonical Credit Load Override records the exact Study Period, approved maximum, reason, approver, timestamp, and status; Rizal's historical Semester 3 load therefore has an explicit approved 24-credit exception rather than weakening the normal 20-credit policy. The Data Model Explorer exposes this logical table, relationships, per-student filter, and registration endpoint usage.
- `PortalApi.getAcademicModuleRegistration()` derives candidates, prerequisites, lecturers, and schedules from the canonical graph. Its session-only fee-preview and first-confirmation operations validate the academic selection plus Finance ownership and commit registrations, separate tuition/registration invoices, award credits, and issue resolution as one graph transaction. Confirmed periods are read-only; the Alya modules immediately gain valid Summary, upcoming Materials, empty Attendance, exact timetable, Finance charges, and offering-specific community projections. Refresh restores the original fixture; student-view switching retains the canonical transaction in the current session.
- Shared `StatusIndicator`, `CreditLoadBar`, `AddRowButton`, and `RemoveRowButton` patterns now serve Study Plan, Profile, and Module Registration and appear in the Design System Explorer. `TimetableCard` has a registration mode with brand class blocks, accessible X actions, all seven days, and standard empty periods without add/prohibited controls.
- The Academic shell itself is now deferred from the dashboard entry in addition to its individual tab bodies. This absorbed the new registration flow without relaxing bundle limits: the registration entry is 4,478 gzip bytes and eager JavaScript decreased to 191,767 gzip bytes.
- The completed Module Registration work passes `npm run check:all`: direct-token audit, warning-free lint, 191 unit tests, production and bundle checks, and all 60 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 191,767 eager gzip bytes, 100,193-byte Performance chart gzip, 3,328-byte Calendar gzip, 4,712-byte Chat gzip, 2,436-byte Academic Performance gzip, 1,349-byte Academic Modules gzip, and 4,478-byte Academic Module Registration gzip.
- Module Registration now preserves the current class section when its already-selected module is chosen again. Incomplete, duplicate, stale, prerequisite-blocked, clashing, and out-of-policy drafts expose guarded row/form errors; hidden confirmation content no longer assumes every draft row resolves to an offering. The shared `DropdownSelect` has a documented accessible invalid state, and focused unit plus both desktop browser regressions cover the original reselection crash.
- The hardened Module Registration flow passes `npm run check:all`: direct-token audit, warning-free lint, 194 unit tests, production and bundle checks, and all 60 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 191,761 eager gzip bytes, 100,193-byte Performance chart gzip, 3,329-byte Calendar gzip, 4,715-byte Chat gzip, 2,438-byte Academic Performance gzip, 1,349-byte Academic Modules gzip, and 4,628-byte Academic Module Registration gzip.
- Module Registration's editable field rows are now a semantic `DataTable` with Module Name, Code, Section, Lecturer, Mode, Credits, and an independent trash action. The title-row Add Module button and clickable/keyboard-accessible table rows share one controlled `Popup` for add/edit; popup drafts are isolated until saved, derived fields use the shared read-only Input treatment, and minimum credits remain a final-confirmation rule rather than blocking incremental additions. Table removals, edits, timetable removals, the credit bar, reset, and confirmation remain synchronized.
- The completed Module Registration table-and-popup refinement passes `npm run check:all`: direct-token audit, warning-free lint, 199 unit tests, production and bundle checks, and all 60 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 191,814 eager gzip bytes, 100,194-byte Performance chart gzip, 3,329-byte Calendar gzip, 4,713-byte Chat gzip, 2,437-byte Academic Performance gzip, 1,348-byte Academic Modules gzip, and 5,355-byte Academic Module Registration gzip.
- Module Registration suppresses only its final table-row divider and horizontally centres the compact numeric Credits column. The shared `DataTable` exposes a bounded final-divider option and direct centre alignment for contained tables without changing their default dividers. Focused unit coverage, a production build, and the registration browser flow pass at both supported desktop widths.
- `DataTable` now has a reusable `bordered` variant for contained record tables. It owns the unpadded rounded border, applies `surface-raised` only to its header row, then applies 8px header padding, 12px-by-8px body-cell padding, and 16px outer-column insets. Module Registration uses it directly after removing its ad hoc padded wrapper; the default calculation-table treatment remains unchanged. The Design System Explorer shows both variants.
- Module Registration's Semester Start, End, Duration, and Credit Hours summary columns now match the Study Plan Programme Overview: a flexible content-hugging row with a shared 24px gap, muted labels above medium title-colour values, and no metric dividers. The registration-specific credit load bar remains below the metrics.
- Module Registration no longer models section capacity or remaining seats. The canonical Module Offering, route-facing registration contract, fixtures, mock API projection, validation model, Data Model Explorer, docs, and tests now treat every returned section as unlimited; prerequisite, duplicate-module, credit-policy, stale-section, and timetable-clash guards remain intact.
- Add/Edit Module now derives every recurring Class Schedule owned by the selected Module Offering and renders it through shared read-only Inputs. Schedules are ordered Monday-to-Sunday and by start time; physical and hybrid sections show Day, 24-hour Time, and Location, online sections show only Day and Time, multi-session sections receive Class 1/Class 2 labels, and unscheduled sections state `Schedule not confirmed`.
- The seat-free registration and schedule-detail work passes `npm run check:all`: direct-token audit, warning-free lint, 202 unit tests, production and bundle checks, and all 60 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 191,816 eager gzip bytes, 100,192-byte Performance chart gzip, 3,328-byte Calendar gzip, 4,713-byte Chat gzip, 2,432-byte Academic Performance gzip, 1,346-byte Academic Modules gzip, and 5,573-byte Academic Module Registration gzip. The only observed warning was the existing Playwright `NO_COLOR`/`FORCE_COLOR` environment warning during the direct focused browser run; the repository runner completed without it.
- Add/Edit Module now aligns Mode/Credits at 50/50 and physical Day/Time/Location at 25/25/50 through matching four-column grids. Single-digit 24-hour schedule values omit the leading zero (`9:00`, not `09:00`); online classes retain a full-width two-column Day/Time row.
- Module Registration now combines current-semester recommendations with unresolved failures from the active Programme Enrolment. A failed Module appears first as `(Required Repeat)` only when it has a current-term campus offering; any passing Module Result resolves that requirement while preserving prior attempt history. Initial drafts include eligible repeats before fitting recommended modules within the effective limit, confirmation cannot omit them, and the mutation creates the correct next attempt with `registrationType: repeat`.
- Recommended modules blocked by a failed prerequisite remain visible and disabled with the exact minimum-grade reason. Selecting the prerequisite repeat in the same draft does not satisfy it concurrently. The route contract, mock API, model validation, Data Model Explorer endpoint metadata, project/data/feature docs, and tests are synchronized; Explorer usage points only to the Module Result and Grade Band fields actually read by registration derivation.
- The required-repeat and popup-layout work passes `npm run check:all`: direct-token audit, warning-free lint, 205 unit tests, production and bundle checks, and all 60 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 191,811 eager gzip bytes, 100,193-byte Performance chart gzip, 3,327-byte Calendar gzip, 4,711-byte Chat gzip, 2,437-byte Academic Performance gzip, 1,347-byte Academic Modules gzip, and 5,765-byte Academic Module Registration gzip. No warnings remain.
- Audit cleanup now identifies Module Registration as complete in the Academic Performance behaviour document and removes the remaining mojibake from the Module Details schedule, Data Model Explorer weekday type, and logical-data-model prose. Focused Module Details/Data Model verification passes 24 unit tests, the production build passes, and a repository-wide encoding/status rescan finds no remaining audited strings.
- Standard shared `Button` actions now use one 36px height for default and large labelled actions, and the standard square icon size is also 36px. Explicit compact icon-only and inline link treatments remain intentional exceptions. The Design System Explorer and Module Registration browser flow assert the shared height at both desktop widths. `npm run check:all` passes the direct-token audit, warning-free lint, 205 unit tests, production and bundle checks, and all 60 Playwright runs; bundle budgets pass with a 340,900-byte largest chunk, 191,814 eager gzip bytes, 100,194-byte Performance chart gzip, 3,327-byte Calendar gzip, 4,713-byte Chat gzip, 2,439-byte Academic Performance gzip, 1,348-byte Academic Modules gzip, and 5,766-byte Academic Module Registration gzip.
- Module Registration's confirmation total now uses the shared `InnerSection` with the quiet `surface-subtle` background, keeping the total visually grouped beneath the selected-module list without changing registration behavior.
- The completed confirmation-total refinement passes `npm run check:all`: direct-token audit, warning-free lint, 205 unit tests, production and bundle checks, and all 60 Playwright runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 191,815 eager gzip bytes, 100,194-byte Performance chart gzip, 3,329-byte Calendar gzip, 4,713-byte Chat gzip, 2,437-byte Academic Performance gzip, 1,349-byte Academic Modules gzip, and 5,784-byte Academic Module Registration gzip. Live browser inspection confirms the intended background, shared border, 8px radius, and 12px padding.
- Study Plan no longer expands the current semester on first load: every semester starts collapsed, while the shared single Accordion still permits one selected semester at a time and permits collapsing it again. The controlled empty initial value avoids an uncontrolled-to-controlled Accordion warning.
- The collapsed Study Plan refinement passes `npm run check:all`: direct-token audit, warning-free lint, 205 unit tests, production and bundle checks, and all 60 Playwright runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 191,812 eager gzip bytes, 100,194-byte Performance chart gzip, 3,327-byte Calendar gzip, 4,712-byte Chat gzip, 2,437-byte Academic Performance gzip, 1,349-byte Academic Modules gzip, and 5,785-byte Academic Module Registration gzip.
- Closed Module Registration feedback now uses the shared default `surface-subtle` `EmptyState`. Its completed state retains the Study Plan action through the component's optional action slot; the missed state uses the same pattern. Shared Popup footer actions now flex across the full available row width, splitting it equally when both a primary and dismissal action are present.
- The combined Study Plan, EmptyState, and Popup-footer refinements pass `npm run check:all`: direct-token audit, warning-free lint, 206 unit tests, production and bundle checks, and all 62 Playwright runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 191,811 eager gzip bytes, 100,192-byte Performance chart gzip, 3,328-byte Calendar gzip, 4,711-byte Chat gzip, 2,435-byte Academic Performance gzip, 1,351-byte Academic Modules gzip, and 5,886-byte Academic Module Registration gzip.
- The Module Registration confirmation total retains the shared `InnerSection` spacing and `surface-subtle` fill but removes its border, separating it quietly from the bordered module list above. The production build and focused registration flow pass at 1280px and 1440px; the direct focused browser command emits only the existing `NO_COLOR`/`FORCE_COLOR` environment warning.
- Alya's registration-week Dashboard Timetable and Modules empty states now each provide a lightweight `Go to Module Registration` action through the shared `EmptyState` action slot. Both use the normal in-app page handler, so they open the completed Module Registration route without introducing new data or persistence behaviour.
- The dashboard registration-action refinement passes the full quality gate: direct-token audit, warning-free lint, 206 unit tests, production and bundle checks, and all 62 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 191,837 eager gzip bytes, 100,194-byte Performance chart gzip, 3,328-byte Calendar gzip, 4,714-byte Chat gzip, 2,436-byte Academic Performance gzip, 1,348-byte Academic Modules gzip, and 5,892-byte Academic Module Registration gzip.
- Profile now owns one shared route-level summary and active-issue composition above its `TabbedPagePanel`. The strip retains the student's avatar, name, faculty, and programme on the page-level `surface` tier; active issues remain visible and retain their existing actions across Personal, Academic, Resume, and Portfolio. The existing Personal Profile query is lifted to the Profile shell and passed into Personal, so no new API contract, fixture, persistence, or cache path is introduced. Personal tab content now begins directly with Personal Details and its detail sections.
- The shared Profile-shell refinement passes `npm run check:all`: direct-token audit, warning-free lint, 206 unit tests, production and bundle checks, and all 62 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 191,899 eager gzip bytes, 100,194-byte Performance chart gzip, 3,328-byte Calendar gzip, 4,714-byte Chat gzip, 2,439-byte Academic Performance gzip, 1,350-byte Academic Modules gzip, and 5,889-byte Academic Module Registration gzip.
- The shared Profile summary now uses the existing Academic Context query for the current semester. Below the name divider, faculty and programme form a left column beside a right-aligned large, `surface-elevated` `Semester N` Badge; the completed summary remains shared across every Profile tab. The Design System Explorer shows the same elevated large Badge treatment. No Profile contract, fixture, persistence, or new API endpoint was added.
- The Profile semester-summary refinement passes `npm run check:all`: direct-token audit, warning-free lint, 206 unit tests, production and bundle checks, and all 62 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 191,885 eager gzip bytes, 100,193-byte Performance chart gzip, 3,329-byte Calendar gzip, 4,714-byte Chat gzip, 2,439-byte Academic Performance gzip, 1,352-byte Academic Modules gzip, and 5,889-byte Academic Module Registration gzip.
- The Profile current-semester Badge explicitly self-centres vertically against the faculty/programme column when that programme text wraps. It remains a large, right-aligned `surface-elevated` Badge and adds no contract or interaction change.
- The vertical-centering refinement passes the production build and the focused Profile Playwright flow at 1280px and 1440px (two runs). The change-surface audit reports no companion-layer gaps; no warnings were emitted.
- `StudentSummaryCard` now owns the avatar, student name, divider, faculty/programme columns, and current-semester Badge shared above every Profile and Academic tab. Academic pages no longer use the graduation-cap/derived-Year banner. The existing Academic Context boundary now returns only the selected student's stable ID, display name, nullable avatar URL, programme identity, and current Study Period required by the card; no new API method, query key, persistence path, or private Profile data dependency was added.
- The shared Student Summary Card refinement passes `npm run check:all`: direct-token audit, warning-free lint, 207 unit tests, production and bundle checks, and all 64 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 191,890 eager gzip bytes, 100,194-byte Performance chart gzip, 3,326-byte Calendar gzip, 4,713-byte Chat gzip, 2,439-byte Academic Performance gzip, 1,348-byte Academic Modules gzip, and 5,890-byte Academic Module Registration gzip. The change-surface audit reports no companion-layer gaps; no warnings were emitted.
- Profile > Academic now displays the selected student's retained previous qualifications and formal English test results through a dedicated read-only `AcademicProfileResponse`. Canonical Academic Qualification and English Proficiency records belong to stable Student Profile IDs; the mock projection orders them newest first and never leaks records across Rizal and Alya. The tab composes bordered record sections from the existing `DetailSectionCard`, `DetailField`, `FileLink`, and `EmptyState` patterns, supports multiple qualifications plus optional translations and grading scales, and leaves every document action as an accessible no-op placeholder. Resume and Portfolio were outside that change; no mutation, admission-rule engine, storage, upload, or functional download was added to Academic Profile.
- The completed Profile Academic qualification work passes `npm run check:all`: direct-token audit, warning-free lint, 213 unit tests, production and bundle checks, and all 66 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 191,982 eager gzip bytes, 100,193-byte Performance chart gzip, 3,328-byte Calendar gzip, 4,709-byte Chat gzip, 2,437-byte Academic Performance gzip, 1,349-byte Academic Modules gzip, and 5,888-byte Academic Module Registration gzip. The change-surface audit reports no companion-layer gaps. The normal full runner emitted no warnings; the direct focused Playwright command emitted only the existing `NO_COLOR`/`FORCE_COLOR` environment warning.
- Profile Academic section-level titles use the same measured 32px title-to-card rhythm as Personal Details, while multiple records retain a 16px gap. Live browser inspection confirmed the 32px spacing on both pages. The focused component suite passed 4 tests, the production build passed, and both affected Playwright flows passed all 4 runs at 1280px and 1440px; the focused Playwright command emitted only the existing `NO_COLOR`/`FORCE_COLOR` environment warning.
- Shared `IssueCard` controls now use an exact 12px radius, including the Profile Module Registration action and module-level warning/destructive issue cards. The component and desktop Profile action regression coverage assert the radius. This shared visual update passes `npm run check:all`: direct-token audit, warning-free lint, 213 unit tests, production and bundle checks, and all 66 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 191,990 eager gzip bytes, 100,194-byte Performance chart gzip, 3,328-byte Calendar gzip, 4,713-byte Chat gzip, 2,438-byte Academic Performance gzip, 1,348-byte Academic Modules gzip, and 5,889-byte Academic Module Registration gzip. The normal full runner emitted no warnings; focused Playwright emitted only the existing `NO_COLOR`/`FORCE_COLOR` environment warning.
- Profile Academic now adds numbered shared inner-section headers only when the student has multiple retained academic qualifications: Rizal sees `Qualification 1` and `Qualification 2`, while Alya's single qualification remains headerless. English Proficiency records remain headerless. The focused component suite passes all 5 tests, the production build passes, and the Rizal/Alya Playwright flows pass all 4 runs at 1280px and 1440px. Focused lint is clean; Playwright emitted only the existing `NO_COLOR`/`FORCE_COLOR` environment warning.
- Profile > Resume Phase 1 is now implemented inside the shared Profile shell. It provides a changed-only inline Professional Summary save plus bordered Work Experience, Education, Skills, and Awards & Recognition tables. Add and selectable edit rows reuse controlled full-width-action Popups; trash actions use a separate confirmation Popup. Records validate required values, ISO `YYYY-MM` ranges, nullable Present/end values, optional score, and recognition year before a complete session-only Resume replacement.
- Canonical Student Resume, Resume Work Experience, Resume Education, Resume Skill, and Resume Recognition records now belong to the selected stable Student Profile/Resume chain. `ResumeProfileResponse`, `UpdateResumeProfileInput`, logical GET/PATCH Portal API methods, scenario-scoped queries, cache replacement, Data Model Explorer ownership/filtering, fixtures, docs, and tests are synchronized. Rizal is populated and Alya has a null summary with empty addable collections. Resume Education remains deliberately independent from Profile Academic Qualifications.
- The shared `Textarea` now matches Input's semantic field states for long-form content and appears in the Design System Explorer with default, read-only, invalid, and disabled examples. Resume preview/templates/settings, qualification import, PDF generation/export, downloads, uploads, and persistent storage remain deferred.
- The completed Resume Phase 1 change passes the change-surface audit with no companion-layer gaps and `npm run check:all`: direct-token audit, warning-free lint, 223 unit tests, production and bundle checks, and all 68 Playwright runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 192,233 eager gzip bytes, 100,193-byte Performance chart gzip, 3,326-byte Calendar gzip, 4,713-byte Chat gzip, 2,436-byte Academic Performance gzip, 1,349-byte Academic Modules gzip, and 5,882-byte Academic Module Registration gzip. No warnings were emitted. An initial full-gate attempt exposed only a TypeScript test-helper inference issue; it was corrected before the clean rerun.
- Resume's empty Work Experience, Education, Skills, and Awards & Recognition sections now compose the shared `EmptyState` on the `surface-subtle` tier rather than rendering table-empty copy. Their Add actions remain available and now use the secondary Button variant; populated sections retain their bordered DataTables and every popup save action remains primary. Focused Resume unit coverage (7 tests), the Alya first-semester browser flow at 1280px and 1440px (2 runs), and the production build pass. The change-surface audit reports no companion-layer gaps. No warnings were emitted.
- `DatePicker` is now the reusable controlled full-date field for Resume popups. Its raised-surface trigger, calendar icon, invalid/disabled treatment, `surface-elevated` DayPicker popover, clear action, Escape dismissal, and focus return are shown in the Design System Explorer. Resume Work Experience and Education now use `YYYY-MM-DD` Start/End Date values; every existing fixture month was converted to its first day, a blank end date remains `Present`, and the session-only GET/PATCH/cache contract and current-first ordering remain unchanged. The full quality gate passes with direct-token audit, warning-free lint, 227 unit tests, production and bundle checks, and all 68 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 192,316 eager gzip bytes, 100,193-byte Performance chart gzip, 3,212-byte Calendar gzip, 4,711-byte Chat gzip, 2,436-byte Academic Performance gzip, 1,349-byte Academic Modules gzip, and 5,884-byte Academic Module Registration gzip. The change-surface audit reports no companion-layer gaps; no warnings were emitted.
- The DatePicker-specific Calendar variant now anchors previous/next navigation at opposite ends of the month header instead of allowing the controls to stack vertically. Enabled day buttons use `surface-hover` on hover, `surface-elevated` while pressed, and a solid brand selected state with white text. Dashboard and Attendance Calendar variants are unchanged. The change-surface audit reports no companion-layer gaps, and `npm run check:all` passes the direct-token audit, warning-free lint, 228 unit tests, production and bundle checks, and all 68 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 192,306 eager gzip bytes, 100,194-byte Performance chart gzip, 3,211-byte Calendar gzip, 4,715-byte Chat gzip, 2,439-byte Academic Performance gzip, 1,351-byte Academic Modules gzip, and 5,883-byte Academic Module Registration gzip. No warnings were emitted.
- Profile Resume's four secondary section-level Add actions now include a leading, decorative plus icon while retaining their accessible labels and existing Popup behavior. Focused Resume component coverage (7 tests), warning-free lint, the production build, and the Alya scenario browser flow at 1280px and 1440px pass. The focused Playwright server emitted only the existing `NO_COLOR`/`FORCE_COLOR` environment warning.
- The panel type of `ContentSection` provides one stable portal-wide panel hierarchy: its action and no-action states both reserve a 36px header row, keep the existing 16px title-to-content gap, and expose a generic header slot plus action-state marker for layout verification. Sibling section stacks retain 32px gaps. The Design System Explorer presents both variants together with identical title positioning.
- Profile Personal now places Personal Details and its existing cards/actions inside one panel-type `ContentSection`; its inner groups remain 32px apart. Profile Academic uses separate Academic Qualifications and English Proficiency panel sections with 32px between them and 16px to records, superseding the older 32px title-to-card note above. Resume retains its five shared sections. Academic Performance, Modules, Timetable, Attendance, Study Plan, and Module Registration inherit the same stable header geometry, and their layout-matched loading states reserve it.
- The unified-section change-surface audit reports no companion-layer gaps. `npm run check:all` passes the direct-token audit, warning-free lint, 228 unit tests, production and bundle checks, and all 68 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 192,316 eager gzip bytes, 100,194-byte Performance chart gzip, 3,209-byte Calendar gzip, 4,713-byte Chat gzip, 2,454-byte Academic Performance gzip, 1,364-byte Academic Modules gzip, and 5,949-byte Academic Module Registration gzip. No warnings were emitted.
- `ContentSection` is now the only shared section component. Its required `type="page"` branch preserves the Dashboard's 18px title, 32px content gap, plain/boxed behavior, custom heading, and community-sidebar variant; its `type="panel"` branch preserves the 16px title, stable 36px action/no-action header, direct content, and 16px content gap. The retired `PageSection` and `PanelSection` components were removed, while the discriminated TypeScript interface prevents page-only props from being passed to panel sections.
- Dashboard, Dashboard loading fallbacks, community sidebar, Profile, Resume, and all completed Academic routes now consume the same component with explicit types. Generic `content-section` slots expose the type and action state; the Design System Explorer shows the page treatment beside both panel header states. No contract, canonical data, Portal API/query, route, persistence, or feature behavior changed.
- The consolidated Content Section change-surface audit reports no companion-layer gaps. Focused component/consumer coverage passes 15 tests; focused Dashboard, Profile, Academic, Resume, and Explorer browser coverage passes all 12 runs at 1280px and 1440px. `npm run check:all` passes the direct-token audit, warning-free lint, 230 unit tests, production and bundle checks, and all 68 Playwright desktop runs. Bundle budgets pass with a 340,900-byte largest chunk, 192,462 eager gzip bytes, 100,194-byte Performance chart gzip, 3,211-byte Calendar gzip, 4,712-byte Chat gzip, 2,446-byte Academic Performance gzip, 1,355-byte Academic Modules gzip, and 5,939-byte Academic Module Registration gzip. No warnings were emitted.
- Profile > Portfolio Phase 1 is now implemented across the shared Profile shell. Rizal's grid contains three dated projects plus the final Add Project tile, while Alya receives the valid add-only empty state. Nested new, detail, and edit routes keep Profile > Portfolio active and preserve the shared summary and issue cards. Project cards use sibling detail and overflow controls; details provide metadata badges, Edit, confirmed Delete Project, and an explicitly disabled Share placeholder.
- Canonical Portfolio Project records belong directly to stable Student Profile IDs. The validated GET/POST/PATCH/DELETE Portal API boundary, scenario-scoped query mutations, generated opaque IDs, student isolation, date/name ordering, and session reset are synchronized with fixtures, Data Model Explorer metadata, documentation, and tests. Portfolio artwork is retained only as metadata and a temporary object URL; replacement and deletion revoke session-created previews without uploading or persisting file bytes. The currently allowed formats and preview behavior are recorded in the latest Portfolio handoff entry below.
- The Portfolio change-surface audit reports no companion-layer gaps. `npm run check:all` passes the direct-token audit, warning-free lint, 240 unit tests across 42 files, production and bundle checks, and all 70 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 193,170 eager gzip bytes, 100,193-byte Performance chart gzip, 3,212-byte Calendar gzip, 4,710-byte Chat gzip, 2,446-byte Academic Performance gzip, 1,354-byte Academic Modules gzip, and 5,944-byte Academic Module Registration gzip. The first gate exposed an eager-bundle overage caused by a runtime Portfolio validation import; moving that Portfolio-only schema behind the existing lazy boundary resolved it before the clean rerun. No warnings were emitted by the completed full gate.
- The Profile Portfolio grid now uses the explicit four-column `CardGrid` variant. The empty Artwork chooser shares the transparent dashed Add Project tile treatment rather than using `surface-subtle`; selected artwork preview behaviour is unchanged. Focused CardGrid and Portfolio component coverage passes 12 tests, the production build passes, and the Portfolio browser flow passes at 1280px and 1440px. The direct focused Playwright command emits only the existing `NO_COLOR`/`FORCE_COLOR` environment warning.
- Portfolio add/edit actions now live within the right-hand form column. Portfolio detail metadata uses the new shared `DetailField` `readOnlyStacked` variant, placing labels above values without an `InnerSection`. The detail action column now presents full-width Edit and disabled Share controls together, followed by a visibly separated neutral plain Delete Project link that underlines and becomes title-colour on hover. Tools use the shared `Badge` component at its large size with an elevated neutral surface; project-card names use regular-weight title-colour body text. The details artwork fills its available container with `object-cover`, retaining its source ratio and cropping as necessary. Submitting a new project without artwork maps the raw schema failure to `Select artwork to continue.`. The Design System Explorer includes the stacked field example. The change-surface audit reports no companion-layer gaps, and `npm run check:all` passes the direct-token audit, warning-free lint, 241 unit tests across 43 files, production and bundle checks, and all 70 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 193,147 eager gzip bytes, 100,193-byte Performance chart gzip, 3,210-byte Calendar gzip, 4,712-byte Chat gzip, 2,445-byte Academic Performance gzip, 1,355-byte Academic Modules gzip, and 5,947-byte Academic Module Registration gzip. The first full run had one unrelated transient 1440px message-directory timeout; its isolated retry passed, and the clean full rerun passed without warnings.
- Portfolio artwork now accepts one non-empty JPEG, PNG, WebP, GIF, or PDF up to 25 MiB. PDF selections retain the same session-only metadata/object-URL lifecycle and render as labelled artwork tiles in the picker, project grid, and details. Selecting detail artwork opens the shared deferred `MediaPreviewLightbox` also used by chat thumbnails: raster images keep zoom and pan controls, while PDFs use the same accessible modal shell with an embedded document viewer and no image-only controls. The Design System Explorer includes the shared image-preview state. The change-surface audit reports no companion-layer gaps. Focused coverage passes 8 component tests and all 4 Portfolio browser runs at 1280px and 1440px. The clean `npm run check:all` rerun passes the direct-token audit, warning-free lint, 243 unit tests across 44 files, production and bundle checks, and all 72 Playwright desktop runs. Bundle budgets pass with a 340,900-byte largest chunk, 193,159 eager gzip bytes, 100,193-byte Performance chart gzip, 3,213-byte Calendar gzip, 4,733-byte Chat gzip, 2,444-byte Academic Performance gzip, 1,353-byte Academic Modules gzip, and 5,945-byte Academic Module Registration gzip. The initial full browser run exposed a remote Picsum image-decode timing assertion at 1440px; the modal content and source were present, the assertion was made network-independent, its isolated retry passed, and the clean full rerun passed without warnings.
- Shared warning and destructive `IssueCard` icons now sit inside a 36px rounded square using the faded white `title` surface. The icon is a 20px solid white triangle without an outline and retains a legible severity-coloured `!` mark. Issue labels use regular weight and the icon-to-copy gap is 8px; existing start/end placement, hover, keyboard semantics, and action behaviour remain unchanged. The Design System Explorer uses the component directly. The change-surface audit reports no companion-layer gaps; focused IssueCard coverage passes 5 tests, the production build passes, and the Profile Portfolio browser flow passes at both 1280px and 1440px. Direct Playwright output included only the existing non-blocking `NO_COLOR`/`FORCE_COLOR` environment warning.
- The floating sidebar now uses a full-height shared left border to separate Community from Navigation. Navigation's `Log out` row is bottom-aligned beneath an edge-to-edge divider and shares the raised navigation-item treatment. It is intentionally the sole frontend-only logout placeholder; real logout remains out of scope. The production build and focused dashboard/sidebar browser flow pass at 1280px and 1440px. Direct Playwright output included only the existing non-blocking `NO_COLOR`/`FORCE_COLOR` environment warning.
- Chat's Add attachment control now uses the compact raised-surface menu-button treatment rather than the bordered secondary style. Attachment selection, validation, previews, session lifetime, and sending behavior are unchanged. The production build and focused session-chat browser flow pass at 1280px and 1440px. Direct Playwright output included only the existing non-blocking `NO_COLOR`/`FORCE_COLOR` environment warning.
- Sidebar main-navigation rows, accordion triggers, and the footer Log out control now share an explicit 36px height. Nested Profile child links retain their compact treatment. The production build and focused dashboard/sidebar browser flow pass at 1280px and 1440px; direct Playwright output included only the existing non-blocking `NO_COLOR`/`FORCE_COLOR` environment warning.
- The active Chat Composer now has an edge-to-edge top divider and the same padded 36px control-row footer geometry as Navigation's Log out control, aligning both footers at the bottom of the sidebar. Attachment selection, drafting, sending, and textarea sizing remain unchanged. The production build and focused session-chat browser flow pass at 1280px and 1440px; direct Playwright output included only the existing non-blocking `NO_COLOR`/`FORCE_COLOR` environment warning.
- Chat message lists now retain a 12px bottom inset before the composer divider, keeping the final bubble visually separated from the aligned footer. Sending, session drafts, and scrolling behavior are unchanged. The production build and focused session-chat browser flow pass at 1280px and 1440px; direct Playwright output included only the existing non-blocking `NO_COLOR`/`FORCE_COLOR` environment warning.
- Main portal route content now uses a 12px bottom inset instead of the former 48px all-sides inset. At scroll bottom, final profile actions sit 4px above Navigation's bottom controls; the header, top spacing, and horizontal page gutters are unchanged. The production build and focused Personal Profile browser flow pass at 1280px and 1440px; direct Playwright output included only the existing non-blocking `NO_COLOR`/`FORCE_COLOR` environment warning.
- Active Chat Conversation headers (Back and identity content) now remain fixed above the dedicated message scroller, while messages alone scroll before the aligned composer footer. Conversation controls, drafts, sending, and per-conversation scroll restoration remain unchanged. The production build and focused session-chat browser flow pass at 1280px and 1440px; direct Playwright output included only the existing non-blocking `NO_COLOR`/`FORCE_COLOR` environment warning.
- Sidebar menu and accordion-trigger rows now keep their fixed 36px height with `flex-shrink: 0`; when expanded navigation runs out of space, the existing Navigation scroller handles the overflow instead of compressing its controls. Nested links and the footer remain unchanged. The production build and focused dashboard/sidebar browser flow pass at 1280px and 1440px; direct Playwright output included only the existing non-blocking `NO_COLOR`/`FORCE_COLOR` environment warning.

## Latest refinements — 2026-09-01

- Finance's dense desktop tables avoid horizontal scrolling. The Invoices table puts the canonical short invoice number above its title and removes the redundant category/semester description; Recorded Payments uses the same canonical reference for allocations. No separate display-only shortening remains.
- On Finance's destructive outstanding-balance summary, the How to Pay secondary action now uses a `surface-subtle` base for contrast and retains `surface-hover` on hover. Raised clear and credit summaries keep the normal secondary action treatment.
- `SummaryCard` has a semantic destructive surface for attention states: a solid red background with high-contrast label, value, and supporting copy. Finance selects it for a positive outstanding balance only; credit and clear summaries retain the raised surface.
- The How to Pay popup no longer calls out the prototype disclaimer. Its counter and bank-transfer labels no longer use `Demo`, including the beneficiary and account-number prefix; the Finance fixture remains documented as fictional in the product documentation.
- `SummaryCard` now uses its raised surface as the shared default. Finance relies on that default for its Invoices-tab account summary, while Attendance retains its explicit raised metric treatment and consumers may still select the quieter subtle variant where appropriate.
- Finance now shows its signed Amount Outstanding, Account Credit, or zero summary only on the Invoices tab. Payment History omits that summary and gives Pending Transfer Proofs, Recorded Payments, and Credits and Adjustments the same shared empty-state treatment whenever their respective lists are empty.
- Finance's **How to Pay** popup now uses the existing `DetailSectionCard` header action slots for its Campus Counter and Bank Transfer icons. The instruction bodies align beneath their headers; instruction content and popup behaviour are unchanged.
- Student Finance Phase 1 replaces the Finance placeholder with a student-scoped signed account summary, fictional selected-campus counter and bank-transfer instructions, URL-backed Invoices and Payment History tabs, and routed invoice details. Rizal's canonical ledger now uses separate tuition and miscellaneous invoices while retaining internal module-level tuition lines; the student response aggregates each semester's module lines into one tuition charge and exposes no module pricing through Finance or Academic Module Details. Confirmed allocations, credits/debits, reversed payments, nullable non-semester invoice ownership, derived status/sorting, zero balance, and account credit are contract-validated. Alya retains an active clear account with no financial activity.
- Outstanding invoices support one session-only pending bank-transfer proof with editable amount/date/reference and one validated PDF, JPEG, or PNG up to 10 MiB. Create/replace/withdraw update the scoped Finance query cache, retain no file bytes, never create a Payment or alter a balance, and reset on refresh. Student-view switching retains owned proof records for the all-student review workspace. The outstanding-fees Profile issue now routes to Finance; Finance routes, unavailable states, disabled document download explanation, semantic badges, loading/error/empty states, proof confirmation, Data Model Explorer entities/relationships, and documentation are synchronized. Campus payment fixtures are explicitly fictional.
- Finance due dates are now frozen student-facing behaviour. Canonical Invoice records retain valid `dueAt` values for future use, but the Finance DTO, screens, sorting, and Study Plan expose no due date, overdue state, earliest-due summary, overdue count, or open-invoice count. Student invoice status is limited to Unpaid, Partially Paid, Paid, or Void; open invoices sort before completed invoices, then by newest issue date and invoice number.
- The Finance account summary now sits inside the existing tabbed panel as one subtle strip containing only the signed balance. Its How to Pay link opens the shared accessible Popup with the same fictional counter and bank instructions. Outstanding/charge/debit values use destructive red, confirmed payments/allocations/credits use success green, and zero, pending, void, or reversed values remain neutral.
- Outstanding Fees is no longer manually seeded. Finance, Profile, Dashboard, and Study Plan reuse one signed-ledger derivation; a positive account produces exactly one stable aggregate issue, while confirmed payments, credits, void invoices, clear balances, and account credits remove it. Visa, registration, and other canonical issues remain unchanged.
- Repository layout governance now requires every new or changed route page to reference the established shell and at least two comparable completed pages before implementation. Shared route-heading treatment, top-level rhythm, width, and loading/error geometry must be reused unless a documented, tested divergence is intentional; `docs/design-system.md` records Profile and Academic as the concrete heading/spacing reference.
- The browser/document root now has a fixed, overflow-hidden height while `portal-main-scroll` remains the only portal content scroller. Resizing Portfolio's Description textarea taller therefore stays within the main content scroll region and no longer shows a competing document scrollbar. The Portfolio browser regression covers the reported 1569px by 912px viewport in addition to the established 1280px and 1440px desktop coverage. `npm run check:all` passes with 241 unit tests and 72 Playwright desktop runs; direct-token, lint, production, and bundle checks are clean, with no warnings. The later Portfolio visual refinements pass six focused unit tests, both 1280px/1440px browser flows, and a production build with no warnings.
- The top-menu `StudentProfile` avatar is now an accessible direct Personal-profile action; it routes to `/profile/personal-info` and no longer opens an account or logout dropdown. Navigation's footer Log out button remains the sole visible, frontend-only no-op logout placeholder. The Design System Explorer and desktop browser flow cover the new control at 1280px and 1440px.
- Portfolio project-card artwork opacity and metadata-row surface changes now occur immediately on hover, with no card hover transition. The metadata row uses `surface-hover`; the focused Portfolio Playwright flow passes at 1280px and 1440px.
- Calendar and Attendance component tests now pin their August 2026 fixture clock, preventing the real calendar month from changing their initial rendered month after a date rollover. This is test determinism only; runtime Calendar and Attendance behaviour is unchanged.
- Main route content now has a 12px bottom inset, giving Dashboard and long pages slightly more breathing room below their final section while preserving the established header, top gap, and horizontal gutters. The production build and focused Personal Profile browser flow pass at 1280px and 1440px; direct Playwright output included only the existing non-blocking `NO_COLOR`/`FORCE_COLOR` environment warning.
- Finance now uses one feature-owned page shell for overview, loading, read error, unavailable invoice, and invoice details. Every state uses `pageSectionTitleVariants()` and the same 16px top-level rhythm as Profile and Academic; the overview heading remains outside `TabbedPagePanel`, while the panel retains its 32px major-content spacing.
- The shared `SummaryCard` now owns compact label/value summaries across Finance, programme Attendance, and Module Attendance. It supports subtle/raised surfaces, section/metric emphasis, neutral/success/destructive value tones, an optional action before supporting copy, a 4px label-to-value gap, and 8px supporting-content gaps. Finance uses its action slot for How to Pay; Attendance keeps raised metric cards. `StudentSummaryCard` and Performance chart cards remain separate. The Design System Explorer presents both Finance and Attendance variants.
- The repository-local `.agents/skills/cms-student-portal-page/SKILL.md` now governs new route pages and substantial page-shell restructures. It requires two comparable page references, a page-reference matrix, complete route-state geometry, component inventory, desktop route verification, the change-surface audit, and the full quality gate. `AGENTS.md` links the skill; isolated component, content, and CSS edits remain outside its trigger.
- Finance clear-account summaries now omit the `Your account is clear.` supporting label. A zero-balance account presents only its neutral amount and the existing How to Pay link; no balance, API, or payment-instruction behaviour changes.
- `SummaryCard` now renders any optional action in a dedicated right-side column, vertically centred with its label/value column. Finance uses that slot for its secondary How to Pay Button; the accessible payment-instructions Popup is unchanged. Optional supporting copy remains 8px below the shared two-column summary row.
- The shared success Badge now uses a solid green background with high-contrast white foreground copy. Confirmed and positive status semantics are unchanged; the Design System Explorer continues to show the reusable success variant.
- Finance's transfer-proof Popup now uses the same `type-body text-title` labels and 8px field gap as other editable portal forms, replacing its inconsistent uppercase metadata labels. `docs/design-system.md` and `AGENTS.md` now require this documented form-label treatment and comparison with an existing form before adding local label styling.
- Finance Invoice Details now reads as one single-column document inside a feature-owned 12px `surface` panel with a 16px inset. The breadcrumb and compact invoice-number/title/status identity sit above full-width Invoice Summary, Charges, Confirmed Payments and Credits, and conditional Transfer Proof sections. Standard side-by-side `DetailField` rows replace the former narrow stacked summary, while the proof and disabled document actions use a compact bottom-right block. Student-facing `Original Total` copy is now `Invoice Total`; the canonical `totalMinor` contract, ledger reconciliation, due-date freeze, proof lifecycle, and semantic amount colours are unchanged. Invoice-route lazy loading, query loading, read errors, and unavailable IDs reserve the same panel geometry.

## Latest verification — 2026-09-01

- The final change-surface audit reports no obvious companion-layer gaps.
- `npm run check:all` passes the direct-token audit, warning-free lint, 257 unit tests across 47 files, production and bundle checks, and all 76 Playwright desktop runs at 1280px and 1440px.
- Bundle budgets pass with a 340,900-byte largest chunk, 186,448 eager gzip bytes, 100,193-byte Performance chart gzip, 3,211-byte Calendar gzip, 4,801-byte Chat gzip, 2,444-byte Academic Performance gzip, 1,355-byte Academic Modules gzip, and 5,938-byte Academic Module Registration gzip. The shared Summary Card is 0.47 kB gzip and the lazy Finance page is 6.67 kB gzip in the production build.
- Focused Summary Card, Finance, and Attendance coverage passes 10 unit tests. The focused Finance, programme Attendance, Module Attendance, and Design System browser set passes all 10 runs at 1280px and 1440px. The production build, skill validator, `git diff --check`, and change-surface audit pass; the audit reports no obvious companion-layer gaps.
- The first focused browser command used an unsupported wrapper argument form and found no tests; rerunning the same selection through the wrapper's direct Node entrypoint passed all intended flows. The first production build caught three test-only unsupported `exact` query options; replacing them with exact-name regular expressions produced a clean build. Neither issue affected runtime behavior, and the completed full gate emitted no warnings.
- The subsequent clear-account copy refinement passes the focused Finance component suite (5 tests) and a production build. No warnings were emitted; it does not require a new full gate because it is an isolated presentation change.
- The Invoice Details refinement passes the focused Finance component suite (5 tests), production build, and both focused Finance browser runs at 1280px and 1440px. Live inspection at both desktop widths confirms the 16px inset, 24px panel rhythm, 32px section rhythm, 320px right-aligned action block, and zero document/main/panel horizontal overflow.
- The clean `npm run check:all` completion passes the direct-token audit, warning-free lint, 257 unit tests across 47 files, production and bundle checks, and all 76 Playwright desktop runs. Bundle budgets pass with a 340,900-byte largest chunk, 186,586 eager gzip bytes, 100,194-byte Performance chart gzip, 3,213-byte Calendar gzip, 4,799-byte Chat gzip, 2,448-byte Academic Performance gzip, 1,356-byte Academic Modules gzip, and 5,939-byte Academic Module Registration gzip. The change-surface audit reports no obvious companion-layer gaps and `git diff --check` passes.
- The first focused Finance browser run used `margin-left: auto` as an expected computed CSS value; browsers correctly resolve that auto margin to pixels. Replacing the test-only assertion with a direct right-edge alignment check produced the clean focused and full reruns. Runtime layout was unaffected, and the completed gate emitted no warnings.
- The How to Pay refinement passes the focused Finance component suite (5 tests), both focused Finance desktop browser runs at 1280px and 1440px, the production build, `git diff --check`, and the change-surface audit. No warnings were emitted.
- The shared SummaryCard action-column refinement passes 7 focused SummaryCard and Finance component tests and the complete `npm run check:all` gate: direct-token audit, lint, 257 unit tests across 47 files, production and bundle checks, and all 76 Playwright desktop runs at 1280px and 1440px. `git diff --check` and the change-surface audit pass with no warnings.
- The solid-success Badge refinement passes its focused shared badge suite (4 tests) and the complete `npm run check:all` gate: direct-token audit, lint, 257 unit tests across 47 files, production and bundle checks, and all 76 Playwright desktop runs at 1280px and 1440px. `git diff --check` and the change-surface audit pass with no warnings.
- The transfer-proof label refinement passes the focused Finance component suite (5 tests), its two desktop proof-flow browser runs at 1280px and 1440px, and the production build. No warnings were emitted.
- The How to Pay icon-header refinement passes the focused Finance component suite (5 tests), its two desktop Finance browser runs at 1280px and 1440px, the production build, `git diff --check`, and the change-surface audit. No warnings were emitted.
- The Finance tab-summary and payment-history empty-state refinement passes the focused Finance component suite (5 tests), four focused Finance desktop browser runs at 1280px and 1440px, and the complete `npm run check:all` gate. `git diff --check` and the change-surface audit pass with no warnings.
- The SummaryCard default-surface refinement passes seven focused SummaryCard and Finance component tests, the Finance and Design System desktop flows at 1280px and 1440px, and the complete `npm run check:all` gate. `git diff --check` and the change-surface audit pass with no warnings.
- The How to Pay copy refinement passes the focused Finance component suite (5 tests), two Finance desktop browser runs at 1280px and 1440px, and the production build. `git diff --check` and the change-surface audit pass with no warnings.
- The destructive SummaryCard refinement passes eight focused SummaryCard and Finance component tests, the Finance and Design System desktop flows at 1280px and 1440px, the production build, and the complete `npm run check:all` gate. `git diff --check` and the change-surface audit pass with no warnings.
- The destructive-summary How to Pay contrast refinement passes eight focused SummaryCard and Finance component tests, two Finance desktop browser runs at 1280px and 1440px, and the production build. `git diff --check` and the change-surface audit pass with no warnings.
- The Finance dense-table refinement passes 10 focused Finance unit tests, a production build, `git diff --check`, and the focused Finance browser flow at 1280px and 1440px. The browser coverage verifies that the Invoice and Recorded Payments table containers have no horizontal overflow. The change-surface audit reports no obvious companion-layer gaps; no warnings were emitted by the captured checks.

## Finance Scholarships & Incentives — 2026-09-02

- Finance now has a third URL-backed `Scholarships & Incentives` tab at `/finance?tab=awards`. It lists every assignment owned by the selected Programme Enrolment in active, upcoming, completed, suspended, then cancelled order. Award cards show the scheme name and type, staff-controlled status, semester range, fee benefits, applied-credit total, informational allowances, academic renewal conditions, and the exact latest published semester result used for evaluation. Payment History and the awards tab intentionally omit the account summary; Alya receives the shared `No scholarships or incentives applied` empty state.
- The canonical Finance graph now includes Funding Scheme, Student Funding Award, Student Funding Benefit, and Student Funding Renewal Condition records. Benefits form a validated percentage-fee, fixed-fee, or informational-allowance union. Financial Adjustments carry a nullable exact Funding Benefit relationship; graph validation enforces enrolment and period ownership, benefit currency/category/type, renewal ownership, benefit limits, and total invoice collectibility. The Data Model Explorer includes all four records and their Scheme, Programme Enrolment, Study Period, Award, and Adjustment relationships.
- Active fee-benefit calculation uses award time then stable award ID, applies only to eligible same-category invoices inside the assignment period, and caps each stacked credit at the remaining collectible balance. Informational allowances never create adjustments or affect a balance. Academic renewal uses only the latest published Term Result and derives Met, Not Met, or Pending without changing staff-controlled award status or revoking historical credits.
- Rizal's fictional Creative Excellence Scholarship is active for Semesters 4–6. Its 10% tuition benefit posts a RM1,300 credit against the Semester 4 tuition invoice, reducing the invoice, Finance Account, Profile/Dashboard fee issue, Study Plan projection, and proof limit/default from RM2,450 to RM1,150. Its minimum 3.00 CGPA condition evaluates as Met from the latest published 3.33 CGPA; the RM480-per-semester study allowance is explicitly information only. The completed Early Confirmation Incentive now owns the existing historical RM300 Semester 1 tuition credit. Alya has no assignments.
- `GET /api/v1/finance` now returns the scoped awards, benefit details, applied-credit totals, period/status data, renewal evidence, and award-linked adjustment metadata without adding an endpoint or cache. There is no student award mutation, application, acceptance, document, details route, payout schedule, bank detail, or payout reconciliation.
- The completed `npm run check:all` gate passes the direct-token audit, warning-free lint, 263 unit tests across 47 files, production and bundle checks, and all 76 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 186,604 eager gzip bytes, 100,194-byte Performance chart gzip, 3,212-byte Calendar gzip, 4,798-byte Chat gzip, 2,447-byte Academic Performance gzip, 1,356-byte Academic Modules gzip, and 5,941-byte Academic Module Registration gzip. The lazy Finance page is 8.04 kB gzip.
- Focused verification passes 109 Finance/record/route/data-model/dependent-projection tests and the Rizal/Alya Finance browser flows at both desktop widths. `git diff --check` passes, and the change-surface audit reports no obvious companion-layer gaps. One pre-gate parallel browser run timed out while waiting for the initial 1280px Finance shell; its isolated retry and the clean complete gate passed. No persistent warnings remain.
- Invoice Details now applies the same visible minus sign to every reducing entry in its combined `Confirmed Payments and Credits` ledger: confirmed payment allocations and credit adjustments are both green negative amounts. The separate Recorded Payments table remains a positive payment-history view. This isolated presentation refinement passes the focused Finance component test and production build; no warnings were emitted.
- All Finance table monetary columns now use left alignment with tabular figures, including invoice totals and balances, pending-proof amounts, recorded payments, credits/adjustments, and invoice charge lines. Summary cards and side-by-side invoice DetailFields retain their established layout. This isolated presentation refinement passes the focused Finance component test and production build; no warnings were emitted.
- Finance Invoice Details now follows the established Academic Module Details and Profile Portfolio Details hierarchy: the Finance heading and `TabbedPagePanel` remain visible, Invoices stays active, and the inner breadcrumb is `Invoices > Invoice Details` without repeating Finance. The invoice identity uses the shared `SummaryCard`; normal, loading, read-error, and unavailable states all render inside the same active tab geometry. Selecting the active Invoices tab, breadcrumb, or `Back to Invoices` returns to the invoice list. `AGENTS.md`, the repository page skill, design-system guidance, sidebar behaviour, and Finance documentation now require this parent-shell and immediate-parent breadcrumb rule for nested routes.
- The nested Finance refinement passes 16 focused Finance/route tests, the focused Finance Playwright flow at 1280px and 1440px, the page-skill validator, `git diff --check`, and the complete `npm run check:all` gate: direct-token audit, warning-free lint, 263 unit tests across 47 files, production and bundle checks, and all 76 Playwright desktop runs. Bundle budgets pass with a 340,900-byte largest chunk, 186,608 eager gzip bytes, 100,193-byte Performance chart gzip, 3,212-byte Calendar gzip, 4,802-byte Chat gzip, 2,445-byte Academic Performance gzip, 1,354-byte Academic Modules gzip, and 5,940-byte Academic Module Registration gzip. The change-surface audit reports no obvious companion-layer gaps; no persistent warnings remain.
- Semantic `Badge` variants now consistently use faded success, warning, and destructive surfaces with their matching solid label colours. Finance's pending-proof explanation is muted supporting text. Academic Module and Finance Invoice Details now use the shared raised `SummaryCard` for their code/reference and title identities; the Invoice Details status uses the large Badge in the SummaryCard's vertically centred action column. The SummaryCard keeps semantic heading values legal for nested detail identities.
- The badge and detail-identity refinement passes 25 focused shared/component tests, six focused Finance and Module Details Playwright flows at 1280px and 1440px, and the complete `npm run check:all` gate: direct-token audit, warning-free lint, 264 unit tests across 47 files, production and bundle checks, and all 76 Playwright desktop runs at 1280px and 1440px. The initial focused browser run correctly exposed stale `InnerSection` expectations; those test-only assertions were updated to the SummaryCard contract before the clean rerun. No persistent warnings remain.
- The nested-detail SummaryCard border refinement removes the consumer-only borders from the Module and Finance Invoice identities while retaining their raised surfaces, shared spacing, semantic headings, and invoice-status alignment. It passes 21 focused SummaryCard, Finance, and Module Detail component tests, the production build, and four focused Finance/Module Details Playwright flows at 1280px and 1440px. No warnings were emitted.

## Finance Invoice Details activity ledger — 2026-09-02

- Invoice Details now condenses Category and Semester into an opt-in divided column row inside the existing invoice identity `SummaryCard`. The shared card preserves its number/title/status primary row, adds the Study Plan label/value rhythm beneath one full-width divider, and leaves all existing default consumers unchanged. The Design System Explorer presents the new nested-detail variant.
- One chronological `Invoice Activity` `DataTable` replaces the separate Invoice Summary, Charges, and Confirmed Payments/Credits sections. Charge lines use the invoice issue timestamp, confirmed allocations use the payment timestamp, and adjustments use their applied timestamp; ties use source type and stable ID. The fixed Date, Type, Description, and left-aligned Amount columns run oldest to newest, preserve red positive charges/debits and green negative payments/credits, and end with one shared footer divider plus the net `Outstanding Balance`.
- Eligible Invoice Details actions now place equal-width Submit Transfer Proof and disabled secondary Download Invoice buttons side by side. Download no longer has visible helper copy and retains its unavailable reason in the accessible name. The new shared `TooltipIcon` decorates the Submit button as one hover/focus target and moves the balance-invariance explanation into the shared Tooltip surface without nesting an icon control. `Popup` now permits controlled use without an internal trigger; all existing trigger-based consumers remain compatible. Pending-proof Change/Withdraw behaviour, proof validation, ledger values, routes, and API contracts are unchanged.
- Loading geometry reserves the expanded identity, unified ledger, footer, and horizontal action row. Finance/design-system documentation, shared tests, Finance component coverage, and both desktop browser flows are synchronized.

### Invoice summary metadata follow-up — 2026-09-02

- The Invoice Details `SummaryCard` metadata row now includes Issue Date and Charge Amount alongside Category and Semester. Its shared summary columns are content-sized rather than distributing spare card width, while retaining safe wrapping on constrained desktop space.
- For an eligible invoice, disabled Download Invoice now appears on the left and Submit Transfer Proof on the right in the equal-width action row. The unified ledger and financial calculations are unchanged.
- `SummaryCard` now defaults to the quiet `surface-subtle` tier. Explicit raised Attendance and clear-account variants, along with the destructive Finance outstanding variant, retain their specified surfaces; the default nested Module and Invoice identities use the new subtle baseline.
- Follow-up clarification: programme and Module Attendance plus clear/credit Finance summaries now also consume the default subtle surface rather than opting into raised. The destructive outstanding Finance summary remains the only semantic surface override in these flows.
- Verification: focused SummaryCard, Finance, Attendance, and Module Detail tests pass (16 tests); the complete `npm run check:all` gate passes direct-token validation, warning-free lint, 268 unit tests across 48 files, production and bundle checks, and all 76 Playwright desktop runs at 1280px and 1440px.
- Verification: focused SummaryCard/Finance tests pass (11 tests); production build passes; focused Finance and Design System Playwright coverage passes at 1280px and 1440px (4 tests); all unit tests pass (268 tests in 48 files); full Playwright passes (76 tests across both desktop projects); `git diff --check` passes; and the change-surface audit reports no obvious companion-layer gaps. The first combined `npm run check:all` run reached its E2E phase after all checks/unit/build/prod/bundle stages passed, but encountered a transient occupied port 4179; the clean standalone full E2E rerun passed after the port was clear.
- Focused verification passes 26 shared and Finance unit tests plus the Finance and Design System Playwright flows at 1280px and 1440px (four runs). The complete `npm run check:all` gate passes direct-token validation, warning-free lint, 268 unit tests across 48 files, production and bundle checks, and all 76 Playwright desktop runs. Bundle budgets pass with a 340,900-byte largest chunk, 186,608 eager gzip bytes, 100,193-byte Performance chart gzip, 3,212-byte Calendar gzip, 4,797-byte Chat gzip, 2,445-byte Academic Performance gzip, 1,355-byte Academic Modules gzip, and 5,942-byte Academic Module Registration gzip. The lazy Finance page is 8.42 kB gzip, Summary Card is 0.72 kB gzip, and Data Table is 0.97 kB gzip.
- `git diff --check` passes, and the change-surface audit reports no obvious companion-layer gaps. The first focused unit run correctly exposed a missing app-level Tooltip Provider in the Finance test helper and retained prior SummaryCard renders; both test-harness issues were corrected before the clean focused and full reruns. No persistent warnings remain.

## SummaryCard consolidation — 2026-09-03

- Every approved standalone summary strip now composes the shared default-subtle `SummaryCard`: Module Details, programme and Module Attendance, Study Plan Programme Overview, Finance account balance, Invoice Details, and Module Registration. The shared API now supports an optional label, generic Button-or-Badge action, description, content-sized wrapping columns, per-column indicators and semantic tones, plus supporting content. It owns 16px insets, 4px label/value rhythm, 24px by 16px column gaps, medium body column values, one details divider, and 20px before supporting content. Semantic heading consumers no longer repeat the card's `type-section-lg` class.
- Study Plan uses the primary/action/column/indicator combination for Programme Status and its four eligibility requirements. Module Registration's pre-confirmation summary uses a label-free semantic Semester heading, four date/load columns, and `CreditLoadBar` in the supporting slot; confirmed registration instead uses the shared completed `EmptyState`. Their loading states, along with Attendance, Module Details, and Invoice Details, compose the same shared geometry with Skeleton content. Compact Study Plan accordion-trigger metrics and the registration confirmation total intentionally remain their existing interactive-control and `InnerSection` patterns.
- The Design System Explorer presents six representative combinations covering simple identity, metric, destructive action, status columns, invoice columns, and columns with supporting content. `AGENTS.md`, the repository page-building skill, design-system guidance, and affected Academic/Finance documents now require `SummaryCard` for standalone compact summaries, describe slot selection and typography ownership, and document the intentional exclusions. The updated repository skill passes `quick_validate.py`.
- Verification: 28 focused SummaryCard, Study Plan, Finance, and Module Registration unit tests pass. The six affected browser flows pass all 12 runs at 1280px and 1440px with summary-card overflow assertions. `git diff --check` passes and the change-surface audit reports no obvious companion-layer gaps. The clean `npm run check:all` gate passes direct-token validation, warning-free lint, 270 unit tests across 48 files, production and bundle checks, and all 76 Playwright desktop runs. Bundle budgets pass with a 340,900-byte largest chunk, 186,599 eager gzip bytes, 100,194-byte Performance chart gzip, 3,212-byte Calendar gzip, 4,796-byte Chat gzip, 2,444-byte Academic Performance gzip, 1,353-byte Academic Modules gzip, and 5,966-byte Academic Module Registration gzip. The first focused Explorer run exposed only an ambiguous progressbar locator after the new registration example introduced a second valid credit bar; the assertion was scoped to its catalogue section before the clean rerun. No persistent warnings remain.

### Payment History credits column order follow-up — 2026-09-03

- The Credits and Adjustments table now presents Date, Description, Invoice, Amount, then Type, putting the left-aligned semantic amount before its Credit/Debit Badge. No Finance data, balances, or API behaviour changed.
- Verification: the focused Finance page suite passes all 6 tests; the production build and `git diff --check` pass. No warnings were emitted.

## Scholarships & Incentives accordion refinement — 2026-09-03

- The Scholarships & Incentives tab now begins with a default-subtle `SummaryCard` showing authoritative values only: Active Awards counts assignments with lifecycle status `active`, and Credits Applied totals all posted award-linked credits across active and completed awards. Rizal shows 1 active award and RM1,600 applied; Alya keeps the summary at 0 and RM0 above the existing empty state. No projected future savings are invented before future invoices exist.
- Award assignments now use one single-open, collapsible shared Accordion modelled on Study Plan. Every item starts closed; its trigger shows the title, award period, concise non-allowance fee benefit, Renewable or Non-renewable value, and the full Active, Upcoming, Completed, Suspended, or Cancelled Badge. Expanded content uses standard read-only `DetailField` rows for award identity, benefits, applied credits, informational allowances, renewal conditions, current values, combined state, and published-result evidence. The former nested `DetailSectionCard` benefit/renewal cards and two-column grid are removed, while the allowance disclaimer remains visible whenever an informational allowance exists.
- `FinanceAward` now carries the required `renewable` flag projected directly from its existing canonical Funding Scheme. No canonical record, endpoint, query cache, award ordering, ledger calculation, mutation, route, or persistence policy changed. Awards-tab loading now reserves the completed SummaryCard and two accordion trigger rows inside the existing Finance shell.
- Verification: 16 focused Finance contract, projection, and component tests pass. The Rizal and Alya Finance browser flows pass all four runs at 1280px and 1440px, covering keyboard expansion, single-open state, lifecycle values, empty-state summary, and horizontal overflow. The change-surface audit reports no obvious companion-layer gaps and `git diff --check` passes. The clean `npm run check:all` gate passes direct-token validation, warning-free lint, 272 unit tests across 48 files, production and bundle checks, and all 76 Playwright desktop runs. Bundle budgets pass with a 340,900-byte largest chunk, 186,619 eager gzip bytes, 100,193-byte Performance chart gzip, 3,211-byte Calendar gzip, 4,801-byte Chat gzip, 2,444-byte Academic Performance gzip, 1,356-byte Academic Modules gzip, and 5,968-byte Academic Module Registration gzip. The first focused component run briefly selected the geometry-matched loading heading before the completed awards content replaced it; the test now waits for the completed SummaryCard and the clean reruns pass. No persistent warnings remain.

### Awards benefit-detail cleanup — 2026-09-03

- Expanded award benefits now present allowance amounts as plain read-only values, without the redundant `Information Only` Badge. Fee benefits no longer repeat an individual `Credit applied` value; the section retains one green `Total Credits Applied` row as the authoritative applied-credit figure.
- Award accordion triggers now use only the standard-weight award title and far-right lifecycle Badge. Period, benefit, and renewable values remain available in the expanded read-only details, avoiding repeated dense summary columns.
- The read-only field rows in Award Details, Benefits, and Renewal now use a 16px title-to-form gap, matching the established section rhythm.
- Verification: the focused Finance page suite passes all 7 tests; warning-free lint, the production build, and `git diff --check` pass. No warnings were emitted.

### Default Badge surface refinement — 2026-09-03

- The shared default blue Badge now follows the same faded-surface/solid-label treatment as success, warning, and destructive Badges. Its keyboard focus and linked hover states remain semantic brand treatments; the Design System Explorer's existing default Badge demonstrates the updated rule.
- Verification: focused Badge coverage passes all 5 typography tests. The clean `npm run check:all` gate passes direct-token validation, warning-free lint, 273 unit tests across 48 files, production and bundle checks, and all 76 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 186,608 eager gzip bytes, 100,193-byte Performance chart gzip, 3,212-byte Calendar gzip, 4,795-byte Chat gzip, 2,445-byte Academic Performance gzip, 1,355-byte Academic Modules gzip, and 5,965-byte Academic Module Registration gzip. An earlier full-gate attempt encountered an orphaned local Vite listener on port 4179 before E2E began; the process was identified as the prior interrupted local check, stopped, and the clean rerun passed. No persistent warnings remain.

### Materials accordion state refinement — 2026-09-03

- Academic Module Materials now starts with every teaching week collapsed, including the current week; the shared single-open accordion still permits opening and collapsing one week at a time. Completed week status now uses the shared green `success` Badge, while current remains faded brand and locked remains neutral outline.
- Verification: 19 focused Materials and Module Detail tests pass; the focused Materials and related route flows pass at 1280px and 1440px. The clean `npm run check:all` gate passes direct-token validation, lint, 273 unit tests across 48 files, production and bundle checks, and all 76 Playwright desktop runs. `git diff --check` passes and the change-surface audit reports no obvious companion-layer gaps. No warnings remain.

### Module Attendance SummaryCard surface refinement — 2026-09-03

- Module Attendance's four metric summaries remain shared `SummaryCard` instances and now use the existing explicit `raised` surface, giving them the intended separation above the surrounding tab surface. The Design System Explorer presents the raised metric combination; programme Attendance remains on its established subtle surface.
- Verification: focused SummaryCard and Module Attendance tests pass (11 tests), and the clean `npm run check:all` gate passes direct-token validation, warning-free lint, 274 unit tests across 48 files, production and bundle checks, and all 76 Playwright desktop runs at 1280px and 1440px. No warnings remain.

### Completed Finance and portal refinement release — 2026-09-03

- The accumulated Student Finance account, invoice and transfer-proof flows, Scholarships & Incentives, shared SummaryCard and TooltipIcon patterns, nested-page consistency, Academic refinements, sidebar/composer behaviour, documentation, and test coverage are synchronized and ready for release.
- Release verification: the final clean `npm run check:all` passes direct-token validation, warning-free lint, 274 unit tests across 48 files, production and bundle checks, and all 76 Playwright desktop runs at 1280px and 1440px. The change-surface audit reports no obvious companion-layer gaps, the repository page skill validates, and `git diff --check` passes. An earlier full-gate attempt had one transient 1280px sidebar-start miss while its 1440px counterpart passed; the isolated two-width rerun and the complete clean rerun both passed without code changes. No persistent warnings remain.

## Module Registration invoice issuance — 2026-09-04

- Module Registration now has an aggregate fee-preview boundary and a first-confirmation-only mutation. Preview and confirmation both validate the selected Study Period and offerings against the active Finance Account, Programme Version Fee Plan, explicit module tuition Fee Items, one registration Fee Item, and a shared currency; no module-level price reaches the route response or UI.
- Confirmation stages the selected Module Registrations, one deterministic semester tuition invoice, one separate registration-fee invoice, exact canonical lines, eligible capped award-linked credits, and the resolved registration issue. The canonical graph must validate before the session state changes, so duplicate confirmations, pre-existing target invoices, invalid selections, and Finance errors cannot leave a partial result.
- Confirmed Study Periods are permanently read-only. Immediately after confirmation and on later visits, the page replaces the module list and timetable with the shared subtle completed `EmptyState`, including a check icon, invoice-created confirmation copy, and one `View Study Plan` link; Finance remains available through the sidebar. The standard three-module Alya flow creates RM7,800 tuition plus RM400 registration, derives RM8,200 outstanding and one Outstanding Fees issue, and resets to the zero-invoice fixture on refresh.
- Query invalidation now includes Finance, Calendar, the wider Academic projections, Dashboard, Profile, Study Plan, Community, and Chat. The fee-preview popup has geometry-matched loading/error states and cannot confirm until a fresh preview succeeds.
- Verification: the focused registration, Data Model Explorer, and canonical-graph suites pass all 59 tests. The change-surface audit classifies all 17 changed files and reports no obvious companion-layer gaps, and `git diff --check` passes. The clean `npm run check:all` gate passes direct-token validation, warning-free lint, 279 unit tests across 48 files, production and bundle checks, and all 76 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 186,758 eager gzip bytes, 100,193-byte Performance chart gzip, 3,211-byte Calendar gzip, 4,799-byte Chat gzip, 2,443-byte Academic Performance gzip, 1,354-byte Academic Modules gzip, and 6,436-byte Academic Module Registration gzip. No persistent warnings remain.

### Completed registration empty-state restoration — 2026-09-04

- A confirmed Study Period now immediately replaces Module Registration's summary, module list, timetable, and editing controls with the shared subtle `EmptyState`. It shows the check icon, `Registration completed`, the invoice-created confirmation copy, and one `View Study Plan` link. The same state appears on later visits regardless of whether the registration window remains open or has closed; a closed unregistered period retains `Registration missed`. Finance remains available through the persistent sidebar.
- The presentation change does not alter fee preview, atomic registration/invoice confirmation, award credits, cache invalidation, final-registration locking, or session reset. The now-unreachable Registered Badge and its loading placeholder were removed from the pre-confirmation SummaryCard anatomy.
- Verification: the focused EmptyState and Module Registration suites pass all 20 tests, and the revised immediate-confirmation and closed-period flows pass all four focused Playwright runs at 1280px and 1440px. The change-surface audit classifies all 18 changed files and reports no obvious companion-layer gaps, and `git diff --check` passes. The clean `npm run check:all` gate passes direct-token validation, warning-free lint, 279 unit tests across 48 files, production and bundle checks, and all 76 Playwright desktop runs. Bundle budgets pass with a 340,900-byte largest chunk, 186,771 eager gzip bytes, 100,193-byte Performance chart gzip, 3,213-byte Calendar gzip, 4,800-byte Chat gzip, 2,443-byte Academic Performance gzip, 1,354-byte Academic Modules gzip, and 6,399-byte Academic Module Registration gzip. The direct focused Playwright command emitted only Node's environment-level notice that `NO_COLOR` was ignored because `FORCE_COLOR` was set; the repository test wrapper and full quality gate emitted no warnings.

## Campus-specific Malaysian Service Tax — 2026-09-04

- Student citizenship now carries an ISO country code while retaining nationality display text. Profile Personal shows the campus-relative Student Fee Category as Local or International, but the SST engine uses Malaysian citizenship directly. Alya is now a coherent Malaysian/local fixture without a Student Visa / Pass; Study Plan models passport/visa clearance as an applicable/not-applicable union and shows `Not applicable` for local students. Rizal remains Indonesian/international.
- The canonical graph now owns effective-dated Campus Service Tax Profiles, explicit Fee Item tax treatments/exemption codes, enrolment-scoped sponsor or diplomat-dependant evidence, and exactly one immutable Service Tax decision per Invoice. Invoice totals must reconcile line subtotal plus tax and remain tax-inclusive downstream. The Cyberjaya profile applies 6% from 1 July 2025; pre-effective invoices and campuses without a profile are not applicable, Malaysian citizens are exempt, and books/accommodation are explicitly exempt.
- Invoice issuance resolves the profile and fee treatment, applies eligible award credits to the taxable value, applies the approved prototype exemptions, rounds half-up to sen, and snapshots the result. Module Registration preview now shows Tuition, Registration, Eligible award credit, SST, and Amount due without module-price leakage; confirmation atomically creates both invoice tax snapshots. Invoice Details projects the pre-tax subtotal and adds a red `SST` activity row only when the tax amount is positive.
- Rizal's Semester 2 and 3 payments were increased to preserve their paid state after RM870 and RM1,044 SST. Semester 4 registration/resource/international-services SST totals RM90 and remains fully allocated; tuition carries RM702 SST on RM11,700 after its RM1,300 award credit. His only outstanding invoice and account balance are exactly RM1,852. Alya's standard three-module confirmation remains RM8,200 due because both invoices receive Malaysian-citizen exemption snapshots.
- The RMCD Education FAQ confirms non-Malaysian higher-education scope, Malaysian-citizen exemption, the 6% rate, and the 1 July 2025 effective date. Legal warning: it also says company-sponsored tuition remains taxable and institutions providing full scholarships must account for tax. The approved sponsor, diplomat-dependant, and scholarship-netting branches are therefore prototype assumptions that require campus Finance or Malaysian tax-adviser confirmation before production use.
- Verification: focused production build and 286 unit tests across 49 files pass. The change-surface audit classifies all 42 changed files and reports no obvious companion-layer gaps. After correcting two E2E expectations to preserve the intentionally session-only Alya scenario and account for the new read-only field, all four focused desktop reruns pass. The final clean `npm run check:all` passes direct-token validation, warning-free lint, all 286 unit tests, production and bundle checks, and all 76 Playwright runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 186,778 eager gzip bytes, 100,191-byte Performance chart gzip, 3,211-byte Calendar gzip, 4,799-byte Chat gzip, 2,442-byte Academic Performance gzip, 1,354-byte Academic Modules gzip, and 6,508-byte Academic Module Registration gzip.

### Local-student Immigration access — 2026-09-04

- Immigration now follows the existing campus-relative Student Fee Category instead of appearing for every student. Alya's Local profile removes Immigration from both the main navigation and Community services; the filtered Community response also makes the existing service-message ownership check reject direct Immigration chat attempts. Rizal remains International and retains the navigation item, service chat entry, and `/immigration` placeholder.
- The portal shell resolves eligibility through the existing scenario-scoped Personal Profile query. A local, unresolved, or failed eligibility state never renders Immigration. Direct or stale `/immigration` locations are replaced with Dashboard, including after the active student changes, so browser Back cannot restore the inapplicable page. This remains frontend presentation and mock-API enforcement; a future backend must enforce the same eligibility.
- Verification: the focused Portal API suite passes all 33 tests and the production build passes. The dedicated Immigration browser flow passes both desktop runs at 1280px and 1440px. The change-surface audit classifies all 45 accumulated changed files and reports no obvious companion-layer gaps, and `git diff --check` passes. The final clean `npm run check:all` gate passes direct-token validation, warning-free lint, all 287 unit tests across 49 files, production and bundle checks, and all 78 Playwright runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 187,018 eager gzip bytes, 100,194-byte Performance chart gzip, 3,211-byte Calendar gzip, 4,798-byte Chat gzip, 2,444-byte Academic Performance gzip, 1,354-byte Academic Modules gzip, and 6,509-byte Academic Module Registration gzip. The direct focused Playwright command emitted only Node's environment-level notice that `NO_COLOR` was ignored because `FORCE_COLOR` was set; the repository test wrapper and full quality gate emitted no warnings.

### Globally unique short invoice numbers — 2026-09-04

- Every canonical Invoice and every copied Finance invoice reference now uses one globally unique `INV-######` number. `INV-000000` is reserved; the shared Zod contract rejects legacy or malformed values, and graph validation rejects duplicates across students and Finance Accounts. Stable opaque Invoice IDs continue to own relationships and routes.
- Rizal's 19 historical invoices are backfilled chronologically as `INV-000001` through `INV-000019`. Module Registration atomically reserves the next two suffixes before staging its mutation, so the standard Alya confirmation assigns `INV-000020` to tuition and `INV-000021` to registration. Malformed existing numbers, collisions, or sequence exhaustion fail without changing Registration or Finance. Preview-only tax-calculation records remain internal and never enter the canonical graph or Finance response; a production backend must own concurrency-safe permanent numbering.
- The old Finance table formatter was removed. Invoice lists, Invoice Details, payment allocations, adjustments, pending proofs, How to Pay instructions, explorer examples, and accessibility labels now display the exact canonical API number.
- Verification: 63 focused contract, Finance, registration, component, and shared-card tests pass. The Finance and first-semester registration browser flows pass all four focused runs at 1280px and 1440px. The change-surface audit classifies all 48 accumulated changed files and reports no obvious companion-layer gaps, and `git diff --check` passes. The final clean `npm run check:all` gate passes direct-token validation, warning-free lint, all 289 unit tests across 49 files, production and bundle checks, and all 78 Playwright desktop runs. Bundle budgets pass with a 340,900-byte largest chunk, 187,014 eager gzip bytes, 100,193-byte Performance chart gzip, 3,211-byte Calendar gzip, 4,801-byte Chat gzip, 2,441-byte Academic Performance gzip, 1,354-byte Academic Modules gzip, and 6,505-byte Academic Module Registration gzip. One initial focused Playwright command passed its filter as a positional file argument and found no tests; the corrected direct invocation and complete gate passed. No persistent warnings remain.

### Finance pending-proof action row — 2026-09-04

- A pending Transfer Proof now leaves its saved values and muted balance-invariance explanation in the section, while its `Download Invoice`, `Withdraw Transfer Proof`, and rightmost `Change Transfer Proof` controls share one compact, right-aligned three-button action row. The existing confirmation dialog and session-only mutation behaviour are unchanged. Eligible invoices without a saved proof retain the two-button Download/Submit row.
- Payment History now uses a compact fixed pending-proof table with Transfer Date first, followed by Invoice, Bank Reference, Amount, and Status; it omits the redundant file-name column because that filename remains available in Invoice Details.
- Verification: the focused Finance component suite passes all 7 tests; lint, production build, `git diff --check`, and the change-surface audit pass. The named Finance Playwright flow passes at 1280px and 1440px. The final clean `npm run check:all` gate passes direct-token validation, warning-free lint, all 289 unit tests across 49 files, production and bundle checks, and all 78 Playwright desktop runs at 1280px and 1440px. Bundle budgets pass with a 340,900-byte largest chunk, 187,006 eager gzip bytes, 100,194-byte Performance chart gzip, 3,211-byte Calendar gzip, 4,800-byte Chat gzip, 2,445-byte Academic Performance gzip, 1,352-byte Academic Modules gzip, and 6,506-byte Academic Module Registration gzip. The initial direct Playwright invocation emitted Node's environment-level `NO_COLOR`/`FORCE_COLOR` notice only; the final complete gate emitted no warnings.

## Thorough project audit — 2026-09-05

- Audited the current working tree, preserving its existing 48 changed application/documentation/test files. No application behavior, dependencies, commits, or pushes were changed. The detailed report and isolated reproduction suite are in [the 5 September project audit](audits/2026-09-05-project-audit.md) and `tests/audit/`.
- Found 16 confirmed application issues: three P1, eleven P2, and two P3. Highest priorities are registration clearing saved Personal Profile/chat state and attendance being attributed to the wrong student. Further findings cover timetable/offering ownership, historical lecturers and schedules, live-clock/SST/calendar boundaries, failed-send data loss, date validation, sidebar persistence, mutation atomicity, and preview cleanup. The report separates existing-data defects from injected failure and edge-input cases.
- Verification: the existing clean `npm run check:all` passes token validation, warning-free lint, all 289 unit tests across 49 files, production/dev-tool/bundle checks, and all 78 Playwright desktop runs at 1280px and 1440px. Added isolated checks produce 19 failing defect reproductions and 14 passing boundary/read-path checks. The final audit browser run has 10 failing reproductions (five defects at both widths) and two passing 14-route shell-usability sweeps. These intentionally failing audit tests are excluded from the normal suite until fixed. Final lint, `git diff --check`, and all 28 report-file reference checks pass; the advisory change-surface check reports no obvious companion-layer gaps.
- Dependency audit reports two affected transitive packages (fast-uri high, qs moderate) through the shadcn tooling dependency chain. No browser exploit was established. No automatic dependency updates were applied.
- Next approved work item: none; the requested audit is complete. Findings remain open for prioritization and a subsequent repair request.

## Completed audit repairs and registration Finance release — 2026-09-05

- All 16 findings in [the project audit](audits/2026-09-05-project-audit.md) are resolved. Registration preserves saved Personal Profile/emergency-contact edits and existing chat state; new module groups reconcile into the current store and list/detail caches refresh. Failed chat sends retain text/files and support retry, including after closing/reopening an in-flight composer. Message metadata validates before any new conversation or participant is committed.
- Timetable and attendance projections traverse the selected student's exact registered offerings. Attendance joins registration and class session together, and Rizal's intended marks are seeded under his own registrations. Module Lecturer contracts/explorer relationships now retain offering and semester IDs; repeat cards/details show their correct lecturer and historical schedules. The previously displayed GRD301/DAN201 co-lecturers are canonical Teaching Assignments and appear in their Community rosters.
- Transactions use a live clock independent of fixture generation. Registration retains inclusive opening/closing instants and records actual confirmation/issuance times; proof submission works across campus midnight. SST calculation and validation share campus-date normalization. Holidays span all covered dates with exclusive ends, and schedule conflicts include teaching-date intersection and a real common weekday. Date-only contracts reject impossible dates. Invalid tax dates/timezones produce validation issues instead of formatter exceptions.
- Sidebar expansion no longer writes a cookie. Mock-session disposal revokes Portfolio/chat previews and prevents in-flight artwork allocation after session replacement. Production-facing persistence and API boundaries remain unchanged.
- The lockfile updates only fast-uri to 3.1.7 and qs to 6.16.0. Fresh `npm audit --json` reports zero vulnerabilities.
- Final verification: `npm run check:all` passes token checks, warning-free lint, all 351 unit tests across 52 files, TypeScript/production build, production dev-tool exclusion, bundle checks, and all 92 Playwright desktop runs at 1280px and 1440px. The regular suite now includes 62 audit API/component/boundary cases and 14 audit browser runs. `git diff --check`, all 30 report-file references, and the advisory change-surface review pass with no obvious companion-layer gaps. Bundle budgets pass: largest chunk 340,900 bytes; eager JavaScript 186,547 gzip bytes; Performance chart 100,193; Calendar 3,216; Chat 5,061; Academic Performance 2,445; Academic Modules 1,358; Module Registration 6,498.
- This release includes the existing audited first-confirmation invoice issuance, campus SST, Local/International Immigration eligibility, unique invoice numbers, and pending-proof refinements together with the repairs. The user authorized committing and pushing the accumulated work. Next approved feature work: none; the repair request is complete.

## Profile Documents library — 2026-09-05

- Documents is now the fifth Profile tab and sidebar child at `/profile/documents`. The top-level Documents item is removed; `/documents` redirects with history replacement. The existing Profile heading, student summary, issues, and tab shell remain shared. [Profile Documents](profile-documents.md) records the reference-page matrix, contracts, ownership, and current behaviour.
- Student Documents projects existing passport and qualification filenames directly from canonical owners and adds student-specific enrolment letters. University Documents projects the active enrolment campus's Student Handbook. Stable source-based row IDs preserve distinct documents with identical filenames. New canonical library records, graph validation, a read-only Portal API/query boundary, explorer tables/relationships/source-field metadata, and all behaviour documents are synchronized.
- Two feature-owned table compositions reuse ContentSection, bordered DataTable, IconButton, and EmptyState. View/Download remain keyboard-accessible no-op placeholders. No file delivery, preview, upload, document management, or persistence was added.
- Browser coverage exposed and fixed shared-tab duplicate navigation from mouse-down/focus/click events before controlled route updates. New tab gestures now create one navigation; current-tab resets remain available. Main-scroll reset includes location keys so reselecting the current sidebar destination also returns to the top. The Design System Explorer displays tab-activation counts.
- Verification: `npm run check:all` passes token validation, warning-free lint, 367 unit tests across 54 files, TypeScript/production build, production dev-tool exclusion, bundle budgets, and all 102 Playwright desktop runs at 1280px and 1440px. Both desktop screenshots were inspected; a separate keyboard End/Back browser check passes. `git diff --check`, all 42 documentation references, and the advisory change-surface review pass with no companion-layer gaps. The initial browser failures reproduced duplicate history entries and same-location scroll retention, both now fixed and regression-covered. No quality-gate warnings remain.
- Next approved work item: none; the Documents feature and verification are complete. File actions remain placeholders until functional delivery is explicitly approved.

### University Forms removal — 2026-09-05

- Removed the canonical University Forms entry from Profile Documents; the Student Handbook remains. The explorer description, empty-state copy, source-of-truth documents, and document-list tests are synchronized. Online forms are deferred to a later feature and are not being implemented now.
- Verification: `npm run check:all` passed token checks, lint, TypeScript, production build, production exclusions, bundle checks, all 367 unit tests across 54 files, and all 102 desktop browser tests at 1280 px and 1440 px. `git diff --check` passed. Change-surface advisory prompts were reviewed: the existing API projection and explorer samples derive their rows from the canonical graph, so contracts, queries, and relationships require no changes.

## Campus Services — 2026-09-05

- Implemented the eight read-only Cyberjaya Services pages with an expandable sidebar group, route-backed tabs and campus context. The existing standalone Accommodation placeholder and student Finance page remain separate from the similarly named service information tabs. [Campus Services](campus-services.md) records the page-reference matrix, navigation, content and API behavior.
- Canonical campus page/contact records drive a validated catalogue and detail API, scoped queries, structured reusable content blocks and explorer relationships/samples. Different campuses can publish different categories and ordering; absent enrolments and unconfigured campuses never receive Cyberjaya fallback content. All 42 source-content PNGs are stored locally with provenance, dimensions, byte counts and checksums in [the source manifest](services-source-manifest.json).
- Added an opt-in scrolling tab row to the shared panel and its Design System Explorer example. Browser checks exposed a fractional-width/focus-inset clipping issue during keyboard navigation and Community resizing; rendered-bound measurements now preserve visibility. Existing tab selection deduplication remains intact.
- Verification: `npm run check:all` passed direct-token checks, the 42-image integrity audit, warning-free lint, all 384 unit tests across 55 files, TypeScript/production build, production dev-tool exclusion, bundle budgets, and all 118 Playwright desktop runs at 1280px and 1440px (2.8 minutes). Largest JavaScript chunk: 340,900 bytes; eager JavaScript: 187,212 gzip bytes. The source-image contact sheet and both desktop layouts were visually inspected. All 31 local documentation links across the seven changed Markdown files resolve; `git diff --check` and the change-surface review pass with no companion-layer gaps.
- Verification fixes: asset integrity runs in a separate Node script to keep application timer types browser-only. The Design System regression now checks both standard and scrolling panel examples. The Documents no-op regression waits for the shell's scheduled chat preload before observing requests; its strict no-request/download assertions remain. Initial runs exposed these issues; the final complete rerun passes without warnings.
- Source limitation: the website's rates, opening hours, journey times, library counts and card benefits are imported snapshots, without independent operator confirmation. No bookings, purchases, forms, payments, contact sending, uploads or persistence were added. Next approved work item: none; the Services implementation and verification are complete.

## Services tab-panel banners — 2026-09-06

- Added shared `TabbedPagePanelBanner` and a banner slot inside the active tab panel, before its normal 16px body padding. All eight Services pages move their existing title, introduction and lead image into a 288px cover banner with 32px internal text insets, a fixed 208px copy column, 24px title and 14px body typography. Body sections and non-banner panels retain their established layouts.
- Campus-owned banner appearance travels through the existing validated service-detail response and explorer metadata; omitted appearance normalizes to dark text with no gradient. Wellness and Recreation use light text. After visual review, the owner explicitly approved a light gradient for Transportation alone; the component default and other seven pages remain gradient-free.
- Missing media preserves the banner height and readable text on a portal surface. Loading uses the same geometry, and unusually long copy remains available in a keyboard-focusable scroll region. The Design System Explorer includes dark/light, gradient-toggle and absent-media/description examples. Source files and copy are unchanged; the source manifest records display cropping and the approved Transportation treatment.
- Verification: `npm run check:all` passed token checks, all 42 source-image integrity checks, warning-free lint, 391 unit tests across 56 files, TypeScript/production build, production dev-tool exclusion, bundle budgets and all 120 desktop Playwright runs at 1280px and 1440px (3.0 minutes). The banner matrix covers eight pages with Community open, closed and resized; final crops and copy were visually reviewed at both widths. Largest JavaScript chunk: 340,900 bytes; eager JavaScript: 187,825 gzip bytes. All 23 local documentation references and `git diff --check` pass. The change-surface prompt was reviewed: the existing validated detail projection already returns the canonical page, so appearance requires no new API method, query or campus relationship. The initial test-query typing error was corrected before the final full rerun; no quality-gate warnings remain.
- Refinement: rewrote each canonical Services banner introduction as three authored lines. The shared `whitespace-pre-line` rendering preserves that compact layout, including in the fixed-width loading geometry; Transportation retains its approved light gradient.
- Verification update: focused banner and Services tests passed (25 unit tests and 18 desktop flows); the full `npm run check:all` rerun passed token and image-integrity checks, lint, 392 unit tests across 56 files, production build and bundle budgets, plus all 120 desktop Playwright runs at 1280px and 1440px (3.1 minutes). The first full run had one unrelated Documents scenario-card timeout under parallel load; its isolated 10-run suite and the clean complete rerun both passed. `git diff --check` and the change-surface review pass with no companion-layer gaps.
- Scope remains read-only and frontend-only. The banner change and verification are complete; no further work item is approved.

## Known warnings

- No warnings in the latest full quality gate.
- Production tax warning: sponsor, diplomat-dependant, and award-netting treatment is provisional and must be reviewed against current RMCD rules before backend implementation or official invoice issuance.
- Next approved work item: none. Online Finance payment and campus verification, Resume preview/export, and Portfolio public sharing/storage remain deferred until explicitly approved; await the product owner's next feature or refinement.

## Starting and closing a session

1. Start: pull the current branch, read the documents above, run `git status --short --branch`, and run the checks relevant to the work before editing.
2. Close: update affected documentation and this handoff, run the checks required by the change; `npm run check:all` is required before a commit or push. Commit a focused change and push it.
3. New-chat prompt: “I am resuming `cms-student-portal`. Read `AGENTS.md` and `docs/handoff.md`, then the relevant detailed docs. Check Git status and summarize the baseline before making changes. My task is: …”
